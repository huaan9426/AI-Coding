# 第9章 监控与可观测性

在前面的章节中，我们深入学习了Kubernetes的核心组件、工作负载管理、服务发现、配置管理、资源调度和存储管理。这些知识让我们能够成功部署和运行容器化应用。但在生产环境中，仅仅让应用运行起来是远远不够的，我们还需要回答以下关键问题：

**运维三大灵魂拷问**：
1. **系统现在怎么样？** - 实时监控（Metrics）
2. **出问题了怎么办？** - 日志追踪（Logging）
3. **为什么会这样？** - 链路追踪（Tracing）

考虑以下生产场景：

📊 **监控场景**：
- Pod内存使用率突然从30%飙升到95%，但没有告警
- 节点CPU持续高负载，但不知道是哪个Pod导致的
- PVC存储容量即将耗尽，没有提前预警
- API Server请求延迟增加3倍，用户开始投诉

🔍 **日志场景**：
- 用户报告支付失败，但不知道错误发生在哪个微服务
- 应用崩溃重启，但日志随容器消失无法排查
- 多个Pod分布在不同节点，日志分散无法统一查询
- 日志量巨大（TB级），搜索速度慢且成本高

🔗 **追踪场景**：
- 一个API请求经过10个微服务，总耗时5秒，瓶颈在哪？
- 订单处理失败，但不知道是库存服务还是支付服务的问题
- 分布式事务跨越多个服务，无法定位失败原因

这就是**可观测性 (Observability)** 要解决的核心问题。可观测性是现代云原生应用的基石，它让我们能够：
- **主动发现问题** - 在用户投诉前就发现异常
- **快速定位根因** - 分钟级而非小时级故障排查
- **优化系统性能** - 基于数据驱动的性能调优
- **容量规划决策** - 准确预测资源需求，降低成本

## 可观测性三大支柱

Kubernetes生态提供了完整的可观测性解决方案，遵循业界公认的"三大支柱"理念：

### 1️⃣ Metrics (指标监控)

**定义**：聚合的数值型时序数据，用于量化系统状态

**典型指标**：
- **基础设施层**：CPU使用率、内存占用、磁盘IOPS、网络带宽
- **容器层**：Pod重启次数、容器OOM次数、镜像拉取时长
- **应用层**：HTTP请求QPS、API延迟P99、错误率、业务指标（订单量）

**技术栈**：Prometheus + Grafana

### 2️⃣ Logging (日志追踪)

**定义**：离散的事件记录，用于描述系统行为

**日志类型**：
- **应用日志**：业务逻辑输出（订单创建、用户登录）
- **审计日志**：安全合规记录（API调用、权限变更）
- **系统日志**：组件运行日志（kubelet、kube-proxy）

**技术栈**：EFK (Elasticsearch + Fluentd + Kibana) 或 Loki + Promtail

### 3️⃣ Tracing (链路追踪)

**定义**：分布式请求的完整调用链路，用于分析服务依赖

**追踪内容**：
- 请求从网关到数据库的完整路径
- 每个微服务的处理耗时和依赖关系
- 跨服务调用的上下文传递

**技术栈**：Jaeger / Zipkin / Tempo

## 本章学习目标

通过本章学习，你将掌握：

✅ **Metrics监控体系**: 理解Prometheus架构，掌握PromQL查询语言，配置Grafana仪表盘
✅ **指标采集实战**: 部署Metrics Server、Node Exporter、kube-state-metrics
✅ **告警规则配置**: 使用AlertManager实现多渠道告警（邮件/Slack/钉钉/企业微信）
✅ **日志收集架构**: 部署EFK栈，实现容器日志的集中收集、索引和查询
✅ **日志查询分析**: 掌握Kibana/Loki查询语法，快速定位问题日志
✅ **分布式追踪**: 部署Jaeger，实现微服务调用链路的可视化分析
✅ **性能分析工具**: 使用kubectl top、Prometheus查询、火焰图分析性能瓶颈
✅ **生产最佳实践**: 监控指标体系设计、告警策略制定、成本优化

## 本章内容概览

```
第9章: 监控与可观测性
├── 9.1 监控基础与架构设计
│   ├── 可观测性核心概念
│   ├── Kubernetes监控架构
│   ├── Metrics API与资源指标
│   └── 监控数据采集路径
│
├── 9.2 Prometheus核心原理与实战
│   ├── Prometheus架构与数据模型
│   ├── 服务发现与目标抓取
│   ├── PromQL查询语言详解
│   ├── Recording Rules性能优化
│   └── 数据持久化与高可用
│
├── 9.3 Grafana可视化与仪表盘
│   ├── Grafana部署与配置
│   ├── 数据源集成（Prometheus/Loki/Jaeger）
│   ├── 仪表盘设计最佳实践
│   ├── 变量与模板应用
│   └── 企业级仪表盘示例
│
├── 9.4 告警规则与AlertManager
│   ├── AlertManager架构与配置
│   ├── 告警规则编写（CPU/内存/磁盘/Pod）
│   ├── 告警路由与分组
│   ├── 抑制与静默策略
│   └── 多渠道通知集成（钉钉/企业微信/PagerDuty）
│
├── 9.5 日志管理与EFK栈
│   ├── 日志收集架构设计
│   ├── Fluentd/Fluent Bit部署
│   ├── Elasticsearch集群搭建
│   ├── Kibana日志查询与分析
│   └── Loki轻量级日志方案
│
├── 9.6 分布式追踪与Jaeger
│   ├── OpenTelemetry标准
│   ├── Jaeger架构与部署
│   ├── 应用埋点与SDK集成
│   ├── 调用链路分析实战
│   └── 性能瓶颈定位
│
├── 9.7 Kubernetes事件监控
│   ├── Events资源详解
│   ├── kube-eventer部署
│   ├── 事件持久化存储
│   └── 关键事件告警
│
└── 9.8 监控最佳实践与生产案例
    ├── 监控指标体系设计（USE/RED方法）
    ├── 黄金指标与SLI/SLO/SLA
    ├── 成本优化（数据保留策略/采样率）
    ├── 生产故障案例（OOM/磁盘满/雪崩）
    └── 企业级监控平台架构
```

## 学习路径建议

**初学者路径**（掌握基础监控）：
1. 先学习 9.1 监控基础，理解核心概念
2. 部署 Metrics Server，使用 `kubectl top` 查看资源
3. 学习 9.2 Prometheus 基础，部署单机版
4. 配置 9.3 Grafana 仪表盘，可视化集群状态
5. 实践 9.4 简单告警规则（CPU/内存阈值）

**进阶路径**（构建完整可观测性）：
1. 深入学习 9.2 PromQL，编写复杂查询
2. 部署 9.5 EFK 栈，实现日志集中管理
3. 配置 9.4 高级告警（基于变化率/趋势预测）
4. 学习 9.6 Jaeger，分析微服务调用链
5. 掌握 9.8 最佳实践，设计企业级监控体系

**生产环境准备**：
- Prometheus集群高可用（多副本+Thanos）
- 日志存储成本优化（冷热分离+生命周期管理）
- 告警降噪策略（抑制规则+分级通知）
- 监控数据安全（RBAC+加密传输）

---

## 技术栈总览

本章涉及的核心技术栈：

| 类别 | 开源方案 | 商业方案 | 云原生方案 |
|------|---------|---------|-----------|
| **Metrics** | Prometheus + Grafana | Datadog, New Relic | AWS CloudWatch, GCP Monitoring |
| **Logging** | EFK (Elasticsearch + Fluentd + Kibana) | Splunk, Sumo Logic | AWS CloudWatch Logs, GCP Logging |
| **Tracing** | Jaeger, Zipkin | Datadog APM, Dynatrace | AWS X-Ray, GCP Trace |
| **集成方案** | Loki + Tempo + Grafana (LGTM栈) | Elastic Observability | - |

**本章选择开源方案的原因**：
- ✅ 零成本，适合学习和中小企业
- ✅ 社区活跃，文档丰富
- ✅ 云厂商兼容（可无缝迁移到托管服务）
- ✅ 企业级生产验证（Netflix、Uber、Shopify等使用）

---

**准备好了吗？** 让我们从监控基础开始，逐步构建完整的Kubernetes可观测性平台！

---


## 9.1 监控基础与架构设计

### 9.1.1 可观测性核心概念

#### 什么是可观测性？

**可观测性 (Observability)** 不等于监控 (Monitoring)，它是一个更广泛的概念：

```
监控 (Monitoring)：
  - 已知问题的检测（我知道可能会出现CPU过高，所以设置告警）
  - 预定义指标的收集（固定的Dashboard）
  - 被动响应（收到告警后排查）

可观测性 (Observability)：
  - 未知问题的探索（系统出现从未见过的异常，能快速定位）
  - 任意维度的查询（可以自由组合各种条件查询）
  - 主动洞察（通过数据发现潜在问题）
```

**核心区别示例**：

假设生产环境出现"用户支付成功率从99.5%降到95%"：

```
传统监控思路：
1. 查看预设的Dashboard → 没有"支付成功率"这个指标
2. 登录各个Pod查看日志 → 耗时且分散
3. 猜测可能的原因 → 数据库？网络？代码bug？
4. 逐个排查 → 可能需要数小时

可观测性思路：
1. Metrics: 查询支付API的P99延迟 → 发现从200ms升到2s
2. Tracing: 调用链分析 → 定位到"库存查询"步骤耗时增加
3. Logging: 查看库存服务日志 → 发现大量"connection timeout"
4. 根因定位：库存服务数据库连接池耗尽 → 5分钟内修复
```

#### 可观测性的价值

**业务价值**：

| 维度 | 传统方式 | 可观测性方式 | 价值提升 |
|------|---------|-------------|---------|
| **故障发现** | 用户投诉后被动发现 | 提前5-30分钟主动告警 | 减少用户影响面80% |
| **故障定位** | 平均2-4小时 (MTTR) | 平均5-15分钟 | MTTR降低90% |
| **容量规划** | 基于经验猜测 | 基于历史趋势预测 | 成本降低30-50% |
| **性能优化** | 感觉哪里慢就优化哪里 | 数据驱动，精准定位瓶颈 | 优化效果提升5-10倍 |

**真实案例**：

某电商公司在引入完整可观测性体系后：
- ✅ 99.9% SLA达成率从92%提升到99.7%
- ✅ 平均故障恢复时间从3.5小时降至12分钟
- ✅ 通过容量优化节省云成本38%（年省200万美元）
- ✅ 性能问题定位时间从平均2天降至1小时

#### 可观测性三大支柱详解

##### 1. Metrics (指标监控)

**数据特点**：
- 聚合的数值型数据（如：平均值、P99、累加值）
- 时序存储（时间戳 + 标签 + 数值）
- 低存储成本（每个指标约1-2 bytes/sample）

**典型应用场景**：
```yaml
# 场景1: 资源监控
node_cpu_usage{node="node1", cpu="0"} 45.2    # CPU使用率45.2%
container_memory_usage{pod="nginx-abc", namespace="prod"} 256000000  # 内存256MB

# 场景2: 应用性能
http_requests_total{method="GET", path="/api/users", status="200"} 15840  # 请求总数
http_request_duration_seconds{quantile="0.99"} 0.235  # P99延迟235ms

# 场景3: 业务指标
order_created_total{region="us-west", payment_method="credit_card"} 3580  # 订单数
payment_success_rate 0.995  # 支付成功率99.5%
```

**优势**：
- ✅ 查询速度快（毫秒级）
- ✅ 存储成本低（TB级数据/月仅需几GB）
- ✅ 适合实时告警（秒级检测阈值）
- ✅ 易于可视化（折线图、饼图、热力图）

**局限**：
- ❌ 无法查看详细事件（只知道错误率升高，不知道具体错误信息）
- ❌ 丢失上下文（不知道是哪个用户、哪个订单出错）

##### 2. Logging (日志追踪)

**数据特点**：
- 离散的事件记录（每条日志是一个完整事件）
- 包含丰富上下文（时间、级别、消息、堆栈等）
- 存储成本高（每GB日志约占用1-3GB存储）

**日志级别**：
```python
# 应用日志示例
DEBUG: [2024-01-20 10:30:15] User session cache hit: user_id=12345
INFO:  [2024-01-20 10:30:16] Order created: order_id=ORD-789, amount=99.99, user=12345
WARN:  [2024-01-20 10:30:17] Payment retry attempt 2/3: gateway_timeout
ERROR: [2024-01-20 10:30:18] Payment failed: order=ORD-789, error=ConnectionTimeout
FATAL: [2024-01-20 10:30:19] Database connection pool exhausted
```

**结构化日志 vs 非结构化日志**：

```json
// 非结构化日志 (难以查询)
"2024-01-20 10:30:16 User john@example.com created order ORD-789 with amount $99.99"

// 结构化日志 (易于查询和分析)
{
  "timestamp": "2024-01-20T10:30:16Z",
  "level": "INFO",
  "message": "Order created",
  "order_id": "ORD-789",
  "user_email": "john@example.com",
  "amount": 99.99,
  "currency": "USD",
  "trace_id": "a1b2c3d4e5f6"  // 关联Tracing
}
```

**优势**：
- ✅ 包含完整上下文（能看到详细错误信息、堆栈追踪）
- ✅ 支持全文搜索（快速找到特定错误消息）
- ✅ 适合问题排查（结合时间戳精准定位）

**局限**：
- ❌ 存储成本高（日志量大的系统每月数TB）
- ❌ 查询速度慢（全文搜索TB级数据可能需要数秒到数分钟）
- ❌ 难以聚合分析（不适合统计趋势）

##### 3. Tracing (链路追踪)

**数据特点**：
- 分布式请求的完整调用链（一次用户请求可能跨越10+服务）
- 包含时序关系和依赖（哪个步骤先执行，哪个服务调用了哪个服务）
- 采样存储（通常只保存1-10%的请求，否则数据量过大）

**核心概念**：

```
Trace (追踪): 一次完整的请求链路
  └── Span (跨度): 链路中的一个操作步骤
      ├── Span ID: 当前步骤的唯一标识
      ├── Parent Span ID: 父级步骤的ID
      ├── Start Time: 开始时间
      ├── Duration: 持续时长
      └── Tags/Logs: 附加信息（HTTP状态码、错误信息等）
```

**实际案例**：

一次"用户下单"请求的完整Trace：

```
[Trace ID: a1b2c3d4e5f6]
│
├── [Span 1] API Gateway (总耗时: 1250ms)
│   │
│   ├── [Span 2] 用户认证服务 (100ms)
│   │   └── [Span 3] Redis查询缓存 (5ms)
│   │
│   ├── [Span 4] 订单服务 (1100ms)
│   │   ├── [Span 5] 库存服务 (800ms) ← 瓶颈！
│   │   │   └── [Span 6] MySQL查询库存 (780ms) ← 根因：慢查询
│   │   │
│   │   ├── [Span 7] 优惠券服务 (50ms)
│   │   │
│   │   └── [Span 8] 支付服务 (200ms)
│   │       └── [Span 9] 第三方支付网关 (180ms)
│   │
│   └── [Span 10] 日志记录 (50ms)
```

通过Tracing可以一眼看出：
- ✅ 总耗时1250ms，其中库存服务占64%（800ms）
- ✅ 库存服务的瓶颈在MySQL查询（780ms）
- ✅ 优化方向：优化库存查询SQL或增加缓存

**优势**：
- ✅ 可视化服务依赖（自动生成服务拓扑图）
- ✅ 精准定位瓶颈（每个步骤的耗时一目了然）
- ✅ 理解复杂调用链（微服务架构下尤为重要）

**局限**：
- ❌ 需要应用埋点（代码侵入，增加开发成本）
- ❌ 采样率权衡（100%采样存储成本高，低采样率可能漏掉关键请求）
- ❌ 学习曲线（需要理解分布式追踪概念）

#### 三大支柱的协同使用

**典型故障排查流程**：

```
步骤1: Metrics发现异常
  → Grafana Dashboard显示"API错误率从0.1%升到5%"

步骤2: Metrics定位时间和范围
  → PromQL查询: rate(http_requests_total{status=~"5.."}[5m])
  → 发现是"订单服务"在"10:30-10:45"错误率异常

步骤3: Tracing定位具体服务
  → Jaeger查询"订单服务"在该时间段的Trace
  → 发现80%的慢请求都卡在"库存查询"步骤

步骤4: Logging查看详细错误
  → Kibana搜索: service="inventory" AND level="ERROR" AND time:[10:30 TO 10:45]
  → 找到错误日志: "com.mysql.jdbc.exceptions.MySQLTimeoutException: Connection timeout"

步骤5: 根因分析
  → 结合Metrics: MySQL连接数从50突增到200（连接池上限）
  → 结合Logging: 发现某个定时任务在10:30启动，执行了大量慢查询
  → 解决方案: 优化慢查询SQL，增加连接池大小

总耗时: 5-10分钟（如果只用Logging可能需要1-2小时）
```

**协同价值总结**：

| 问题类型 | 主要使用工具 | 辅助工具 |
|---------|------------|---------|
| 发现异常 | Metrics (Prometheus告警) | - |
| 定位服务 | Tracing (Jaeger) | Metrics (缩小时间范围) |
| 查看详情 | Logging (Kibana) | Tracing (获取Trace ID) |
| 趋势分析 | Metrics (Grafana Dashboard) | - |
| 容量规划 | Metrics (历史数据分析) | Logging (错误日志统计) |

---

### 9.1.2 Kubernetes监控架构

#### Kubernetes监控层次

Kubernetes的监控需要覆盖多个层次，每一层都有不同的关注点：

```
┌─────────────────────────────────────────────────────────────┐
│                     应用层 (Application)                      │
│  ● 业务指标: 订单量、支付成功率、用户活跃度                     │
│  ● 应用性能: API延迟、错误率、QPS                              │
│  ● 自定义Metrics: /metrics端点暴露的业务指标                   │
├─────────────────────────────────────────────────────────────┤
│                   容器层 (Container)                          │
│  ● 容器资源: CPU、内存、网络、磁盘IO                            │
│  ● 容器状态: 重启次数、OOM次数、镜像拉取时长                     │
│  ● 容器日志: stdout/stderr输出                                │
├─────────────────────────────────────────────────────────────┤
│                    Pod层 (Pod)                               │
│  ● Pod状态: Running/Pending/Failed/CrashLoopBackOff          │
│  ● Pod事件: 调度失败、健康检查失败、Volume挂载失败               │
│  ● Pod资源: requests/limits设置、实际使用量                    │
├─────────────────────────────────────────────────────────────┤
│              Kubernetes资源层 (K8s Resources)                │
│  ● Deployment: 副本数、可用副本、更新状态                       │
│  ● Service: Endpoint数量、流量分布                            │
│  ● PVC/PV: 存储使用率、IOPS、吞吐量                           │
│  ● Node: 节点状态、可调度资源、污点和标签                       │
├─────────────────────────────────────────────────────────────┤
│                   节点层 (Node)                              │
│  ● 系统资源: CPU、内存、磁盘、网络                             │
│  ● 系统服务: kubelet、kube-proxy、容器运行时                   │
│  ● 系统日志: /var/log/messages、journalctl                   │
├─────────────────────────────────────────────────────────────┤
│               控制平面层 (Control Plane)                      │
│  ● API Server: 请求延迟、错误率、审计日志                      │
│  ● etcd: 数据库大小、写入延迟、Leader选举                      │
│  ● Scheduler: 调度延迟、队列长度、失败次数                     │
│  ● Controller Manager: Reconcile循环耗时、工作队列深度        │
└─────────────────────────────────────────────────────────────┘
```

#### Kubernetes原生监控架构

Kubernetes提供了两套官方监控API：

##### 1. Resource Metrics API (资源指标)

**用途**：提供Pod和Node的CPU/内存使用情况，用于HPA和VPA

**架构**：

```
┌──────────────┐
│ kubectl top  │  (查询CPU/内存)
└──────┬───────┘
       │
       ↓
┌──────────────────────────────────────┐
│   API Server (Metrics API)           │
│   /apis/metrics.k8s.io/v1beta1/      │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│   Metrics Server                     │  (聚合器)
│   - 从kubelet抓取指标                 │
│   - 内存存储(最近15分钟)               │
│   - 不持久化                          │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│   kubelet (每个Node)                 │
│   - cAdvisor: 容器资源数据            │
│   - /stats/summary API               │
└──────────────────────────────────────┘
```

**Metrics Server的局限**：
- ❌ 只保存最近15分钟数据（无法查看历史趋势）
- ❌ 只有CPU/内存（无法查看网络、磁盘IO）
- ❌ 无法自定义指标（只能看资源，看不到业务指标）
- ❌ 不支持告警（需要配合其他工具）

**适用场景**：
- ✅ HPA自动扩缩容（基于CPU/内存）
- ✅ 快速查看当前资源使用（kubectl top）
- ✅ 轻量级部署（小型集群）

##### 2. Custom Metrics API (自定义指标)

**用途**：支持任意自定义指标（如：QPS、错误率），用于高级HPA

**架构**：

```
┌──────────────────────────────────────┐
│   HPA Controller                     │
│   (根据自定义指标扩缩容)               │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│   API Server                         │
│   /apis/custom.metrics.k8s.io/v1beta1│
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│   Prometheus Adapter                 │  (适配器)
│   - 将Prometheus指标转换为K8s API     │
│   - 支持PromQL查询                    │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│   Prometheus                         │
│   - 抓取应用的/metrics端点            │
│   - 存储时序数据                      │
└──────────────────────────────────────┘
```

**示例**：基于QPS自动扩缩容

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Pods
    pods:
      metric:
        name: http_requests_per_second  # 自定义指标
      target:
        type: AverageValue
        averageValue: "1000"  # 每个Pod处理1000 QPS时扩容
```

#### 完整的生产级监控架构

```
                         ┌─────────────────────────────────────┐
                         │      外部用户/运维人员                │
                         └─────────────┬───────────────────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
            ┌───────▼────────┐  ┌─────▼──────┐  ┌───────▼────────┐
            │   Grafana      │  │  Kibana    │  │  Jaeger UI     │
            │  (可视化)       │  │ (日志查询)  │  │  (链路追踪)     │
            └───────┬────────┘  └─────┬──────┘  └───────┬────────┘
                    │                 │                  │
         ┌──────────┼─────────────────┼──────────────────┘
         │          │                 │
┌────────▼────┐ ┌───▼──────────┐ ┌───▼─────────────┐
│ Prometheus  │ │ Elasticsearch│ │ Jaeger Collector│
│ (Metrics)   │ │  (日志存储)   │ │  (Trace存储)    │
└────────┬────┘ └───▲──────────┘ └───▲─────────────┘
         │          │                │
         │  ┌───────┴────────┐  ┌────┴─────────┐
         │  │   Fluentd      │  │ Jaeger Agent │
         │  │  (日志收集)     │  │  (Trace采集) │
         │  └───────▲────────┘  └────▲─────────┘
         │          │                │
         │  ┌───────┴────────────────┴─────────────┐
         │  │                                       │
    ┌────▼──▼────────────────────────────────────┐ │
    │         Kubernetes Cluster                 │ │
    │  ┌──────────────────────────────────────┐  │ │
    │  │ kube-state-metrics (K8s资源状态)      │◄─┘ │
    │  └──────────────────────────────────────┘    │
    │  ┌──────────────────────────────────────┐    │
    │  │ Node Exporter (节点指标)              │◄───┤
    │  └──────────────────────────────────────┘    │
    │  ┌──────────────────────────────────────┐    │
    │  │ Application Pods (应用指标/日志)      │◄───┘
    │  │  - /metrics (Prometheus格式)          │
    │  │  - stdout/stderr日志                  │
    │  │  - OpenTelemetry SDK (Tracing埋点)    │
    │  └──────────────────────────────────────┘    │
    └─────────────────────────────────────────────┘
```

**各组件职责**：

| 组件 | 职责 | 数据量级 |
|------|------|---------|
| **Prometheus** | 抓取和存储Metrics | 中等（每秒数千样本） |
| **kube-state-metrics** | 暴露K8s资源状态为Metrics | 低（资源对象级别） |
| **Node Exporter** | 暴露节点系统指标 | 中等（每个节点数百指标） |
| **Fluentd/Fluent Bit** | 收集容器日志并转发到ES | 高（每秒GB级别） |
| **Elasticsearch** | 存储和索引日志 | 极高（TB级别） |
| **Kibana** | 日志查询和可视化 | - |
| **Jaeger Agent** | 接收应用Trace数据并转发 | 中等（采样后） |
| **Jaeger Collector** | 聚合Trace并写入存储 | 中等 |
| **Grafana** | 统一可视化平台 | - |

---

### 9.1.3 Metrics API与资源指标

#### Metrics Server详解

**Metrics Server是什么？**

Metrics Server是Kubernetes官方提供的轻量级、短期的资源指标收集器。它从每个节点的kubelet收集CPU和内存使用数据，并通过Metrics API暴露给Kubernetes组件（如HPA、VPA、kubectl top）。

**核心特点**：
- ✅ 官方维护，稳定可靠
- ✅ 部署简单（单个Deployment）
- ✅ 资源占用低（每个节点仅需10-20MB内存）
- ✅ 与kubectl集成（kubectl top命令）
- ❌ 只保存最近数据（不适合历史分析）
- ❌ 功能有限（仅CPU/内存）

**架构图**：

```
┌─────────────────────────────────────────────────────────────┐
│                     Kubernetes API Server                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Aggregation Layer (API聚合层)                       │   │
│  │  - /apis/metrics.k8s.io/v1beta1/nodes                │   │
│  │  - /apis/metrics.k8s.io/v1beta1/pods                 │   │
│  │  - /apis/metrics.k8s.io/v1beta1/namespaces/{ns}/pods │   │
│  └─────────────────┬───────────────────────────────────┘   │
└────────────────────┼───────────────────────────────────────┘
                     │
                     ↓
        ┌────────────────────────────┐
        │   Metrics Server           │
        │  ┌──────────────────────┐  │
        │  │ In-Memory Storage    │  │ (仅保留15分钟数据)
        │  │ - CPU/Memory         │  │
        │  │ - 60秒刷新间隔        │  │
        │  └──────────────────────┘  │
        │  ┌──────────────────────┐  │
        │  │ Kubelet Client       │  │ (抓取kubelet数据)
        │  └──────────────────────┘  │
        └─────────┬──────────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
┌───▼────┐   ┌───▼────┐   ┌───▼────┐
│kubelet │   │kubelet │   │kubelet │
│ Node1  │   │ Node2  │   │ Node3  │
│  └──┐  │   │  └──┐  │   │  └──┐  │
│ cAdv│  │   │ cAdv│  │   │ cAdv│  │ (容器资源监控)
└─────┘  │   └─────┘  │   └─────┘  │
   Pod1     Pod2        Pod3
```

#### 部署Metrics Server

**1. 快速部署（官方YAML）**

```bash
# 下载官方部署文件
kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
```

**2. 自定义部署（生产环境推荐）**

```yaml
# metrics-server-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics-server
  namespace: kube-system
  labels:
    k8s-app: metrics-server
spec:
  replicas: 1  # 生产环境建议2个副本（高可用）
  selector:
    matchLabels:
      k8s-app: metrics-server
  template:
    metadata:
      labels:
        k8s-app: metrics-server
    spec:
      serviceAccountName: metrics-server
      containers:
      - name: metrics-server
        image: registry.k8s.io/metrics-server/metrics-server:v0.7.0
        imagePullPolicy: IfNotPresent
        args:
        - --cert-dir=/tmp
        - --secure-port=10250
        - --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname
        - --kubelet-use-node-status-port
        - --metric-resolution=15s  # 数据采集间隔（默认60s，可调整为15s提高精度）
        # 如果kubelet使用自签名证书，需要添加以下参数（测试环境）
        # - --kubelet-insecure-tls
        ports:
        - containerPort: 10250
          name: https
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /livez
            port: https
            scheme: HTTPS
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /readyz
            port: https
            scheme: HTTPS
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            cpu: 100m
            memory: 200Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          seccompProfile:
            type: RuntimeDefault
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp-dir
          mountPath: /tmp
      volumes:
      - name: tmp-dir
        emptyDir: {}
      priorityClassName: system-cluster-critical
      nodeSelector:
        kubernetes.io/os: linux
---
apiVersion: v1
kind: Service
metadata:
  name: metrics-server
  namespace: kube-system
  labels:
    k8s-app: metrics-server
spec:
  selector:
    k8s-app: metrics-server
  ports:
  - port: 443
    protocol: TCP
    targetPort: https
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: metrics-server
  namespace: kube-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: system:metrics-server
rules:
- apiGroups:
  - ""
  resources:
  - nodes/metrics
  verbs:
  - get
- apiGroups:
  - ""
  resources:
  - pods
  - nodes
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: system:metrics-server
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:metrics-server
subjects:
- kind: ServiceAccount
  name: metrics-server
  namespace: kube-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: system:aggregated-metrics-reader
  labels:
    rbac.authorization.k8s.io/aggregate-to-view: "true"
    rbac.authorization.k8s.io/aggregate-to-edit: "true"
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
rules:
- apiGroups:
  - metrics.k8s.io
  resources:
  - pods
  - nodes
  verbs:
  - get
  - list
  - watch
---
apiVersion: apiregistration.k8s.io/v1
kind: APIService
metadata:
  name: v1beta1.metrics.k8s.io
spec:
  service:
    name: metrics-server
    namespace: kube-system
  group: metrics.k8s.io
  version: v1beta1
  insecureSkipTLSVerify: true
  groupPriorityMinimum: 100
  versionPriority: 100
```

**3. 部署并验证**

```bash
# 部署
kubectl apply -f metrics-server-deployment.yaml

# 检查Pod状态
kubectl get pod -n kube-system -l k8s-app=metrics-server

# 输出示例:
# NAME                              READY   STATUS    RESTARTS   AGE
# metrics-server-5f8d6b7c9d-7x4ml   1/1     Running   0          2m

# 检查API注册
kubectl get apiservice v1beta1.metrics.k8s.io

# 输出示例:
# NAME                     SERVICE                      AVAILABLE   AGE
# v1beta1.metrics.k8s.io   kube-system/metrics-server   True        2m

# 测试查询（等待1-2分钟数据采集完成）
kubectl top nodes
kubectl top pods -A
```

**常见问题排查**：

```bash
# 问题1: Metrics Server Pod状态为CrashLoopBackOff
kubectl logs -n kube-system -l k8s-app=metrics-server

# 常见错误: "x509: cannot validate certificate for xxx because it doesn't contain any IP SANs"
# 解决: 添加 --kubelet-insecure-tls 参数（仅限测试环境）

# 问题2: kubectl top nodes 报错 "Error from server (ServiceUnavailable): the server is currently unable to handle the request"
# 原因: Metrics Server还在采集数据，等待1-2分钟

# 问题3: 查看详细日志
kubectl logs -n kube-system deploy/metrics-server --tail=100 -f
```

#### 使用kubectl top查看资源

**1. 查看节点资源**

```bash
# 查看所有节点的CPU和内存使用
kubectl top nodes

# 输出示例:
# NAME       CPU(cores)   CPU%   MEMORY(bytes)   MEMORY%
# node1      450m         22%    3500Mi          45%
# node2      320m         16%    2800Mi          36%
# node3      580m         29%    4200Mi          54%
```

**解读**：
- `CPU(cores)`: 使用的CPU核心数（450m = 0.45核）
- `CPU%`: 占节点总CPU的百分比（假设节点有2核，450m/2000m = 22.5%）
- `MEMORY(bytes)`: 内存使用量（3500Mi ≈ 3.5GB）
- `MEMORY%`: 占节点总内存的百分比

**2. 查看Pod资源**

```bash
# 查看所有命名空间的Pod
kubectl top pods -A

# 查看特定命名空间
kubectl top pods -n production

# 查看特定Pod的容器级别资源
kubectl top pods nginx-deployment-abc123 --containers

# 输出示例:
# POD                          CONTAINER    CPU(cores)   MEMORY(bytes)
# nginx-deployment-abc123      nginx        10m          50Mi
# nginx-deployment-abc123      sidecar      2m           20Mi

# 排序（按CPU降序）
kubectl top pods -A --sort-by=cpu

# 排序（按内存降序）
kubectl top pods -A --sort-by=memory
```

**3. 结合其他命令分析**

```bash
# 找出内存使用TOP 10的Pod
kubectl top pods -A --sort-by=memory | head -11

# 找出CPU使用超过100m的Pod
kubectl top pods -A | awk '$2 > 100'

# 对比requests/limits配置
kubectl get pods nginx-abc -o json | jq '.spec.containers[].resources'
kubectl top pod nginx-abc
```

#### Metrics API的编程使用

**1. 直接调用API**

```bash
# 查询所有节点指标
kubectl get --raw /apis/metrics.k8s.io/v1beta1/nodes | jq .

# 输出示例:
# {
#   "kind": "NodeMetricsList",
#   "apiVersion": "metrics.k8s.io/v1beta1",
#   "metadata": {},
#   "items": [
#     {
#       "metadata": {
#         "name": "node1",
#         "creationTimestamp": "2024-01-20T10:30:00Z"
#       },
#       "timestamp": "2024-01-20T10:29:45Z",
#       "window": "30s",
#       "usage": {
#         "cpu": "450m",
#         "memory": "3500Mi"
#       }
#     }
#   ]
# }

# 查询特定Pod指标
kubectl get --raw /apis/metrics.k8s.io/v1beta1/namespaces/default/pods/nginx-abc | jq .
```

**2. 使用client-go访问Metrics API (Go)**

```go
package main

import (
    "context"
    "fmt"
    metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
    "k8s.io/client-go/kubernetes"
    "k8s.io/client-go/tools/clientcmd"
    metricsv "k8s.io/metrics/pkg/client/clientset/versioned"
)

func main() {
    // 加载kubeconfig
    config, err := clientcmd.BuildConfigFromFlags("", "/home/user/.kube/config")
    if err != nil {
        panic(err)
    }

    // 创建Metrics客户端
    metricsClient, err := metricsv.NewForConfig(config)
    if err != nil {
        panic(err)
    }

    // 查询节点指标
    nodeMetrics, err := metricsClient.MetricsV1beta1().NodeMetricses().List(context.TODO(), metav1.ListOptions{})
    if err != nil {
        panic(err)
    }

    for _, node := range nodeMetrics.Items {
        cpu := node.Usage.Cpu().MilliValue()  // 转换为毫核
        memory := node.Usage.Memory().Value() / 1024 / 1024  // 转换为MiB
        fmt.Printf("Node: %s, CPU: %dm, Memory: %dMi\n", node.Name, cpu, memory)
    }

    // 查询Pod指标
    podMetrics, err := metricsClient.MetricsV1beta1().PodMetricses("default").List(context.TODO(), metav1.ListOptions{})
    if err != nil {
        panic(err)
    }

    for _, pod := range podMetrics.Items {
        for _, container := range pod.Containers {
            cpu := container.Usage.Cpu().MilliValue()
            memory := container.Usage.Memory().Value() / 1024 / 1024
            fmt.Printf("Pod: %s, Container: %s, CPU: %dm, Memory: %dMi\n",
                pod.Name, container.Name, cpu, memory)
        }
    }
}
```

**3. 使用Python访问Metrics API**

```python
from kubernetes import client, config

# 加载kubeconfig
config.load_kube_config()

# 创建API客户端
api_client = client.ApiClient()
custom_api = client.CustomObjectsApi(api_client)

# 查询节点指标
node_metrics = custom_api.list_cluster_custom_object(
    group="metrics.k8s.io",
    version="v1beta1",
    plural="nodes"
)

for node in node_metrics['items']:
    name = node['metadata']['name']
    cpu = node['usage']['cpu']
    memory = node['usage']['memory']
    print(f"Node: {name}, CPU: {cpu}, Memory: {memory}")

# 查询Pod指标
pod_metrics = custom_api.list_namespaced_custom_object(
    group="metrics.k8s.io",
    version="v1beta1",
    namespace="default",
    plural="pods"
)

for pod in pod_metrics['items']:
    pod_name = pod['metadata']['name']
    for container in pod['containers']:
        container_name = container['name']
        cpu = container['usage']['cpu']
        memory = container['usage']['memory']
        print(f"Pod: {pod_name}, Container: {container_name}, CPU: {cpu}, Memory: {memory}")
```

#### HPA基于Metrics自动扩缩容

**1. 基于CPU的HPA**

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: nginx-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nginx
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70  # CPU使用率超过70%时扩容
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300  # 缩容前等待5分钟观察
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60  # 每分钟最多缩容50%
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
      - type: Percent
        value: 100
        periodSeconds: 30  # 每30秒最多扩容100%（翻倍）
      - type: Pods
        value: 4
        periodSeconds: 30  # 每30秒最多增加4个Pod
      selectPolicy: Max  # 选择扩容最快的策略
```

**2. 基于内存的HPA**

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: java-app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: java-app
  minReplicas: 3
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80  # 内存使用率超过80%时扩容
```

**3. 多指标组合HPA**

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web
  minReplicas: 5
  maxReplicas: 50
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  # 任意一个指标达到阈值就触发扩容
```

**4. 验证HPA**

```bash
# 部署HPA
kubectl apply -f nginx-hpa.yaml

# 查看HPA状态
kubectl get hpa nginx-hpa

# 输出示例:
# NAME        REFERENCE          TARGETS   MINPODS   MAXPODS   REPLICAS   AGE
# nginx-hpa   Deployment/nginx   15%/70%   2         10        2          5m

# 查看详细信息
kubectl describe hpa nginx-hpa

# 模拟负载测试（触发扩容）
kubectl run -it --rm load-generator --image=busybox --restart=Never -- sh -c "while true; do wget -q -O- http://nginx; done"

# 观察HPA自动扩容
kubectl get hpa nginx-hpa --watch
```

---

### 9.1.4 监控数据采集路径

#### kubelet的cAdvisor

**cAdvisor (Container Advisor)** 是kubelet内置的容器资源监控组件，负责收集节点上所有容器的资源使用情况。

**cAdvisor暴露的API端点**：

```bash
# 1. Summary API (Metrics Server使用)
# 提供节点和Pod级别的聚合数据
curl -k https://NODE_IP:10250/stats/summary \
  --header "Authorization: Bearer $(kubectl get secret -n kube-system -o jsonpath='{.items[?(@.metadata.annotations.kubernetes\.io/service-account\.name=="default")].data.token}' | base64 -d)"

# 返回示例:
# {
#   "node": {
#     "nodeName": "node1",
#     "cpu": {
#       "time": "2024-01-20T10:30:00Z",
#       "usageNanoCores": 450000000,  // 450m CPU
#       "usageCoreNanoSeconds": 1234567890
#     },
#     "memory": {
#       "time": "2024-01-20T10:30:00Z",
#       "usageBytes": 3670016000,  // 3.5GB
#       "workingSetBytes": 3500000000
#     }
#   },
#   "pods": [...]
# }

# 2. Metrics API (Prometheus使用)
# 提供更详细的容器级别数据
curl -k https://NODE_IP:10250/metrics

# 返回Prometheus格式:
# container_cpu_usage_seconds_total{container="nginx",namespace="default",pod="nginx-abc"} 12.34
# container_memory_working_set_bytes{container="nginx",namespace="default",pod="nginx-abc"} 52428800
```

**cAdvisor监控的指标维度**：

| 指标类别 | 具体指标 | 说明 |
|---------|---------|------|
| **CPU** | cpu.usageNanoCores | 当前CPU使用（纳核） |
|  | cpu.usageCoreNanoSeconds | 累计CPU使用时间 |
| **内存** | memory.usageBytes | 总内存使用（包括缓存） |
|  | memory.workingSetBytes | 工作集内存（真实使用） |
|  | memory.rssBytes | RSS内存（匿名内存） |
|  | memory.pageFaults | 页面错误次数 |
| **网络** | network.rxBytes | 接收字节数 |
|  | network.txBytes | 发送字节数 |
|  | network.rxErrors | 接收错误数 |
| **磁盘** | fs.usedBytes | 文件系统使用量 |
|  | fs.capacityBytes | 文件系统总容量 |
| **容器** | startTime | 容器启动时间 |
|  | restartCount | 重启次数 |

#### Prometheus数据采集流程

Prometheus采用**拉模式 (Pull Model)** 采集数据，与传统的推模式（如StatsD）不同。

**完整的数据流**：

```
┌─────────────────────────────────────────────────────────────┐
│                      Prometheus Server                      │
│  ┌────────────────────────────────────────────────────┐     │
│  │  Service Discovery (服务发现)                       │     │
│  │  - Kubernetes API (自动发现Pod/Service/Node)        │     │
│  │  - 静态配置文件                                      │     │
│  └─────────┬──────────────────────────────────────────┘     │
│            │                                                 │
│  ┌─────────▼──────────────────────────────────────────┐     │
│  │  Scraper (抓取器)                                   │     │
│  │  - 定期HTTP GET /metrics                            │     │
│  │  - 默认间隔: 15s-60s                                │     │
│  └─────────┬──────────────────────────────────────────┘     │
│            │                                                 │
│  ┌─────────▼──────────────────────────────────────────┐     │
│  │  TSDB (时序数据库)                                  │     │
│  │  - 压缩存储 (每个样本约1-2字节)                      │     │
│  │  - 默认保留15天                                     │     │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ HTTP GET /metrics (每15s)
                          │
         ┌────────────────┼────────────────┬──────────────────┐
         │                │                │                  │
    ┌────▼─────┐    ┌────▼─────┐    ┌────▼─────┐      ┌────▼─────┐
    │kubelet   │    │kube-state│    │  Node    │      │  App     │
    │/metrics  │    │-metrics  │    │ Exporter │      │ /metrics │
    └──────────┘    └──────────┘    └──────────┘      └──────────┘
    (容器资源)       (K8s资源状态)   (节点系统指标)     (应用指标)
```

**Prometheus配置示例**：

```yaml
# prometheus-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s  # 全局抓取间隔
      evaluation_interval: 15s  # 规则评估间隔
      external_labels:
        cluster: 'production'
        region: 'us-west-2'

    # 抓取配置
    scrape_configs:
    # 1. 抓取kubelet (容器资源指标)
    - job_name: 'kubernetes-kubelet'
      scheme: https
      tls_config:
        ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
      bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
      kubernetes_sd_configs:
      - role: node  # 服务发现: 所有节点
      relabel_configs:
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)
      - target_label: __address__
        replacement: kubernetes.default.svc:443
      - source_labels: [__meta_kubernetes_node_name]
        regex: (.+)
        target_label: __metrics_path__
        replacement: /api/v1/nodes/${1}/proxy/metrics

    # 2. 抓取kube-state-metrics (K8s资源状态)
    - job_name: 'kube-state-metrics'
      kubernetes_sd_configs:
      - role: service
      relabel_configs:
      - source_labels: [__meta_kubernetes_service_label_app_kubernetes_io_name]
        action: keep
        regex: kube-state-metrics

    # 3. 抓取Node Exporter (节点系统指标)
    - job_name: 'node-exporter'
      kubernetes_sd_configs:
      - role: node
      relabel_configs:
      - source_labels: [__address__]
        regex: '(.*):10250'
        replacement: '${1}:9100'
        target_label: __address__

    # 4. 抓取应用Pod (自定义指标)
    - job_name: 'kubernetes-pods'
      kubernetes_sd_configs:
      - role: pod
      relabel_configs:
      # 只抓取带有 prometheus.io/scrape=true 注解的Pod
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
        action: keep
        regex: true
      # 自定义抓取端口 (默认/metrics)
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_port]
        action: replace
        target_label: __address__
        regex: (.+)
        replacement: $1
      # 自定义抓取路径
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
        action: replace
        target_label: __metrics_path__
        regex: (.+)
      # 添加命名空间标签
      - source_labels: [__meta_kubernetes_namespace]
        target_label: kubernetes_namespace
      # 添加Pod名称标签
      - source_labels: [__meta_kubernetes_pod_name]
        target_label: kubernetes_pod_name
```

**应用如何暴露Metrics**：

```yaml
# 示例: Go应用暴露Prometheus指标
apiVersion: v1
kind: Pod
metadata:
  name: my-app
  annotations:
    prometheus.io/scrape: "true"  # 告诉Prometheus抓取此Pod
    prometheus.io/port: "8080"    # 指标端口
    prometheus.io/path: "/metrics"  # 指标路径
spec:
  containers:
  - name: app
    image: my-app:v1.0
    ports:
    - containerPort: 8080
      name: metrics
```

```go
// Go应用代码示例
package main

import (
    "net/http"
    "github.com/prometheus/client_golang/prometheus"
    "github.com/prometheus/client_golang/prometheus/promhttp"
)

var (
    // 自定义Counter指标
    httpRequestsTotal = prometheus.NewCounterVec(
        prometheus.CounterOpts{
            Name: "http_requests_total",
            Help: "Total number of HTTP requests",
        },
        []string{"method", "endpoint", "status"},
    )

    // 自定义Histogram指标
    httpRequestDuration = prometheus.NewHistogramVec(
        prometheus.HistogramOpts{
            Name: "http_request_duration_seconds",
            Help: "HTTP request latency",
            Buckets: []float64{0.01, 0.05, 0.1, 0.5, 1, 2, 5},
        },
        []string{"method", "endpoint"},
    )
)

func init() {
    // 注册指标
    prometheus.MustRegister(httpRequestsTotal)
    prometheus.MustRegister(httpRequestDuration)
}

func main() {
    // 暴露/metrics端点
    http.Handle("/metrics", promhttp.Handler())
    
    // 业务Handler
    http.HandleFunc("/api/users", func(w http.ResponseWriter, r *http.Request) {
        timer := prometheus.NewTimer(httpRequestDuration.WithLabelValues(r.Method, "/api/users"))
        defer timer.ObserveDuration()

        // 业务逻辑...
        
        httpRequestsTotal.WithLabelValues(r.Method, "/api/users", "200").Inc()
        w.Write([]byte("OK"))
    })

    http.ListenAndServe(":8080", nil)
}
```

**访问/metrics端点的输出**：

```
# HELP http_requests_total Total number of HTTP requests
# TYPE http_requests_total counter
http_requests_total{method="GET",endpoint="/api/users",status="200"} 15840

# HELP http_request_duration_seconds HTTP request latency
# TYPE http_request_duration_seconds histogram
http_request_duration_seconds_bucket{method="GET",endpoint="/api/users",le="0.01"} 8500
http_request_duration_seconds_bucket{method="GET",endpoint="/api/users",le="0.05"} 14200
http_request_duration_seconds_bucket{method="GET",endpoint="/api/users",le="0.1"} 15600
http_request_duration_seconds_bucket{method="GET",endpoint="/api/users",le="0.5"} 15820
http_request_duration_seconds_bucket{method="GET",endpoint="/api/users",le="1"} 15838
http_request_duration_seconds_bucket{method="GET",endpoint="/api/users",le="+Inf"} 15840
http_request_duration_seconds_sum{method="GET",endpoint="/api/users"} 458.92
http_request_duration_seconds_count{method="GET",endpoint="/api/users"} 15840
```

#### 日志采集流程

**容器日志的生命周期**：

```
┌──────────────────────────────────────────────────────────┐
│                    Application Pod                       │
│  ┌────────────────────────────────────────────────┐     │
│  │  Application Container                          │     │
│  │  - stdout: 标准输出                              │     │
│  │  - stderr: 标准错误                              │     │
│  └──────────┬─────────────────────────────────────┘     │
└─────────────┼───────────────────────────────────────────┘
              │
              ↓
  ┌────────────────────────────────────┐
  │  Container Runtime (Docker/containerd) │
  │  - 写入日志文件:                     │
  │    /var/log/pods/<namespace>_<pod>_<uid>/<container>/0.log  │
  │  - JSON格式:                         │
  │    {"log":"[INFO] User logged in\n", "stream":"stdout", "time":"2024-01-20T10:30:00Z"} │
  └──────────┬─────────────────────────┘
              │
              ↓
  ┌────────────────────────────────────┐
  │  kubelet (日志轮转)                 │
  │  - 默认单文件上限: 10MB              │
  │  - 默认保留文件数: 5                │
  │  - 超过限制自动轮转                  │
  └──────────┬─────────────────────────┘
              │
              ↓
  ┌────────────────────────────────────┐
  │  Fluentd/Fluent Bit (DaemonSet)    │
  │  - 使用tail -f监听日志文件           │
  │  - 解析JSON格式                     │
  │  - 添加K8s元数据(Pod名/命名空间)     │
  │  - 过滤/转换/路由                   │
  └──────────┬─────────────────────────┘
              │
              ↓
  ┌────────────────────────────────────┐
  │  Elasticsearch / Loki              │
  │  - 索引和存储日志                   │
  │  - 支持全文搜索                     │
  └────────────────────────────────────┘
```

**Fluentd配置示例**：

```yaml
# fluentd-daemonset.yaml
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluentd
  namespace: kube-system
spec:
  selector:
    matchLabels:
      app: fluentd
  template:
    metadata:
      labels:
        app: fluentd
    spec:
      serviceAccountName: fluentd
      containers:
      - name: fluentd
        image: fluent/fluentd-kubernetes-daemonset:v1-debian-elasticsearch
        env:
        - name: FLUENT_ELASTICSEARCH_HOST
          value: "elasticsearch.monitoring.svc.cluster.local"
        - name: FLUENT_ELASTICSEARCH_PORT
          value: "9200"
        - name: FLUENT_UID
          value: "0"  # 以root运行（需要读取日志文件）
        resources:
          requests:
            cpu: 100m
            memory: 200Mi
          limits:
            cpu: 1000m
            memory: 512Mi
        volumeMounts:
        - name: varlog
          mountPath: /var/log
          readOnly: true
        - name: varlibdockercontainers
          mountPath: /var/lib/docker/containers
          readOnly: true
        - name: fluentd-config
          mountPath: /fluentd/etc/
      volumes:
      - name: varlog
        hostPath:
          path: /var/log
      - name: varlibdockercontainers
        hostPath:
          path: /var/lib/docker/containers
      - name: fluentd-config
        configMap:
          name: fluentd-config
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: fluentd-config
  namespace: kube-system
data:
  fluent.conf: |
    # 输入: 容器日志
    <source>
      @type tail
      path /var/log/containers/*.log
      pos_file /var/log/fluentd-containers.log.pos
      tag kubernetes.*
      read_from_head true
      <parse>
        @type json
        time_key time
        time_format %Y-%m-%dT%H:%M:%S.%NZ
      </parse>
    </source>

    # 过滤: 添加Kubernetes元数据
    <filter kubernetes.**>
      @type kubernetes_metadata
      @id filter_kube_metadata
    </filter>

    # 过滤: 排除系统命名空间的DEBUG日志
    <filter kubernetes.**>
      @type grep
      <exclude>
        key log
        pattern /DEBUG/
      </exclude>
      <exclude>
        key $.kubernetes.namespace_name
        pattern /^(kube-system|kube-public)$/
      </exclude>
    </filter>

    # 输出: 发送到Elasticsearch
    <match kubernetes.**>
      @type elasticsearch
      host elasticsearch.monitoring.svc.cluster.local
      port 9200
      logstash_format true
      logstash_prefix kubernetes
      <buffer>
        @type file
        path /var/log/fluentd-buffers/kubernetes.buffer
        flush_mode interval
        flush_interval 5s
        flush_at_shutdown true
        retry_max_interval 30
      </buffer>
    </match>
```

---

**📊 9.1节总结**

本节我们深入学习了Kubernetes监控的基础概念和架构设计：

✅ **可观测性三大支柱**：
- Metrics: 聚合的时序数据，快速发现异常
- Logging: 离散的事件记录，查看详细上下文
- Tracing: 分布式调用链路，定位性能瓶颈

✅ **Kubernetes监控架构**：
- 6层监控体系（应用→容器→Pod→K8s资源→节点→控制平面）
- Metrics Server提供基础资源指标
- Prometheus生态提供完整监控方案

✅ **Metrics API实战**：
- 部署Metrics Server
- 使用kubectl top查看资源
- 配置HPA自动扩缩容
- 编程访问Metrics API

✅ **监控数据采集**：
- kubelet的cAdvisor暴露容器指标
- Prometheus拉模式采集数据
- Fluentd收集和转发日志

**下一节预告**：我们将深入学习Prometheus的核心原理，包括数据模型、PromQL查询语言、服务发现机制，以及如何在生产环境部署高可用的Prometheus集群。

---

## 9.2 Prometheus核心原理与实战

在9.1节中，我们了解了Kubernetes监控的整体架构，以及Metrics API的基础用法。但Metrics Server的功能非常有限（仅CPU/内存，仅保留15分钟）。在生产环境中，我们需要一个功能强大、可扩展的监控解决方案，这就是**Prometheus**。

Prometheus是由SoundCloud开发并于2016年加入CNCF的开源监控系统，目前已成为云原生监控的事实标准。它被Netflix、Uber、DigitalOcean、GitLab等数千家公司在生产环境使用。

**为什么选择Prometheus？**

| 特性 | Prometheus | 传统监控(Zabbix/Nagios) |
|------|-----------|----------------------|
| **数据模型** | 多维时序数据(标签) | 单维数据(主机+指标) |
| **查询语言** | PromQL(灵活强大) | 固定模板 |
| **服务发现** | 原生支持K8s | 需手动配置 |
| **存储** | 本地时序数据库 | 关系型数据库 |
| **告警** | 内置AlertManager | 依赖外部插件 |
| **生态** | Grafana/Exporters丰富 | 生态较小 |

### 9.2.1 Prometheus架构与数据模型

#### Prometheus整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      Prometheus生态系统                          │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │               Prometheus Server                        │    │
│  │  ┌──────────────────────────────────────────────┐     │    │
│  │  │  Retrieval (数据抓取)                         │     │    │
│  │  │  - Service Discovery (服务发现)               │     │    │
│  │  │  - HTTP Pull (拉取/metrics端点)               │     │    │
│  │  │  - Scrape间隔: 15s-60s                       │     │    │
│  │  └──────────────┬───────────────────────────────┘     │    │
│  │                 │                                      │    │
│  │  ┌──────────────▼───────────────────────────────┐     │    │
│  │  │  TSDB (时序数据库)                            │     │    │
│  │  │  - 本地存储 (默认15天)                        │     │    │
│  │  │  - 压缩算法 (每样本1-2字节)                   │     │    │
│  │  │  - 块存储 (2小时一个块)                       │     │    │
│  │  └──────────────┬───────────────────────────────┘     │    │
│  │                 │                                      │    │
│  │  ┌──────────────▼───────────────────────────────┐     │    │
│  │  │  PromQL Engine (查询引擎)                     │     │    │
│  │  │  - 即时查询 (instant query)                   │     │    │
│  │  │  - 范围查询 (range query)                     │     │    │
│  │  └──────────────┬───────────────────────────────┘     │    │
│  │                 │                                      │    │
│  │  ┌──────────────▼───────────────────────────────┐     │    │
│  │  │  HTTP Server (API服务器)                      │     │    │
│  │  │  - /api/v1/query (即时查询)                   │     │    │
│  │  │  - /api/v1/query_range (范围查询)             │     │    │
│  │  │  - /api/v1/series (时间序列元数据)            │     │    │
│  │  └──────────────┬───────────────────────────────┘     │    │
│  └─────────────────┼──────────────────────────────────────┘    │
│                    │                                            │
│  ┌─────────────────┼────────────────────────────┐              │
│  │                 │                            │              │
│  │  ┌──────────────▼──────┐  ┌────────────────▼──────┐        │
│  │  │  Grafana            │  │  AlertManager         │        │
│  │  │  (可视化)            │  │  (告警管理)            │        │
│  │  │  - Dashboard        │  │  - 告警路由            │        │
│  │  │  - 图表/面板         │  │  - 分组/抑制/静默      │        │
│  │  └─────────────────────┘  └───────────┬───────────┘        │
│  │                                        │                    │
│  │                         ┌──────────────▼──────────────┐     │
│  │                         │  通知渠道                    │     │
│  │                         │  - Email / Slack / 钉钉     │     │
│  │                         │  - PagerDuty / Webhook     │     │
│  │                         └────────────────────────────┘     │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │               监控目标 (Targets)                          │ │
│  │  ┌────────────┐  ┌────────────┐  ┌──────────────────┐   │ │
│  │  │ Exporters  │  │ K8s资源    │  │ 应用程序          │   │ │
│  │  │ - Node     │  │ - kubelet  │  │ - /metrics端点   │   │ │
│  │  │ - MySQL    │  │ - kube-    │  │ - Prometheus SDK │   │ │
│  │  │ - Redis    │  │   state-   │  │                  │   │ │
│  │  │ - Nginx    │  │   metrics  │  │                  │   │ │
│  │  └────────────┘  └────────────┘  └──────────────────┘   │ │
│  └──────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**核心组件说明**：

1. **Prometheus Server**: 核心组件，负责抓取、存储和查询指标
2. **Exporters**: 第三方服务的指标暴露器（如MySQL Exporter）
3. **Pushgateway**: 支持短生命周期任务的推送（非K8s场景）
4. **AlertManager**: 告警管理和通知
5. **Grafana**: 可视化平台

#### Prometheus数据模型

Prometheus的数据模型是其强大的核心。它采用**多维时序数据**模型，每个指标由**指标名**和**标签集**唯一标识。

**1. 指标格式**

```
<metric_name>{<label_name>=<label_value>, ...} <sample_value> [<timestamp>]
```

示例：

```
# HTTP请求总数
http_requests_total{method="GET", endpoint="/api/users", status="200"} 15840 1705752000000

# 组件拆解:
# - metric_name: http_requests_total (指标名)
# - labels: method="GET", endpoint="/api/users", status="200" (标签)
# - value: 15840 (样本值)
# - timestamp: 1705752000000 (时间戳,毫秒,可选)
```

**2. 指标类型**

Prometheus定义了4种指标类型：

##### (1) Counter (计数器)

**特点**：
- 单调递增（只能增加，不能减少）
- 重启后归零
- 常用于累计型指标

**典型场景**：
```
# HTTP请求总数
http_requests_total{method="GET", endpoint="/api/users", status="200"} 15840

# 错误总数
http_errors_total{type="timeout"} 127

# 数据库查询总数
db_queries_total{database="users", operation="select"} 892340
```

**PromQL查询**：
```promql
# 查询Counter通常使用rate()或irate()计算速率

# 每秒请求数 (QPS)
rate(http_requests_total[5m])

# 每秒错误率
rate(http_errors_total[5m])

# 5分钟内的请求增量
increase(http_requests_total[5m])
```

##### (2) Gauge (仪表盘)

**特点**：
- 可增可减
- 表示当前状态
- 可直接使用

**典型场景**：
```
# 当前内存使用量 (字节)
node_memory_usage_bytes 3670016000

# 当前连接数
mysql_connections_active 42

# CPU温度
node_cpu_temperature_celsius{cpu="0"} 56.7

# 队列长度
queue_length{queue="orders"} 1523
```

**PromQL查询**：
```promql
# Gauge可以直接使用,也可以用聚合函数

# 当前内存使用率
node_memory_usage_bytes / node_memory_total_bytes * 100

# 最大连接数
max_over_time(mysql_connections_active[1h])

# 平均队列长度
avg_over_time(queue_length[5m])
```

##### (3) Histogram (直方图)

**特点**：
- 统计数据分布
- 自动计算多个分位数桶
- 适合延迟/大小等指标

**数据结构**：
```
# 假设API延迟的Histogram指标
http_request_duration_seconds_bucket{le="0.01"} 8500     # ≤10ms的请求数
http_request_duration_seconds_bucket{le="0.05"} 14200   # ≤50ms的请求数
http_request_duration_seconds_bucket{le="0.1"} 15600    # ≤100ms的请求数
http_request_duration_seconds_bucket{le="0.5"} 15820    # ≤500ms的请求数
http_request_duration_seconds_bucket{le="1"} 15838      # ≤1s的请求数
http_request_duration_seconds_bucket{le="+Inf"} 15840   # 所有请求数
http_request_duration_seconds_sum 458.92                # 总耗时
http_request_duration_seconds_count 15840               # 请求总数
```

**PromQL查询**：
```promql
# P99延迟 (99%的请求延迟低于此值)
histogram_quantile(0.99, rate(http_request_duration_seconds_bucket[5m]))

# P95延迟
histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))

# 平均延迟
rate(http_request_duration_seconds_sum[5m]) / rate(http_request_duration_seconds_count[5m])

# 超过500ms的请求占比
(http_request_duration_seconds_bucket{le="+Inf"} - http_request_duration_seconds_bucket{le="0.5"}) 
/ http_request_duration_seconds_bucket{le="+Inf"}
```

##### (4) Summary (摘要)

**特点**：
- 类似Histogram，但在客户端计算分位数
- 不需要预定义桶
- 计算成本高

**数据结构**：
```
# Go应用的GC延迟Summary
go_gc_duration_seconds{quantile="0.5"} 0.000123   # P50 (中位数)
go_gc_duration_seconds{quantile="0.9"} 0.000456   # P90
go_gc_duration_seconds{quantile="0.99"} 0.001234  # P99
go_gc_duration_seconds_sum 12.456                 # 总耗时
go_gc_duration_seconds_count 8234                 # GC总次数
```

**Histogram vs Summary对比**：

| 特性 | Histogram | Summary |
|------|----------|---------|
| **分位数计算** | 服务端(PromQL) | 客户端(应用) |
| **聚合能力** | 可跨实例聚合 | 不可聚合 |
| **性能开销** | 低(客户端) | 高(客户端) |
| **灵活性** | 高(可查任意分位数) | 低(固定分位数) |
| **推荐场景** | 大部分场景 | 已知分位数需求 |

**推荐：优先使用Histogram，它更灵活且支持聚合**

**3. 标签 (Labels) 的最佳实践**

**良好的标签设计**：

```promql
# ✅ 好的标签设计 (基数可控)
http_requests_total{
  method="GET",           # 基数: ~10 (GET/POST/PUT/DELETE...)
  endpoint="/api/users",  # 基数: ~100 (API端点数量)
  status="200",           # 基数: ~20 (HTTP状态码)
  service="order",        # 基数: ~50 (微服务数量)
  environment="prod"      # 基数: 3 (dev/staging/prod)
}
# 总基数: 10 * 100 * 20 * 50 * 3 = 3,000,000 (可接受)
```

**错误的标签设计**：

```promql
# ❌ 错误的标签设计 (基数爆炸)
http_requests_total{
  user_id="12345",        # 基数: 百万级 (用户数量)
  request_id="uuid-...",  # 基数: 无限 (每次请求唯一)
  timestamp="1705752000"  # 基数: 无限 (每秒一个)
}
# 导致: 数百万个时间序列,内存爆炸,查询极慢
```

**标签设计原则**：

1. **低基数**: 每个标签的唯一值应控制在数百以内
2. **有意义**: 标签应该是聚合/过滤的维度（如service、环境）
3. **避免高基数**: 不要用user_id、IP地址、UUID作为标签
4. **一致性**: 同一指标的标签名应保持一致

**4. 指标命名规范**

Prometheus官方推荐的命名规范：

```
<namespace>_<subsystem>_<name>_<unit>

示例:
- node_cpu_seconds_total          # namespace=node, subsystem=cpu, name=seconds, unit=total
- http_request_duration_seconds   # namespace=http, subsystem=request, name=duration, unit=seconds
- mysql_slave_lag_seconds         # namespace=mysql, subsystem=slave, name=lag, unit=seconds
```

**命名规则**：

1. **使用下划线**: 不要用驼峰或连字符
2. **包含单位**: 使用基础单位（seconds, bytes, ratio）
3. **Counter加_total**: 所有Counter指标应以_total结尾
4. **避免重复**: 不要在指标名中重复标签信息

```promql
# ✅ 良好命名
http_requests_total{method="GET"}
node_memory_bytes{type="free"}

# ❌ 错误命名
httpRequestsGET              # 驼峰 + 标签信息在名称中
node_memory_mb               # 非基础单位
requests                     # Counter缺少_total
```

#### 数据存储机制

**1. 时序数据库 (TSDB) 原理**

Prometheus使用自研的时序数据库，采用**块存储**和**高效压缩**：

```
数据目录结构:
/data/prometheus/
├── 01HQXXX/                    # 块ID (2小时一个块)
│   ├── chunks/                 # 压缩的时序数据
│   │   └── 000001
│   ├── index                   # 倒排索引 (标签→序列ID)
│   ├── meta.json               # 块元数据
│   └── tombstones              # 删除标记
├── 01HQYYY/
├── wal/                        # WAL (Write-Ahead Log)
│   ├── 00000000
│   └── checkpoint.00000001
└── queries.active              # 活跃查询
```

**存储流程**：

```
1. 数据写入WAL (预写日志)
   → 防止进程崩溃丢失数据

2. 内存中构建块 (2小时)
   → 高性能查询最近数据

3. 块落盘 (每2小时)
   → 持久化到磁盘

4. 压缩合并 (后台)
   → 合并多个小块为大块
   → 压缩比可达10:1

5. 删除过期数据
   → 超过保留期限自动删除
```

**2. 压缩算法**

Prometheus使用**Delta-of-Delta + XOR编码**：

```
原始数据 (时间戳 + 值):
1705752000  100.5
1705752060  102.3
1705752120  101.8
1705752180  103.1

压缩后存储:
- 第一个时间戳: 1705752000 (完整存储)
- 后续时间戳: 存储差值的差值
  60, 0, 0, ... (大部分是0,高度压缩)
  
- 第一个值: 100.5 (完整存储)
- 后续值: XOR编码
  1.8 XOR 100.5 = xxx (存储XOR结果的前导零压缩)

压缩比: 平均1.37字节/样本 (原始需要16字节)
```

**3. 数据保留策略**

```yaml
# Prometheus配置
global:
  scrape_interval: 15s

# 存储配置
storage:
  tsdb:
    path: /prometheus/data
    retention.time: 15d      # 保留15天 (默认)
    retention.size: 50GB     # 或者按大小限制
```

**存储容量估算**：

```
公式:
needed_disk_space = retention_time_seconds * ingested_samples_per_second * bytes_per_sample

示例计算:
- 保留时间: 15天 = 1,296,000秒
- 每秒采样: 10,000个指标 * 1个值 = 10,000 samples/s
- 每样本大小: 1.5字节 (压缩后)

存储需求 = 1,296,000 * 10,000 * 1.5 / 1024 / 1024 / 1024
         ≈ 18 GB

建议预留50%空间: 18 * 1.5 = 27 GB
```

**监控Prometheus自身的存储**：

```promql
# 磁盘使用量
prometheus_tsdb_storage_blocks_bytes

# 内存中序列数
prometheus_tsdb_head_series

# 每秒采样速率
rate(prometheus_tsdb_head_samples_appended_total[5m])

# 压缩耗时
prometheus_tsdb_compaction_duration_seconds
```

---

### 9.2.2 服务发现与目标抓取

#### Kubernetes服务发现机制

Prometheus原生支持Kubernetes服务发现，可以自动发现集群中的监控目标，无需手动配置。

**支持的发现类型**：

| 发现类型 | 用途 | 典型目标 |
|---------|------|---------|
| **node** | 发现所有节点 | Node Exporter, kubelet |
| **pod** | 发现所有Pod | 应用自定义指标 |
| **service** | 发现所有Service | kube-state-metrics |
| **endpoints** | 发现Service的Endpoint | 负载均衡后的Pod |
| **ingress** | 发现所有Ingress | Ingress Controller |

**完整的服务发现配置示例**：

```yaml
# prometheus-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      scrape_timeout: 10s
      evaluation_interval: 15s
      external_labels:
        cluster: 'k8s-prod'
        region: 'us-west-2'

    # ============= 1. 抓取节点指标 (Node Exporter) =============
    scrape_configs:
    - job_name: 'kubernetes-nodes'
      kubernetes_sd_configs:
      - role: node
      
      # Relabel配置 (重写标签)
      relabel_configs:
      # 保留所有节点标签为__meta_kubernetes_node_label_*
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)
      
      # 将节点名设置为instance标签
      - source_labels: [__meta_kubernetes_node_name]
        target_label: instance
      
      # 设置抓取路径为Node Exporter的端口
      - source_labels: [__address__]
        regex: '([^:]+):.*'      # 提取IP部分
        replacement: '${1}:9100'  # 添加Node Exporter端口
        target_label: __address__

    # ============= 2. 抓取kubelet (容器指标) =============
    - job_name: 'kubernetes-kubelet'
      scheme: https
      tls_config:
        ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        insecure_skip_verify: true
      bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
      
      kubernetes_sd_configs:
      - role: node
      
      relabel_configs:
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)
      - target_label: __address__
        replacement: kubernetes.default.svc:443
      - source_labels: [__meta_kubernetes_node_name]
        regex: (.+)
        target_label: __metrics_path__
        replacement: /api/v1/nodes/${1}/proxy/metrics

    # ============= 3. 抓取cAdvisor (容器资源) =============
    - job_name: 'kubernetes-cadvisor'
      scheme: https
      tls_config:
        ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        insecure_skip_verify: true
      bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
      
      kubernetes_sd_configs:
      - role: node
      
      relabel_configs:
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)
      - target_label: __address__
        replacement: kubernetes.default.svc:443
      - source_labels: [__meta_kubernetes_node_name]
        regex: (.+)
        target_label: __metrics_path__
        replacement: /api/v1/nodes/${1}/proxy/metrics/cadvisor

    # ============= 4. 抓取kube-state-metrics (K8s资源状态) =============
    - job_name: 'kube-state-metrics'
      kubernetes_sd_configs:
      - role: service
      
      relabel_configs:
      # 只抓取kube-state-metrics服务
      - source_labels: [__meta_kubernetes_service_label_app_kubernetes_io_name]
        action: keep
        regex: kube-state-metrics
      - source_labels: [__meta_kubernetes_namespace]
        target_label: namespace
      - source_labels: [__meta_kubernetes_service_name]
        target_label: service

    # ============= 5. 抓取Pod (应用自定义指标) =============
    - job_name: 'kubernetes-pods'
      kubernetes_sd_configs:
      - role: pod
      
      relabel_configs:
      # 只抓取带有prometheus.io/scrape=true注解的Pod
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
        action: keep
        regex: true
      
      # 自定义抓取端口 (默认使用Pod的第一个端口)
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_port, __meta_kubernetes_pod_ip]
        action: replace
        regex: ([^;]+);(.+)
        replacement: ${2}:${1}
        target_label: __address__
      
      # 自定义抓取路径 (默认/metrics)
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
        action: replace
        target_label: __metrics_path__
        regex: (.+)
      
      # 添加命名空间标签
      - source_labels: [__meta_kubernetes_namespace]
        target_label: namespace
      
      # 添加Pod名称标签
      - source_labels: [__meta_kubernetes_pod_name]
        target_label: pod
      
      # 添加容器名称标签
      - source_labels: [__meta_kubernetes_pod_container_name]
        target_label: container
      
      # 保留Pod的所有标签
      - action: labelmap
        regex: __meta_kubernetes_pod_label_(.+)

    # ============= 6. 抓取Service (通过Service发现) =============
    - job_name: 'kubernetes-services'
      kubernetes_sd_configs:
      - role: service
      
      metrics_path: /probe
      params:
        module: [http_2xx]  # 黑盒监控模块
      
      relabel_configs:
      # 只抓取带有prometheus.io/probe=true注解的Service
      - source_labels: [__meta_kubernetes_service_annotation_prometheus_io_probe]
        action: keep
        regex: true
      - source_labels: [__address__]
        target_label: __param_target
      - target_label: __address__
        replacement: blackbox-exporter:9115
      - source_labels: [__param_target]
        target_label: instance
      - action: labelmap
        regex: __meta_kubernetes_service_label_(.+)
```

**Relabel配置详解**：

Relabel是Prometheus最强大也最复杂的功能之一，它可以在抓取前重写标签。

**常用的Relabel操作**：

1. **keep**: 只保留匹配的目标

```yaml
- source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
  action: keep
  regex: true
# 含义: 只抓取annotations中prometheus.io/scrape=true的Pod
```

2. **drop**: 丢弃匹配的目标

```yaml
- source_labels: [__meta_kubernetes_namespace]
  action: drop
  regex: (kube-system|kube-public)
# 含义: 不抓取kube-system和kube-public命名空间的Pod
```

3. **replace**: 替换标签值

```yaml
- source_labels: [__meta_kubernetes_pod_name]
  target_label: pod
  action: replace
# 含义: 将__meta_kubernetes_pod_name的值复制到pod标签
```

4. **labelmap**: 批量映射标签

```yaml
- action: labelmap
  regex: __meta_kubernetes_pod_label_(.+)
# 含义: 将Pod的所有标签 (如app=nginx) 映射为Prometheus标签
```

5. **labeldrop**: 删除标签

```yaml
- action: labeldrop
  regex: __meta_kubernetes_pod_.*
# 含义: 删除所有以__meta_kubernetes_pod_开头的临时标签
```

**应用如何配置被Prometheus抓取**：

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
  namespace: production
  annotations:
    prometheus.io/scrape: "true"    # 启用抓取
    prometheus.io/port: "8080"      # 指标端口
    prometheus.io/path: "/metrics"  # 指标路径 (可选,默认/metrics)
  labels:
    app: my-app
    version: v1.0.0
    team: backend
spec:
  containers:
  - name: app
    image: my-app:v1.0.0
    ports:
    - containerPort: 8080
      name: metrics
```

Prometheus会自动发现这个Pod并抓取 `http://POD_IP:8080/metrics`。

#### 目标抓取机制

**1. 抓取流程**

```
┌─────────────────────────────────────────────────────────┐
│          Prometheus Scrape Lifecycle                    │
└─────────────────────────────────────────────────────────┘

1. 服务发现 (Service Discovery)
   ↓
   发现目标: Pod IP, 端口, 标签等元数据
   
2. Relabel (重写标签)
   ↓
   应用relabel_configs规则
   - keep/drop过滤目标
   - 修改__address__, __metrics_path__等
   
3. HTTP GET请求
   ↓
   GET http://<target>:<port>/<path>
   Headers: 
     - User-Agent: Prometheus/2.x
     - Accept: application/openmetrics-text
   
4. 解析响应
   ↓
   解析Prometheus文本格式:
   # HELP http_requests_total Total requests
   # TYPE http_requests_total counter
   http_requests_total{method="GET"} 123
   
5. 存储到TSDB
   ↓
   写入时序数据库
   
6. 等待下一个scrape_interval
   ↓
   默认15秒后重复
```

**2. 抓取配置参数**

```yaml
scrape_configs:
- job_name: 'my-app'
  scrape_interval: 30s       # 抓取间隔 (覆盖global)
  scrape_timeout: 10s        # 超时时间 (必须 < scrape_interval)
  metrics_path: '/metrics'   # 抓取路径
  scheme: http               # 协议 (http/https)
  
  # 基本认证
  basic_auth:
    username: admin
    password: secret
  
  # Bearer Token认证
  bearer_token_file: /var/run/secrets/token
  
  # TLS配置
  tls_config:
    ca_file: /etc/prometheus/ca.crt
    cert_file: /etc/prometheus/client.crt
    key_file: /etc/prometheus/client.key
    insecure_skip_verify: false
  
  # 静态目标
  static_configs:
  - targets:
    - 'localhost:9090'
    - '192.168.1.10:9100'
    labels:
      environment: production
      region: us-west-2
```

**3. 抓取状态监控**

Prometheus提供了丰富的指标来监控自身的抓取状态：

```promql
# 当前抓取目标数量
count(up)

# 抓取失败的目标
count(up == 0)

# 抓取失败率
(count(up == 0) / count(up)) * 100

# 抓取耗时 (P99)
histogram_quantile(0.99, rate(prometheus_target_scrape_duration_seconds_bucket[5m]))

# 每秒抓取的样本数
rate(prometheus_target_scrapes_sample_scraped_total[5m])

# 抓取超时次数
rate(prometheus_target_scrapes_exceeded_sample_limit_total[5m])
```

**查看抓取目标状态**：

访问Prometheus Web UI: `http://<prometheus>:9090/targets`

可以看到：
- 所有目标的健康状态 (UP/DOWN)
- 最后抓取时间
- 抓取耗时
- 错误信息

---

### 9.2.3 PromQL查询语言详解

PromQL (Prometheus Query Language) 是Prometheus的查询语言，它功能强大但学习曲线较陡。掌握PromQL是使用Prometheus的关键。

#### PromQL基础语法

**1. 即时向量选择器 (Instant Vector Selector)**

选择某个时间点的所有时间序列：

```promql
# 最简单的查询: 选择所有http_requests_total序列
http_requests_total

# 带标签过滤: 精确匹配
http_requests_total{method="GET", status="200"}

# 标签匹配操作符:
=   # 精确相等
!=  # 不相等
=~  # 正则匹配
!~  # 正则不匹配

# 示例:
http_requests_total{status=~"2..", method!="POST"}
# 含义: status是2xx, 且method不是POST
```

**2. 范围向量选择器 (Range Vector Selector)**

选择一段时间范围内的所有样本：

```promql
# 选择最近5分钟的所有样本
http_requests_total[5m]

# 时间单位:
s  # 秒
m  # 分钟
h  # 小时
d  # 天
w  # 周
y  # 年

# 示例:
http_requests_total{method="GET"}[1h]  # 最近1小时
node_cpu_seconds_total[30s]            # 最近30秒
```

**3. 偏移修饰符 (Offset Modifier)**

查询过去某个时间点的数据：

```promql
# 查询5分钟前的即时值
http_requests_total offset 5m

# 查询1小时前的5分钟范围数据
rate(http_requests_total[5m] offset 1h)

# 查询昨天同一时间的值
http_requests_total offset 1d
```

#### PromQL聚合操作符

**1. 聚合函数**

| 函数 | 说明 | 示例 |
|------|------|------|
| **sum** | 求和 | `sum(http_requests_total)` |
| **min** | 最小值 | `min(node_memory_free_bytes)` |
| **max** | 最大值 | `max(node_cpu_temperature)` |
| **avg** | 平均值 | `avg(http_request_duration_seconds)` |
| **count** | 计数 | `count(up == 1)` |
| **stddev** | 标准差 | `stddev(http_request_duration_seconds)` |
| **topk** | 前K个最大值 | `topk(5, http_requests_total)` |
| **bottomk** | 前K个最小值 | `bottomk(5, node_memory_free_bytes)` |
| **quantile** | 分位数 | `quantile(0.95, http_request_duration_seconds)` |

**by / without 子句**：

```promql
# 按method和status分组求和
sum(http_requests_total) by (method, status)

# 结果示例:
# {method="GET", status="200"} 15000
# {method="GET", status="404"} 120
# {method="POST", status="200"} 8000

# without: 排除某些标签,保留其他所有标签
sum(http_requests_total) without (instance, pod)
# 含义: 聚合时不区分instance和pod,保留method/status等标签
```

**2. 速率函数 (最常用)**

```promql
# rate(): 计算每秒平均增长率 (用于Counter)
rate(http_requests_total[5m])
# 含义: 过去5分钟的平均每秒请求数 (QPS)

# irate(): 计算瞬时增长率 (更敏感,用于快速变化的指标)
irate(http_requests_total[5m])
# 含义: 使用最近两个数据点计算增长率

# increase(): 计算时间范围内的增量
increase(http_requests_total[1h])
# 含义: 过去1小时的请求总增量

# rate vs irate:
# rate: 平滑,适合告警
# irate: 敏感,适合快速发现峰值
```

**3. 时间聚合函数**

```promql
# 过去一段时间的最大值
max_over_time(http_request_duration_seconds[1h])

# 过去一段时间的最小值
min_over_time(node_memory_free_bytes[1h])

# 过去一段时间的平均值
avg_over_time(cpu_usage[5m])

# 其他:
sum_over_time()    # 求和
count_over_time()  # 计数
stddev_over_time() # 标准差
quantile_over_time(0.95, http_request_duration_seconds[1h])  # P95
```

**4. 二元操作符**

**算术操作符**：

```promql
# 加减乘除
node_memory_total_bytes - node_memory_free_bytes  # 已用内存

# 内存使用率
(node_memory_total_bytes - node_memory_free_bytes) / node_memory_total_bytes * 100

# 幂运算
cpu_usage ^ 2

# 模运算
node_time_seconds % 3600  # 当前小时内的秒数
```

**比较操作符**：

```promql
# 返回布尔值 (0或1)
http_requests_total > 1000      # 大于1000的序列
node_memory_free_bytes < 1e9    # 小于1GB的节点

# 作为过滤器 (保留原值)
http_requests_total > bool 1000  # 返回0或1
```

**逻辑操作符**：

```promql
# and: 交集
http_requests_total > 1000 and http_errors_total > 10

# or: 并集
up{job="api"} or up{job="web"}

# unless: 差集
up unless on(instance) disk_full
# 含义: 返回up中存在但disk_full中不存在的序列
```

**向量匹配**：

```promql
# one-to-one匹配 (默认)
method:http_requests:rate5m / on(method) group_left method:http_requests:total

# many-to-one匹配
sum(rate(http_requests_total[5m])) by (method, status)
  /
sum(rate(http_requests_total[5m])) by (method)
# group_left: 左侧多个序列匹配右侧一个序列
```

#### 实用PromQL查询示例

**1. 系统资源监控**

```promql
# CPU使用率 (%)
100 - (avg by (instance) (irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)

# 内存使用率 (%)
(1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100

# 磁盘使用率 (%)
(node_filesystem_size_bytes - node_filesystem_free_bytes) / node_filesystem_size_bytes * 100

# 磁盘IO (IOPS)
rate(node_disk_reads_completed_total[5m]) + rate(node_disk_writes_completed_total[5m])

# 网络流量 (MB/s)
rate(node_network_receive_bytes_total[5m]) / 1024 / 1024
```

**2. 容器监控**

```promql
# 容器CPU使用率 (%)
sum(rate(container_cpu_usage_seconds_total{pod!=""}[5m])) by (pod, namespace) * 100

# 容器内存使用量 (GB)
sum(container_memory_working_set_bytes{pod!=""}) by (pod, namespace) / 1024 / 1024 / 1024

# 容器重启次数
kube_pod_container_status_restarts_total

# OOM次数
sum(increase(container_oom_events_total[1h])) by (pod, namespace)
```

**3. 应用性能监控 (RED方法)**

RED方法是服务监控的黄金标准：
- **R**ate: 请求速率
- **E**rrors: 错误率
- **D**uration: 请求延迟

```promql
# Rate: 每秒请求数 (QPS)
sum(rate(http_requests_total[5m])) by (service, method)

# Errors: 错误率 (%)
sum(rate(http_requests_total{status=~"5.."}[5m])) by (service)
  /
sum(rate(http_requests_total[5m])) by (service)
  * 100

# Duration: P95延迟 (秒)
histogram_quantile(0.95, 
  sum(rate(http_request_duration_seconds_bucket[5m])) by (le, service)
)

# Duration: P99延迟
histogram_quantile(0.99, 
  sum(rate(http_request_duration_seconds_bucket[5m])) by (le, service)
)
```

**4. Kubernetes资源监控**

```promql
# Pod数量 (按状态)
count(kube_pod_status_phase{phase="Running"}) by (namespace)

# Deployment副本数 vs 期望副本数
kube_deployment_status_replicas_available / kube_deployment_spec_replicas * 100

# PVC使用率
kubelet_volume_stats_used_bytes / kubelet_volume_stats_capacity_bytes * 100

# 节点状态 (Ready节点数)
count(kube_node_status_condition{condition="Ready", status="true"})

# 节点可调度资源 (CPU)
sum(kube_node_status_allocatable{resource="cpu"}) - sum(kube_pod_container_resource_requests{resource="cpu"})
```

**5. 业务指标监控**

```promql
# 订单创建速率 (每分钟)
rate(order_created_total[5m]) * 60

# 支付成功率 (%)
sum(rate(payment_total{status="success"}[5m]))
  /
sum(rate(payment_total[5m]))
  * 100

# 活跃用户数 (去重计数)
count(sum by (user_id) (rate(user_activity_total[5m]) > 0))

# 营收速率 (美元/秒)
rate(revenue_dollars_total[5m])
```

#### PromQL性能优化

**1. 避免高基数查询**

```promql
# ❌ 错误: 查询所有user_id (百万级基数)
sum by (user_id) (http_requests_total)

# ✅ 正确: 按服务聚合
sum by (service, method) (rate(http_requests_total[5m]))
```

**2. 使用Recording Rules预计算**

对于复杂的查询，可以使用Recording Rules预先计算并存储结果：

```yaml
# recording_rules.yml
groups:
- name: api_performance
  interval: 15s
  rules:
  # 预计算QPS
  - record: job:http_requests:rate5m
    expr: sum(rate(http_requests_total[5m])) by (job, method)
  
  # 预计算错误率
  - record: job:http_errors:ratio
    expr: |
      sum(rate(http_requests_total{status=~"5.."}[5m])) by (job)
        /
      sum(rate(http_requests_total[5m])) by (job)
```

之后可以直接查询预计算的结果：

```promql
# 直接使用预计算结果 (更快)
job:http_requests:rate5m{job="api"}

# 而不是每次都计算 (更慢)
sum(rate(http_requests_total{job="api"}[5m])) by (method)
```

**3. 合理使用时间范围**

```promql
# ❌ 错误: 范围过大,查询慢
rate(http_requests_total[1d])

# ✅ 正确: 5分钟足够计算速率
rate(http_requests_total[5m])

# 规则: rate()的范围应该是scrape_interval的4倍以上
# 如果scrape_interval=15s, 则range至少60s, 推荐5m
```

---

### 9.2.4 Prometheus部署与配置

#### 在Kubernetes中部署Prometheus

**方式1: 使用Helm Chart (推荐)**

```bash
# 添加Prometheus社区Helm仓库
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

# 创建命名空间
kubectl create namespace monitoring

# 安装kube-prometheus-stack (包含Prometheus + Grafana + AlertManager)
helm install prometheus prometheus-community/kube-prometheus-stack \
  --namespace monitoring \
  --set prometheus.prometheusSpec.retention=30d \
  --set prometheus.prometheusSpec.storageSpec.volumeClaimTemplate.spec.resources.requests.storage=50Gi \
  --set prometheus.prometheusSpec.resources.requests.memory=2Gi \
  --set prometheus.prometheusSpec.resources.limits.memory=4Gi \
  --set grafana.adminPassword=admin123

# 查看部署状态
kubectl get pods -n monitoring
```

**方式2: 手动部署 (完整理解)**

下面展示完整的手动部署配置：

**1. 创建ServiceAccount和RBAC**

```yaml
# prometheus-rbac.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: prometheus
  namespace: monitoring
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: prometheus
rules:
# 读取节点信息
- apiGroups: [""]
  resources:
  - nodes
  - nodes/metrics
  - nodes/proxy
  - services
  - endpoints
  - pods
  verbs: ["get", "list", "watch"]
# 读取配置
- apiGroups: [""]
  resources:
  - configmaps
  verbs: ["get"]
# 读取Ingress
- apiGroups: ["networking.k8s.io"]
  resources:
  - ingresses
  verbs: ["get", "list", "watch"]
# 非资源URL (用于健康检查)
- nonResourceURLs: ["/metrics"]
  verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: prometheus
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: prometheus
subjects:
- kind: ServiceAccount
  name: prometheus
  namespace: monitoring
```

**2. 创建ConfigMap (Prometheus配置)**

```yaml
# prometheus-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      scrape_timeout: 10s
      evaluation_interval: 15s
      external_labels:
        cluster: 'k8s-production'
        region: 'us-west-2'

    # AlertManager配置
    alerting:
      alertmanagers:
      - static_configs:
        - targets:
          - alertmanager:9093

    # 规则文件
    rule_files:
    - '/etc/prometheus/rules/*.yml'

    # 抓取配置 (前面已详细介绍)
    scrape_configs:
    - job_name: 'prometheus'
      static_configs:
      - targets: ['localhost:9090']
    
    - job_name: 'kubernetes-nodes'
      kubernetes_sd_configs:
      - role: node
      relabel_configs:
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)
      - source_labels: [__address__]
        regex: '([^:]+):.*'
        replacement: '${1}:9100'
        target_label: __address__
    
    - job_name: 'kubernetes-pods'
      kubernetes_sd_configs:
      - role: pod
      relabel_configs:
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
        action: keep
        regex: true
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_port, __meta_kubernetes_pod_ip]
        action: replace
        regex: ([^;]+);(.+)
        replacement: ${2}:${1}
        target_label: __address__
      - source_labels: [__meta_kubernetes_namespace]
        target_label: namespace
      - source_labels: [__meta_kubernetes_pod_name]
        target_label: pod
```

**3. 创建Deployment**

```yaml
# prometheus-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: monitoring
  labels:
    app: prometheus
spec:
  replicas: 1  # 单实例 (高可用需要使用StatefulSet + Thanos)
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      serviceAccountName: prometheus
      containers:
      - name: prometheus
        image: prom/prometheus:v2.48.0
        args:
        - '--config.file=/etc/prometheus/prometheus.yml'
        - '--storage.tsdb.path=/prometheus'
        - '--storage.tsdb.retention.time=30d'      # 保留30天
        - '--storage.tsdb.retention.size=50GB'     # 或50GB上限
        - '--web.enable-lifecycle'                 # 允许热重载
        - '--web.enable-admin-api'                 # 启用管理API
        ports:
        - containerPort: 9090
          name: web
        volumeMounts:
        - name: config
          mountPath: /etc/prometheus
        - name: data
          mountPath: /prometheus
        resources:
          requests:
            cpu: 500m
            memory: 2Gi
          limits:
            cpu: 2000m
            memory: 4Gi
        livenessProbe:
          httpGet:
            path: /-/healthy
            port: 9090
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /-/ready
            port: 9090
          initialDelaySeconds: 30
          periodSeconds: 10
      volumes:
      - name: config
        configMap:
          name: prometheus-config
      - name: data
        persistentVolumeClaim:
          claimName: prometheus-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data
  namespace: monitoring
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: gp3  # AWS EBS gp3
```

**4. 创建Service**

```yaml
# prometheus-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: monitoring
  labels:
    app: prometheus
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: 9090
    name: web
  selector:
    app: prometheus
---
# 如果需要外部访问,创建Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus
  namespace: monitoring
  annotations:
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: prometheus-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: 'Authentication Required'
spec:
  ingressClassName: nginx
  rules:
  - host: prometheus.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: prometheus
            port:
              number: 9090
  tls:
  - hosts:
    - prometheus.example.com
    secretName: prometheus-tls
```

**5. 部署**

```bash
# 部署所有资源
kubectl apply -f prometheus-rbac.yaml
kubectl apply -f prometheus-config.yaml
kubectl apply -f prometheus-deployment.yaml
kubectl apply -f prometheus-service.yaml

# 检查状态
kubectl get pods -n monitoring
kubectl logs -n monitoring -l app=prometheus --tail=100

# 端口转发访问 (如果没有Ingress)
kubectl port-forward -n monitoring svc/prometheus 9090:9090

# 访问: http://localhost:9090
```

#### Prometheus配置热重载

Prometheus支持配置热重载，无需重启容器：

```bash
# 方法1: 使用API (需要启用--web.enable-lifecycle)
curl -X POST http://prometheus:9090/-/reload

# 方法2: 发送SIGHUP信号
kubectl exec -n monitoring prometheus-xxx -- kill -HUP 1

# 方法3: 修改ConfigMap后自动重载 (需要配置ConfigMap Reloader)
# 使用kube-prometheus-stack时自动包含此功能
```

#### Prometheus高可用架构

单实例Prometheus存在单点故障风险，生产环境建议使用高可用架构：

**方案1: Prometheus联邦 (Federation)**

```
┌──────────────┐    ┌──────────────┐
│ Prometheus 1 │    │ Prometheus 2 │  (多个独立Prometheus)
│ (shard 1)    │    │ (shard 2)    │
└──────┬───────┘    └──────┬───────┘
       │                   │
       └───────┬───────────┘
               │
        ┌──────▼───────┐
        │  Prometheus  │  (全局Prometheus)
        │  (Global)    │  (联邦聚合)
        └──────────────┘
```

**方案2: Thanos (推荐)**

Thanos提供长期存储、全局查询、数据去重等高级功能：

```
┌─────────────────────────────────────────────────────────┐
│                      Thanos架构                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐     ┌──────────────┐                 │
│  │ Prometheus 1 │     │ Prometheus 2 │                 │
│  │ + Sidecar    │     │ + Sidecar    │                 │
│  └──────┬───────┘     └──────┬───────┘                 │
│         │                     │                         │
│         └──────┬──────────────┘                         │
│                │                                        │
│         ┌──────▼──────┐                                │
│         │ Object      │  (S3/GCS/MinIO)                │
│         │ Storage     │  (长期存储)                     │
│         └──────┬──────┘                                │
│                │                                        │
│         ┌──────▼──────┐                                │
│         │ Thanos      │  (查询所有数据源)               │
│         │ Query       │                                │
│         └──────┬──────┘                                │
│                │                                        │
│         ┌──────▼──────┐                                │
│         │  Grafana    │                                │
│         └─────────────┘                                │
└─────────────────────────────────────────────────────────┘
```

**部署Thanos (使用Helm)**

```bash
helm install thanos bitnami/thanos \
  --namespace monitoring \
  --set query.enabled=true \
  --set query.dnsDiscovery.enabled=true \
  --set query.dnsDiscovery.sidecarsService=prometheus-thanos-discovery \
  --set query.dnsDiscovery.sidecarsNamespace=monitoring \
  --set objstoreConfig.type=S3 \
  --set objstoreConfig.config.bucket=thanos-metrics \
  --set objstoreConfig.config.endpoint=s3.amazonaws.com \
  --set objstoreConfig.config.region=us-west-2
```

---

### 9.2.5 Recording Rules与性能优化

#### Recording Rules详解

Recording Rules允许预先计算复杂查询并存储结果，大幅提升查询性能。

**1. Recording Rules配置**

```yaml
# prometheus-rules.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-rules
  namespace: monitoring
data:
  recording_rules.yml: |
    groups:
    # API性能指标
    - name: api_performance
      interval: 15s  # 计算间隔
      rules:
      # QPS (每秒请求数)
      - record: job:http_requests:rate5m
        expr: sum(rate(http_requests_total[5m])) by (job, method, status)
        labels:
          metric_type: rate
      
      # 错误率 (%)
      - record: job:http_errors:ratio
        expr: |
          sum(rate(http_requests_total{status=~"5.."}[5m])) by (job)
            /
          sum(rate(http_requests_total[5m])) by (job)
      
      # P95延迟
      - record: job:http_request_duration:p95
        expr: |
          histogram_quantile(0.95,
            sum(rate(http_request_duration_seconds_bucket[5m])) by (le, job)
          )
      
      # P99延迟
      - record: job:http_request_duration:p99
        expr: |
          histogram_quantile(0.99,
            sum(rate(http_request_duration_seconds_bucket[5m])) by (le, job)
          )

    # 节点资源指标
    - name: node_resources
      interval: 30s
      rules:
      # CPU使用率
      - record: instance:node_cpu:usage
        expr: |
          100 - (avg by (instance) (irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)
      
      # 内存使用率
      - record: instance:node_memory:usage_ratio
        expr: |
          1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)
      
      # 磁盘使用率
      - record: instance:node_disk:usage_ratio
        expr: |
          (node_filesystem_size_bytes - node_filesystem_free_bytes) 
            / node_filesystem_size_bytes

    # 容器资源指标
    - name: container_resources
      interval: 15s
      rules:
      # 容器CPU使用 (millicores)
      - record: namespace_pod_container:cpu_usage:rate5m
        expr: |
          sum(rate(container_cpu_usage_seconds_total{pod!=""}[5m])) 
            by (namespace, pod, container) * 1000
      
      # 容器内存使用 (bytes)
      - record: namespace_pod_container:memory_usage:bytes
        expr: |
          sum(container_memory_working_set_bytes{pod!=""}) 
            by (namespace, pod, container)
      
      # 容器网络接收速率 (bytes/s)
      - record: namespace_pod:network_receive:rate5m
        expr: |
          sum(rate(container_network_receive_bytes_total[5m])) 
            by (namespace, pod)
```

**2. 应用Recording Rules**

```yaml
# 修改Prometheus Deployment,挂载rules
spec:
  template:
    spec:
      containers:
      - name: prometheus
        volumeMounts:
        - name: rules
          mountPath: /etc/prometheus/rules
      volumes:
      - name: rules
        configMap:
          name: prometheus-rules

# 修改prometheus.yml,引用规则文件
rule_files:
- '/etc/prometheus/rules/*.yml'
```

```bash
# 应用配置
kubectl apply -f prometheus-rules.yaml

# 热重载Prometheus
curl -X POST http://prometheus:9090/-/reload

# 验证规则加载
# 访问: http://prometheus:9090/rules
```

**3. 使用Recording Rules查询**

```promql
# 直接使用预计算结果 (快速)
job:http_requests:rate5m{job="api-server"}

# 对比原始查询 (慢)
sum(rate(http_requests_total{job="api-server"}[5m])) by (method, status)

# 性能对比:
# Recording Rule: ~10ms
# 原始查询: ~500ms (50倍提升)
```

#### Prometheus性能调优

**1. 资源配置优化**

```yaml
# 根据指标规模调整资源
spec:
  containers:
  - name: prometheus
    resources:
      requests:
        cpu: 2000m       # 2核起步
        memory: 4Gi      # 4GB内存起步
      limits:
        cpu: 4000m
        memory: 8Gi

# 经验公式:
# 内存需求 = 活跃时间序列数 * 1KB + 磁盘数据 * 0.0001
# 示例: 100万序列 + 50GB磁盘数据 = 1GB + 5MB ≈ 1GB内存
```

**2. 存储优化**

```yaml
# 调整数据保留策略
args:
- '--storage.tsdb.retention.time=30d'  # 保留30天
- '--storage.tsdb.retention.size=50GB'  # 或50GB上限(二选一生效)

# 调整块大小 (默认2小时)
- '--storage.tsdb.min-block-duration=2h'
- '--storage.tsdb.max-block-duration=36h'

# 压缩设置
- '--storage.tsdb.wal-compression'  # 启用WAL压缩(节省50%空间)
```

**3. 查询优化**

```promql
# ❌ 慢查询 (扫描大量数据)
rate(http_requests_total[1d])

# ✅ 优化 (缩小时间范围)
rate(http_requests_total[5m])

# ❌ 慢查询 (高基数标签)
sum by (user_id) (http_requests_total)

# ✅ 优化 (低基数标签)
sum by (service, method) (http_requests_total)
```

**4. 采样率优化**

```yaml
# 调整采样间隔
scrape_configs:
- job_name: 'low-priority'
  scrape_interval: 60s    # 低优先级目标: 60秒
  
- job_name: 'high-priority'
  scrape_interval: 15s    # 高优先级目标: 15秒
```

**5. 监控Prometheus自身**

```promql
# Prometheus内存使用
process_resident_memory_bytes / 1024 / 1024 / 1024

# 活跃时间序列数
prometheus_tsdb_head_series

# 每秒采样速率
rate(prometheus_tsdb_head_samples_appended_total[5m])

# 查询延迟P99
histogram_quantile(0.99, rate(prometheus_http_request_duration_seconds_bucket[5m]))

# WAL损坏次数 (应该为0)
prometheus_tsdb_wal_corruptions_total
```

---

**📊 9.2节总结**

本节我们深入学习了Prometheus的核心原理与实战部署：

✅ **Prometheus架构**：
- 整体架构与核心组件
- 时序数据库TSDB原理
- 高效压缩算法 (Delta-of-Delta + XOR)

✅ **数据模型**：
- 4种指标类型 (Counter/Gauge/Histogram/Summary)
- 标签设计最佳实践
- 指标命名规范

✅ **服务发现**：
- Kubernetes原生服务发现
- Relabel配置详解
- 目标抓取机制

✅ **PromQL查询**：
- 基础语法 (即时/范围向量)
- 聚合操作符与速率函数
- RED方法实用查询
- 性能优化技巧

✅ **生产部署**：
- Helm Chart快速部署
- 手动部署完整配置
- 高可用架构 (Federation/Thanos)
- Recording Rules性能优化

**下一节预告**：我们将学习Grafana可视化平台，包括数据源集成、仪表盘设计最佳实践、变量与模板应用，以及企业级监控仪表盘的构建。

---

## 9.3 Grafana可视化与仪表盘

在9.2节中，我们深入学习了Prometheus的核心原理和PromQL查询。虽然Prometheus自带Web UI可以查询数据，但它的可视化能力非常有限。在生产环境中，我们需要一个强大的可视化平台来：

- 📊 创建美观的实时监控仪表盘
- 📈 展示多维度的指标趋势图表
- 🎯 集中管理多个数据源
- 🚨 可视化告警状态
- 👥 支持团队协作和权限管理

**Grafana**是业界最流行的开源可视化平台，它与Prometheus完美集成，提供了丰富的图表类型和强大的定制能力。

**Grafana核心优势**：

| 特性 | 说明 | 价值 |
|------|------|------|
| **多数据源** | 支持60+数据源 (Prometheus/Loki/Jaeger/ES/MySQL等) | 统一监控平台 |
| **丰富图表** | 折线图/柱状图/热力图/Gauge/Table/Stat/Logs面板等 | 满足各种展示需求 |
| **变量模板** | 动态Dashboard (切换集群/命名空间/服务等) | 一个Dashboard适配多环境 |
| **告警集成** | 原生告警规则+多渠道通知 | 独立于Prometheus的告警 |
| **权限管理** | Organization/Team/User三级权限 | 企业级多租户支持 |
| **插件生态** | 数百个社区插件 | 高度可扩展 |

### 9.3.1 Grafana部署与配置

#### 在Kubernetes中部署Grafana

**方式1: 使用Helm Chart (推荐)**

```bash
# 使用kube-prometheus-stack (已包含Grafana)
helm install prometheus prometheus-community/kube-prometheus-stack \
  --namespace monitoring \
  --set grafana.enabled=true \
  --set grafana.adminPassword=admin123 \
  --set grafana.persistence.enabled=true \
  --set grafana.persistence.size=10Gi \
  --set grafana.ingress.enabled=true \
  --set grafana.ingress.hosts[0]=grafana.example.com

# 或单独安装Grafana
helm repo add grafana https://grafana.github.io/helm-charts
helm install grafana grafana/grafana \
  --namespace monitoring \
  --set adminPassword=admin123 \
  --set persistence.enabled=true \
  --set persistence.size=10Gi
```

**方式2: 手动部署**

```yaml
# grafana-deployment.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: monitoring
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
    # Prometheus数据源
    - name: Prometheus
      type: prometheus
      access: proxy
      url: http://prometheus:9090
      isDefault: true
      editable: true
      jsonData:
        timeInterval: 15s
        queryTimeout: 60s
    
    # Loki数据源 (如果部署了Loki)
    - name: Loki
      type: loki
      access: proxy
      url: http://loki:3100
      editable: true
    
    # Jaeger数据源 (如果部署了Jaeger)
    - name: Jaeger
      type: jaeger
      access: proxy
      url: http://jaeger-query:16686
      editable: true
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: monitoring
data:
  grafana.ini: |
    [server]
    protocol = http
    http_port = 3000
    domain = grafana.example.com
    root_url = %(protocol)s://%(domain)s/
    
    [security]
    admin_user = admin
    admin_password = admin123
    secret_key = SW2YcwTIb9zpOOhoPsMm
    
    [auth]
    disable_login_form = false
    disable_signout_menu = false
    
    [auth.anonymous]
    enabled = false
    
    [users]
    allow_sign_up = false
    allow_org_create = false
    auto_assign_org = true
    auto_assign_org_role = Viewer
    
    [log]
    mode = console
    level = info
    
    [alerting]
    enabled = true
    execute_alerts = true
    
    [unified_alerting]
    enabled = true
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: monitoring
  labels:
    app: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      securityContext:
        fsGroup: 472
        runAsUser: 472
      containers:
      - name: grafana
        image: grafana/grafana:10.2.0
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: GF_SECURITY_ADMIN_USER
          value: admin
        - name: GF_SECURITY_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: grafana-admin
              key: password
        volumeMounts:
        - name: config
          mountPath: /etc/grafana
        - name: datasources
          mountPath: /etc/grafana/provisioning/datasources
        - name: data
          mountPath: /var/lib/grafana
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
      volumes:
      - name: config
        configMap:
          name: grafana-config
      - name: datasources
        configMap:
          name: grafana-datasources
      - name: data
        persistentVolumeClaim:
          claimName: grafana-data
---
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
type: Opaque
stringData:
  password: admin123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: monitoring
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp3
---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring
  labels:
    app: grafana
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  selector:
    app: grafana
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
  - host: grafana.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana
            port:
              number: 3000
  tls:
  - hosts:
    - grafana.example.com
    secretName: grafana-tls
```

**部署并访问**：

```bash
# 部署Grafana
kubectl apply -f grafana-deployment.yaml

# 检查状态
kubectl get pods -n monitoring -l app=grafana
kubectl logs -n monitoring -l app=grafana --tail=50

# 端口转发访问 (如果没有Ingress)
kubectl port-forward -n monitoring svc/grafana 3000:3000

# 浏览器访问: http://localhost:3000
# 默认账号: admin / admin123
```

#### Grafana配置详解

**1. 配置文件结构**

```
/etc/grafana/
├── grafana.ini              # 主配置文件
└── provisioning/            # 自动配置目录
    ├── datasources/         # 数据源配置
    │   └── datasources.yaml
    ├── dashboards/          # Dashboard配置
    │   └── dashboards.yaml
    ├── notifiers/           # 告警通知配置
    │   └── notifiers.yaml
    └── plugins/             # 插件配置
        └── plugins.yaml
```

**2. 重要配置项**

```ini
# grafana.ini

# 服务器配置
[server]
protocol = http             # http或https
http_port = 3000           # 监听端口
domain = grafana.example.com
root_url = %(protocol)s://%(domain)s/
enable_gzip = true

# 数据库配置 (默认SQLite,生产建议MySQL/PostgreSQL)
[database]
type = mysql
host = mysql:3306
name = grafana
user = grafana
password = secret
max_open_conn = 100
max_idle_conn = 50

# 安全配置
[security]
admin_user = admin
admin_password = $__env{GF_SECURITY_ADMIN_PASSWORD}  # 从环境变量读取
secret_key = $__env{GF_SECURITY_SECRET_KEY}
disable_gravatar = false
cookie_secure = true
cookie_samesite = lax

# 用户管理
[users]
allow_sign_up = false          # 禁止自注册
allow_org_create = false       # 禁止创建Organization
auto_assign_org = true
auto_assign_org_role = Viewer  # 新用户默认角色

# 认证配置
[auth]
disable_login_form = false
disable_signout_menu = false
oauth_auto_login = false

# LDAP认证 (企业环境)
[auth.ldap]
enabled = false
config_file = /etc/grafana/ldap.toml

# 匿名访问 (公共Dashboard)
[auth.anonymous]
enabled = false
org_name = Main Org.
org_role = Viewer

# 日志配置
[log]
mode = console file
level = info
filters = rendering:debug

# 告警配置
[alerting]
enabled = true
execute_alerts = true
error_or_timeout = alerting
nodata_or_nullvalues = no_data
concurrent_render_limit = 5

# 统一告警 (Grafana 8.0+)
[unified_alerting]
enabled = true
execute_alerts = true
evaluation_timeout = 30s
max_attempts = 3

# SMTP配置 (邮件告警)
[smtp]
enabled = true
host = smtp.gmail.com:587
user = alerts@example.com
password = secret
from_address = alerts@example.com
from_name = Grafana Alerts

# 插件配置
[plugins]
enable_alpha = false
app_tls_skip_verify_insecure = false
```

---

### 9.3.2 数据源集成

Grafana支持60+种数据源，下面介绍Kubernetes监控最常用的几种。

#### Prometheus数据源配置

**1. 通过UI添加**

```
1. 登录Grafana
2. 左侧菜单 → Configuration → Data Sources
3. 点击 "Add data source"
4. 选择 "Prometheus"
5. 填写配置:
   - Name: Prometheus
   - URL: http://prometheus:9090 (K8s内部Service地址)
   - Access: Server (default)
   - Scrape interval: 15s
6. 点击 "Save & Test"
```

**2. 通过Provisioning自动配置 (推荐)**

```yaml
# datasources.yaml
apiVersion: 1
datasources:
- name: Prometheus
  type: prometheus
  access: proxy
  url: http://prometheus:9090
  isDefault: true
  editable: true
  jsonData:
    timeInterval: 15s        # 查询间隔
    queryTimeout: 60s        # 查询超时
    httpMethod: POST         # 使用POST (支持更长查询)
    customQueryParameters: '' # 自定义参数
    manageAlerts: false      # 不管理Prometheus告警
  version: 1
```

**3. Prometheus数据源高级配置**

```yaml
datasources:
- name: Prometheus
  type: prometheus
  access: proxy
  url: http://prometheus:9090
  isDefault: true
  jsonData:
    # 基础配置
    timeInterval: 15s
    queryTimeout: 60s
    httpMethod: POST
    
    # 认证配置 (如果Prometheus启用了认证)
    basicAuth: true
    basicAuthUser: admin
  secureJsonData:
    basicAuthPassword: secret
    
    # TLS配置
    tlsAuth: true
    tlsCACert: |
      -----BEGIN CERTIFICATE-----
      ...
      -----END CERTIFICATE-----
    tlsClientCert: |
      -----BEGIN CERTIFICATE-----
      ...
      -----END CERTIFICATE-----
    tlsClientKey: |
      -----BEGIN PRIVATE KEY-----
      ...
      -----END PRIVATE KEY-----
    
    # 自定义HTTP头
    httpHeaderName1: X-Custom-Header
  secureJsonData:
    httpHeaderValue1: custom-value
```

**4. 测试Prometheus连接**

```bash
# 在Grafana Pod中测试
kubectl exec -n monitoring -it grafana-xxx -- sh

# 测试连接
curl http://prometheus:9090/api/v1/query?query=up

# 应该返回JSON数据
```

#### Loki数据源配置 (日志)

Loki是Grafana Labs开发的轻量级日志聚合系统，与Prometheus架构相似。

```yaml
datasources:
- name: Loki
  type: loki
  access: proxy
  url: http://loki:3100
  editable: true
  jsonData:
    maxLines: 1000           # 最大返回行数
    timeout: 60              # 查询超时 (秒)
    derivedFields:           # 从日志提取字段
    - datasourceUid: jaeger  # 关联到Jaeger
      matcherRegex: "traceID=(\\w+)"
      name: TraceID
      url: "$${__value.raw}"
```

**Loki查询示例**：

```logql
# 查询特定命名空间的日志
{namespace="production"}

# 查询特定Pod的错误日志
{namespace="production", pod=~"api-.*"} |= "ERROR"

# 正则过滤
{app="nginx"} |~ "HTTP.*5[0-9]{2}"

# 统计错误率
rate({namespace="production"} |= "ERROR" [5m])
```

#### Jaeger数据源配置 (链路追踪)

```yaml
datasources:
- name: Jaeger
  type: jaeger
  access: proxy
  url: http://jaeger-query:16686
  editable: true
  jsonData:
    tracesToLogs:            # 关联日志
      datasourceUid: loki
      tags: ['pod', 'namespace']
      spanStartTimeShift: '-1h'
      spanEndTimeShift: '1h'
```

#### Elasticsearch数据源配置

```yaml
datasources:
- name: Elasticsearch
  type: elasticsearch
  access: proxy
  url: http://elasticsearch:9200
  database: "[logstash-]YYYY.MM.DD"  # 索引模式
  jsonData:
    esVersion: "7.10.0"
    timeField: "@timestamp"
    logMessageField: message
    logLevelField: level
    interval: Daily
```

#### 多Prometheus数据源 (多集群监控)

```yaml
datasources:
# 生产环境Prometheus
- name: Prometheus-Prod
  type: prometheus
  access: proxy
  url: http://prometheus-prod:9090
  isDefault: true
  jsonData:
    timeInterval: 15s
    customQueryParameters: 'cluster=production'

# 测试环境Prometheus
- name: Prometheus-Staging
  type: prometheus
  access: proxy
  url: http://prometheus-staging:9090
  jsonData:
    timeInterval: 15s
    customQueryParameters: 'cluster=staging'

# 开发环境Prometheus
- name: Prometheus-Dev
  type: prometheus
  access: proxy
  url: http://prometheus-dev:9090
  jsonData:
    timeInterval: 30s
    customQueryParameters: 'cluster=development'
```

---

### 9.3.3 仪表盘设计最佳实践

#### Dashboard基础概念

**Dashboard结构**：

```
Dashboard (仪表盘)
├── Rows (行)
│   ├── Panel 1 (面板1: 折线图)
│   ├── Panel 2 (面板2: Gauge)
│   └── Panel 3 (面板3: Table)
├── Variables (变量)
│   ├── $cluster
│   ├── $namespace
│   └── $pod
└── Settings (设置)
    ├── General (通用)
    ├── Annotations (注释)
    ├── Variables (变量)
    ├── Links (链接)
    └── JSON Model (JSON模型)
```

#### 创建第一个Dashboard

**1. Kubernetes集群概览Dashboard**

```
步骤1: 创建Dashboard
- 左侧菜单 → Dashboards → New Dashboard
- 点击 "Add visualization"
- 选择数据源: Prometheus

步骤2: 添加集群节点数面板
- Panel Title: Cluster Nodes
- Visualization: Stat (统计)
- Query:
  count(kube_node_info)
- 设置:
  - Value options → Show: Calculate → Last (非空)
  - Standard options → Unit: none
  - Standard options → Color scheme: Green
  - Thresholds: 正常(绿色), <3 警告(黄色), <2 严重(红色)

步骤3: 添加Pod数量面板
- Panel Title: Running Pods
- Visualization: Stat
- Query:
  count(kube_pod_status_phase{phase="Running"})

步骤4: 添加CPU使用率面板
- Panel Title: Cluster CPU Usage
- Visualization: Time series (时序图)
- Query:
  100 - (avg(irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)
- Legend: Hide (因为只有一条线)

步骤5: 添加内存使用率面板
- Panel Title: Cluster Memory Usage
- Visualization: Time series
- Query:
  (1 - (sum(node_memory_MemAvailable_bytes) / sum(node_memory_MemTotal_bytes))) * 100
```

**2. Dashboard完整JSON示例**

```json
{
  "dashboard": {
    "title": "Kubernetes Cluster Overview",
    "tags": ["kubernetes", "cluster"],
    "timezone": "browser",
    "panels": [
      {
        "id": 1,
        "gridPos": {"h": 4, "w": 6, "x": 0, "y": 0},
        "type": "stat",
        "title": "Cluster Nodes",
        "targets": [
          {
            "expr": "count(kube_node_info)",
            "refId": "A"
          }
        ],
        "options": {
          "colorMode": "background",
          "graphMode": "none",
          "textMode": "value_and_name"
        },
        "fieldConfig": {
          "defaults": {
            "unit": "none",
            "thresholds": {
              "mode": "absolute",
              "steps": [
                {"value": null, "color": "red"},
                {"value": 3, "color": "yellow"},
                {"value": 5, "color": "green"}
              ]
            }
          }
        }
      },
      {
        "id": 2,
        "gridPos": {"h": 4, "w": 6, "x": 6, "y": 0},
        "type": "stat",
        "title": "Running Pods",
        "targets": [
          {
            "expr": "count(kube_pod_status_phase{phase=\"Running\"})",
            "refId": "A"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "color": {"mode": "thresholds"},
            "thresholds": {
              "steps": [
                {"value": null, "color": "green"}
              ]
            }
          }
        }
      },
      {
        "id": 3,
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 4},
        "type": "timeseries",
        "title": "CPU Usage",
        "targets": [
          {
            "expr": "100 - (avg(irate(node_cpu_seconds_total{mode=\"idle\"}[5m])) * 100)",
            "legendFormat": "CPU Usage %",
            "refId": "A"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "percent",
            "min": 0,
            "max": 100,
            "color": {"mode": "palette-classic"}
          }
        }
      }
    ],
    "time": {
      "from": "now-6h",
      "to": "now"
    },
    "timepicker": {
      "refresh_intervals": ["5s", "10s", "30s", "1m", "5m"]
    },
    "refresh": "30s"
  }
}
```

#### 图表类型选择指南

| 图表类型 | 适用场景 | 典型指标 |
|---------|---------|---------|
| **Time series** | 展示时序趋势 | CPU使用率、内存、网络流量 |
| **Stat** | 显示单一数值 | 节点数、Pod数、当前QPS |
| **Gauge** | 显示百分比 | CPU使用率、磁盘使用率 |
| **Bar gauge** | 多个条形对比 | 各节点内存使用对比 |
| **Table** | 展示明细数据 | Pod列表、告警列表 |
| **Heatmap** | 展示分布 | 延迟分布、错误码分布 |
| **Pie chart** | 展示占比 | 各命名空间Pod占比 |
| **Logs** | 日志流 | 应用日志、错误日志 |
| **Node Graph** | 服务拓扑 | 微服务调用关系 |

#### Dashboard设计原则

**1. 信息层次 (金字塔原则)**

```
┌─────────────────────────────────────────┐
│  Level 1: 全局状态 (顶部)                │
│  - 关键指标 (Stat/Gauge)                │
│  - 一眼看出系统健康状态                  │
│  - 示例: 节点数、CPU、内存、告警数       │
├─────────────────────────────────────────┤
│  Level 2: 核心趋势 (中部)                │
│  - 时序图 (Time series)                 │
│  - 展示关键指标的历史趋势                │
│  - 示例: CPU/内存趋势、网络流量、QPS    │
├─────────────────────────────────────────┤
│  Level 3: 详细数据 (底部)                │
│  - 表格/明细 (Table)                    │
│  - 用于深入分析和排查问题                │
│  - 示例: Pod列表、Top 10 CPU Pod        │
└─────────────────────────────────────────┘
```

**示例布局**：

```
┌────────────────────────────────────────────────────────────┐
│ [Nodes: 10] [Pods: 127] [CPU: 45%] [Memory: 62%] [🔴 Alerts: 3] │  ← Level 1
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌─────────────────────┐  ┌─────────────────────┐        │
│  │   CPU Usage %       │  │   Memory Usage %    │        │  ← Level 2
│  │  [折线图 - 6h]       │  │  [折线图 - 6h]       │        │
│  └─────────────────────┘  └─────────────────────┘        │
│                                                            │
│  ┌─────────────────────┐  ┌─────────────────────┐        │
│  │  Network I/O        │  │   Disk I/O          │        │
│  │  [折线图 - 6h]       │  │  [折线图 - 6h]       │        │
│  └─────────────────────┘  └─────────────────────┘        │
│                                                            │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Top 10 Pods by CPU Usage                            │ │  ← Level 3
│  │  [Table: Pod名称 | 命名空间 | CPU | 内存 | 状态]      │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**2. 配色原则**

```yaml
# 使用语义化颜色
健康状态: 绿色 (#73BF69)
警告状态: 黄色 (#F2CC0C)
错误状态: 红色 (#E02F44)
信息展示: 蓝色 (#5794F2)

# 避免:
- 过多颜色 (不超过5种主色)
- 饱和度过高 (刺眼)
- 红绿色盲不友好的配色
```

**3. 时间范围设置**

```yaml
默认时间范围建议:
- 实时监控: Last 15 minutes (快速发现问题)
- 趋势分析: Last 6 hours (查看日内趋势)
- 容量规划: Last 7 days (周期性分析)
- 月度报告: Last 30 days

刷新间隔建议:
- 实时监控: 5s-10s
- 普通监控: 30s-1m
- 历史分析: 关闭自动刷新 (Manual)
```

**4. Panel优化技巧**

```yaml
# 减少查询数量
❌ 错误: 每个Panel一个查询
✅ 正确: 使用多个Series在一个Panel中

# 使用合理的时间范围
❌ 错误: rate(http_requests_total[1d])
✅ 正确: rate(http_requests_total[5m])

# 添加有意义的Legend
❌ 错误: {{instance}}
✅ 正确: {{namespace}}/{{pod}} - {{container}}

# 设置合理的Y轴范围
时间序列: Min=0, Max=Auto (避免Y轴抖动)
百分比: Min=0, Max=100
```

---

### 9.3.4 变量与模板应用

变量 (Variables) 是Grafana最强大的功能之一，它让一个Dashboard可以适配多个环境/集群/服务。

#### 变量类型

**1. Query变量 (从数据源查询)**

最常用的变量类型，从Prometheus查询标签值。

```yaml
# 变量名: cluster
# 类型: Query
# 数据源: Prometheus
# Query:
label_values(kube_node_info, cluster)

# 用法: 在Panel中使用 $cluster
up{cluster="$cluster"}
```

**完整配置示例**：

```json
{
  "name": "namespace",
  "type": "query",
  "datasource": "Prometheus",
  "query": "label_values(kube_pod_info, namespace)",
  "regex": "",
  "sort": 1,
  "refresh": 2,
  "multi": true,
  "includeAll": true,
  "allValue": ".*",
  "current": {
    "selected": true,
    "text": "All",
    "value": "$__all"
  }
}
```

**关键配置项**：

| 配置项 | 说明 | 示例 |
|--------|------|------|
| **multi** | 是否允许多选 | true: 可选多个命名空间 |
| **includeAll** | 是否包含"All"选项 | true: 显示所有命名空间 |
| **allValue** | "All"的实际值 | ".*" (正则匹配所有) |
| **refresh** | 刷新策略 | On Dashboard Load (加载时) |
| **sort** | 排序方式 | 1: Alphabetical (字母序) |
| **regex** | 过滤结果 | /^prod-.*/ (只显示prod开头) |

**2. Custom变量 (自定义列表)**

手动定义的固定列表。

```json
{
  "name": "environment",
  "type": "custom",
  "query": "production,staging,development",
  "current": {
    "text": "production",
    "value": "production"
  }
}
```

**3. Constant变量 (常量)**

固定值，通常用于全局配置。

```json
{
  "name": "alert_threshold",
  "type": "constant",
  "query": "80",
  "hide": 2  // 0: 显示, 1: 显示标签, 2: 完全隐藏
}
```

**4. Interval变量 (时间间隔)**

动态调整查询时间范围。

```json
{
  "name": "interval",
  "type": "interval",
  "query": "1m,5m,10m,30m,1h,6h,12h,1d",
  "auto": true,
  "auto_count": 30,
  "auto_min": "10s"
}
```

**使用示例**：

```promql
# 使用$interval变量
rate(http_requests_total[$interval])

# Grafana会根据时间范围自动选择合适的interval
# 时间范围: 1小时 → interval: 1m
# 时间范围: 7天  → interval: 1h
```

**5. Data source变量 (数据源)**

在多个数据源间切换。

```json
{
  "name": "datasource",
  "type": "datasource",
  "query": "prometheus",
  "current": {
    "text": "Prometheus-Prod",
    "value": "Prometheus-Prod"
  }
}
```

#### 变量高级用法

**1. 级联变量 (Chained Variables)**

一个变量依赖另一个变量的值。

```yaml
# 变量1: cluster
Query: label_values(kube_node_info, cluster)

# 变量2: namespace (依赖cluster)
Query: label_values(kube_pod_info{cluster="$cluster"}, namespace)

# 变量3: pod (依赖namespace)
Query: label_values(kube_pod_info{cluster="$cluster", namespace="$namespace"}, pod)
```

**使用效果**：
```
选择 cluster=production → namespace下拉列表只显示production集群的命名空间
选择 namespace=api → pod下拉列表只显示api命名空间的Pod
```

**2. 正则过滤**

```yaml
# 只显示以"prod-"开头的命名空间
Query: label_values(kube_pod_info, namespace)
Regex: /^prod-.*/

# 排除系统命名空间
Query: label_values(kube_pod_info, namespace)
Regex: /^(?!kube-|default).*/
```

**3. 变量格式化**

在查询中使用变量时，可以用不同的格式：

```promql
# 原始值
up{namespace="$namespace"}

# 正则匹配 (多选时)
up{namespace=~"$namespace"}

# Pipe分隔 (用于Loki)
{namespace=~"${namespace:pipe}"}
# 结果: {namespace=~"api|web|worker"}

# CSV格式
# 结果: "api","web","worker"

# JSON格式
# 结果: ["api","web","worker"]
```

**常用格式化选项**：

| 格式 | 语法 | 输出示例 | 用途 |
|------|------|---------|------|
| **regex** | `${var:regex}` | `api\|web` | Prometheus标签匹配 |
| **pipe** | `${var:pipe}` | `api\|web` | Loki查询 |
| **csv** | `${var:csv}` | `"api","web"` | SQL IN查询 |
| **json** | `${var:json}` | `["api","web"]` | JSON API |
| **raw** | `${var:raw}` | `$__all` | 获取原始值 |

#### 实战: 多集群多环境通用Dashboard

**变量配置**：

```yaml
# 1. 数据源变量
Name: datasource
Type: Data source
Query: prometheus
Label: Prometheus数据源

# 2. 集群变量
Name: cluster
Type: Query
Datasource: $datasource
Query: label_values(kube_node_info, cluster)
Label: 集群
Multi-value: true
Include All: true

# 3. 命名空间变量
Name: namespace
Type: Query
Datasource: $datasource
Query: label_values(kube_pod_info{cluster=~"$cluster"}, namespace)
Label: 命名空间
Multi-value: true
Include All: true
Regex: /^(?!kube-|default).*/  # 排除系统命名空间

# 4. 应用变量
Name: app
Type: Query
Datasource: $datasource
Query: label_values(kube_pod_labels{cluster=~"$cluster", namespace=~"$namespace"}, label_app)
Label: 应用
Multi-value: true
Include All: true

# 5. 时间间隔变量
Name: interval
Type: Interval
Query: 1m,5m,10m,30m,1h
Auto: true
Auto min: 30s
```

**Panel查询示例**：

```promql
# CPU使用率 (支持多集群/多命名空间/多应用)
sum(rate(container_cpu_usage_seconds_total{
  cluster=~"$cluster",
  namespace=~"$namespace",
  pod=~"$app.*"
}[$interval])) by (pod)

# 内存使用
sum(container_memory_working_set_bytes{
  cluster=~"$cluster",
  namespace=~"$namespace",
  pod=~"$app.*"
}) by (pod) / 1024 / 1024 / 1024

# QPS
sum(rate(http_requests_total{
  cluster=~"$cluster",
  namespace=~"$namespace",
  app=~"$app"
}[$interval])) by (app, method)

# 错误率
sum(rate(http_requests_total{
  cluster=~"$cluster",
  namespace=~"$namespace",
  app=~"$app",
  status=~"5.."
}[$interval])) by (app)
  /
sum(rate(http_requests_total{
  cluster=~"$cluster",
  namespace=~"$namespace",
  app=~"$app"
}[$interval])) by (app)
  * 100
```

**使用体验**：

```
顶部变量栏:
[Prometheus数据源 ▼] [集群: All ▼] [命名空间: All ▼] [应用: All ▼] [间隔: Auto ▼]

用户操作:
1. 选择数据源: Prometheus-Prod
2. 选择集群: production
3. 选择命名空间: api, web (多选)
4. 选择应用: user-service, order-service (多选)

→ Dashboard自动刷新，只显示选中的Pod数据
→ 一个Dashboard适配所有环境！
```

---

### 9.3.5 企业级仪表盘示例

#### Dashboard 1: Kubernetes集群总览

**目标受众**: 运维团队、管理层
**刷新间隔**: 30秒
**时间范围**: 最近6小时

**布局设计**：

```yaml
Row 1: 关键指标 (Stat)
- 集群节点数 (Ready/Total)
- 运行中的Pod数量
- 当前CPU使用率
- 当前内存使用率
- 活跃告警数量

Row 2: 资源趋势 (Time series)
- CPU使用率趋势 (6小时)
- 内存使用率趋势 (6小时)
- 网络I/O (接收/发送)
- 磁盘I/O (读/写)

Row 3: 节点状态 (Table)
- 节点列表 (名称/状态/CPU/内存/Pod数/年龄)
```

**关键查询**：

```promql
# 节点Ready数量
count(kube_node_status_condition{condition="Ready", status="true"})

# 总节点数
count(kube_node_info)

# 集群CPU使用率
(1 - avg(rate(node_cpu_seconds_total{mode="idle"}[5m]))) * 100

# 集群内存使用率
(1 - sum(node_memory_MemAvailable_bytes) / sum(node_memory_MemTotal_bytes)) * 100

# 运行中的Pod
count(kube_pod_status_phase{phase="Running"})

# 活跃告警
count(ALERTS{alertstate="firing"})

# 节点详情表格
sort_desc(
  (1 - avg by (node) (rate(node_cpu_seconds_total{mode="idle"}[5m]))) * 100
)
```

#### Dashboard 2: 应用性能监控 (RED方法)

**目标受众**: 开发团队、SRE
**刷新间隔**: 10秒
**时间范围**: 最近1小时

**布局设计**：

```yaml
Row 1: RED指标 (Stat + Gauge)
- 当前QPS
- 平均延迟
- P95延迟
- P99延迟
- 错误率 (%)
- 成功率 (%)

Row 2: 趋势图 (Time series)
- QPS趋势 (按method分组)
- 延迟趋势 (P50/P95/P99)
- 错误率趋势

Row 3: 详细分析
- HTTP状态码分布 (饼图)
- 慢请求Top 10 (Table)
- 错误日志 (Logs面板)
```

**关键查询**：

```promql
# Rate: QPS
sum(rate(http_requests_total{
  namespace=~"$namespace",
  app=~"$app"
}[$interval])) by (app, method)

# Errors: 错误率
sum(rate(http_requests_total{
  namespace=~"$namespace",
  app=~"$app",
  status=~"5.."
}[$interval])) by (app)
  /
sum(rate(http_requests_total{
  namespace=~"$namespace",
  app=~"$app"
}[$interval])) by (app)
  * 100

# Duration: P95延迟
histogram_quantile(0.95,
  sum(rate(http_request_duration_seconds_bucket{
    namespace=~"$namespace",
    app=~"$app"
  }[$interval])) by (le, app)
)

# Duration: P99延迟
histogram_quantile(0.99,
  sum(rate(http_request_duration_seconds_bucket{
    namespace=~"$namespace",
    app=~"$app"
  }[$interval])) by (le, app)
)

# HTTP状态码分布
sum by (status) (
  increase(http_requests_total{
    namespace=~"$namespace",
    app=~"$app"
  }[1h])
)
```

#### Dashboard 3: 节点详细监控

**目标受众**: 运维团队
**刷新间隔**: 30秒
**时间范围**: 最近6小时

```yaml
Variables:
- node: label_values(kube_node_info, node)

Row 1: 节点基本信息
- 节点名称
- 节点IP
- 内核版本
- kubelet版本
- 运行时间

Row 2: CPU详情
- CPU使用率 (%)
- CPU各模式时间 (user/system/iowait/idle)
- CPU负载 (1m/5m/15m)
- CPU核心使用分布

Row 3: 内存详情
- 内存使用率 (%)
- 内存使用明细 (used/free/buffers/cached)
- Swap使用率

Row 4: 磁盘详情
- 磁盘使用率 (按挂载点)
- 磁盘IOPS (读/写)
- 磁盘吞吐量 (读/写 MB/s)

Row 5: 网络详情
- 网络流量 (接收/发送 MB/s)
- 网络错误率
- TCP连接状态分布
```

**关键查询**：

```promql
# 节点CPU使用率
100 - (avg by (instance) (
  irate(node_cpu_seconds_total{
    instance="$node",
    mode="idle"
  }[5m])
) * 100)

# 节点内存使用率
(1 - (
  node_memory_MemAvailable_bytes{instance="$node"}
    /
  node_memory_MemTotal_bytes{instance="$node"}
)) * 100

# 磁盘使用率
(node_filesystem_size_bytes{instance="$node", fstype!~"tmpfs|fuse.*"}
  -
node_filesystem_free_bytes{instance="$node", fstype!~"tmpfs|fuse.*"})
  /
node_filesystem_size_bytes{instance="$node", fstype!~"tmpfs|fuse.*"}
  * 100

# 网络接收速率 (MB/s)
rate(node_network_receive_bytes_total{instance="$node", device!="lo"}[5m]) / 1024 / 1024

# 网络发送速率 (MB/s)
rate(node_network_transmit_bytes_total{instance="$node", device!="lo"}[5m]) / 1024 / 1024
```

#### Dashboard 4: Pod详细监控

```yaml
Variables:
- namespace
- pod

Row 1: Pod基本信息
- Pod状态
- 重启次数
- Node位置
- IP地址
- 创建时间

Row 2: 容器资源使用
- CPU使用 (millicores)
- 内存使用 (MB)
- CPU使用 vs Requests/Limits
- 内存使用 vs Requests/Limits

Row 3: 网络与存储
- 网络流量
- 文件系统使用
- Volume使用率

Row 4: 日志与事件
- 容器日志 (Loki)
- Pod事件
```

**关键查询**：

```promql
# Pod CPU使用 (millicores)
sum(rate(container_cpu_usage_seconds_total{
  namespace="$namespace",
  pod="$pod",
  container!=""
}[5m])) by (container) * 1000

# Pod内存使用 (MB)
sum(container_memory_working_set_bytes{
  namespace="$namespace",
  pod="$pod",
  container!=""
}) by (container) / 1024 / 1024

# Pod重启次数
kube_pod_container_status_restarts_total{
  namespace="$namespace",
  pod="$pod"
}

# Pod CPU Requests
sum(kube_pod_container_resource_requests{
  namespace="$namespace",
  pod="$pod",
  resource="cpu"
}) by (container)

# Pod CPU Limits
sum(kube_pod_container_resource_limits{
  namespace="$namespace",
  pod="$pod",
  resource="cpu"
}) by (container)
```

---

### 9.3.6 Dashboard导入导出与共享

#### 导出Dashboard

**方式1: 通过UI导出**

```
1. 打开Dashboard
2. 点击右上角 "Share" 图标
3. 选择 "Export" 标签
4. 点击 "Save to file" 下载JSON文件
```

**方式2: 通过API导出**

```bash
# 获取Dashboard UID
DASHBOARD_UID="abc123"

# 导出Dashboard JSON
curl -u admin:admin123 \
  http://grafana:3000/api/dashboards/uid/$DASHBOARD_UID \
  | jq '.dashboard' > dashboard.json
```

#### 导入Dashboard

**方式1: 通过UI导入**

```
1. 左侧菜单 → Dashboards → Import
2. 上传JSON文件或粘贴JSON内容
3. 选择数据源
4. 点击 "Import"
```

**方式2: 通过Provisioning自动导入**

```yaml
# dashboards.yaml
apiVersion: 1
providers:
- name: 'default'
  orgId: 1
  folder: ''
  type: file
  disableDeletion: false
  updateIntervalSeconds: 30
  allowUiUpdates: true
  options:
    path: /etc/grafana/provisioning/dashboards
```

```yaml
# 在Kubernetes中挂载Dashboard JSON
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: monitoring
data:
  k8s-cluster.json: |
    {
      "dashboard": {
        "title": "Kubernetes Cluster",
        ...
      }
    }
---
# 在Deployment中挂载
spec:
  volumes:
  - name: dashboards
    configMap:
      name: grafana-dashboards
  containers:
  - name: grafana
    volumeMounts:
    - name: dashboards
      mountPath: /etc/grafana/provisioning/dashboards
```

#### 从官方库导入Dashboard

Grafana官方提供了数千个社区Dashboard：https://grafana.com/grafana/dashboards

**推荐的Kubernetes Dashboard**：

| Dashboard ID | 名称 | 说明 |
|-------------|------|------|
| **315** | Kubernetes cluster monitoring | 集群整体监控 |
| **747** | Kubernetes Deployment | Deployment监控 |
| **1860** | Node Exporter Full | 节点详细监控 |
| **3119** | Kubernetes Cluster (Prometheus) | 集群资源监控 |
| **6417** | Kubernetes Cluster Monitoring | 全面的集群监控 |
| **7249** | Kubernetes Cluster | 简洁的集群视图 |
| **12114** | Kubernetes Nodes | 节点监控 |
| **13770** | Kubernetes / Views / Pods | Pod详细监控 |

**导入步骤**：

```
1. 访问 https://grafana.com/grafana/dashboards/315
2. 复制Dashboard ID: 315
3. 在Grafana中: Dashboards → Import
4. 输入Dashboard ID: 315
5. 选择Prometheus数据源
6. 点击Import
```

#### Dashboard权限管理

**1. Organization级别权限**

```
Admin: 完全控制 (创建/编辑/删除Dashboard)
Editor: 可编辑Dashboard
Viewer: 只读访问
```

**2. Dashboard级别权限**

```yaml
# 通过UI设置:
Dashboard → Settings → Permissions

# 添加权限:
- User: alice@example.com, Role: Editor
- Team: SRE Team, Role: Admin
- Organization: Main Org, Role: Viewer
```

**3. 文件夹权限**

```yaml
# 创建文件夹并设置权限
Dashboards → New Folder → "Production"

# 设置权限:
- Team: SRE Team → Admin
- Team: Dev Team → Editor
- Everyone Else → Viewer
```

---

**📊 9.3节总结**

本节我们深入学习了Grafana可视化平台的部署与使用：

✅ **Grafana部署**：
- Helm Chart快速部署
- 手动部署完整配置
- 配置文件详解 (grafana.ini)

✅ **数据源集成**：
- Prometheus数据源配置 (基础/高级/多集群)
- Loki日志数据源
- Jaeger链路追踪数据源
- Elasticsearch数据源

✅ **仪表盘设计**：
- Dashboard基础概念与结构
- 图表类型选择指南
- 设计原则 (信息层次/配色/时间范围)
- Panel优化技巧

✅ **变量与模板**：
- 5种变量类型 (Query/Custom/Constant/Interval/Data source)
- 变量高级用法 (级联/正则/格式化)
- 多集群多环境通用Dashboard

✅ **企业级Dashboard**：
- 集群总览Dashboard
- RED方法应用性能监控
- 节点详细监控
- Pod详细监控

✅ **Dashboard管理**：
- 导入导出方法
- 官方Dashboard库推荐
- 权限管理

**下一节预告**：我们将学习AlertManager告警管理，包括告警规则编写、告警路由与分组、抑制与静默策略，以及多渠道通知集成（钉钉/企业微信/PagerDuty）。

---
