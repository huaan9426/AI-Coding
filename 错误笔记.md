# AI 应用开发 - 错误笔记

> 记录开发过程中遇到的所有错误和解决方案

---

## 📋 目录

- [Poetry 安装依赖错误](#poetry-安装依赖错误)
  - [错误 #1: Python 版本不兼容](#错误-1-python-版本不兼容)
  - [错误 #2: Python 3.14 无预编译包导致安装失败](#错误-2-python-314-无预编译包导致安装失败)

---

## Poetry 安装依赖错误

### 错误 #1: Python 版本不兼容====

**发生时间：** 2025-11-19
**项目：** pdf-chatbot
**操作：** `poetry add langchain`

#### ❌ 错误信息

```
The current project's supported Python range (>=3.14) is not compatible with some of the required packages Python requirement:
  - langchain requires Python <4.0.0,>=3.10.0, so it will not be installable for Python >=4.0.0

Because no versions of langchain match >1.0.7,<2.0.0
 and langchain (1.0.7) requires Python <4.0.0,>=3.10.0, langchain is forbidden.
So, because pdf-chatbot depends on langchain (^1.0.7), version solving failed.
```

#### 🔍 错误原因

1. **项目配置问题：**
   - 项目的 `pyproject.toml` 文件中设置了 `requires-python = ">=3.14"`
   - 这表示项目只支持 Python 3.14 及以上版本

2. **依赖库要求：**
   - langchain 要求 Python 版本范围是 `>=3.10.0,<4.0.0`
   - 意味着 langchain 支持 Python 3.10 到 3.x 的所有版本，但不保证支持 Python 4.0

3. **冲突点：**
   - 项目设置 `>=3.14` 意味着"可能会用 Python 4.0+"
   - 但 langchain 明确说"我不支持 Python 4.0+"
   - Poetry 认为这两个要求无法同时满足

#### 🔧 解决方案

**第一次尝试（失败）：**
```toml
# 修改 pyproject.toml
requires-python = ">=3.9,<4.0"
```

**结果：** 仍然报错，因为 langchain 最低要求 Python 3.10

**第二次尝试（成功）：**
```toml
# 修改 pyproject.toml
requires-python = ">=3.10,<4.0"
```

**成功安装！** Poetry 安装了 langchain 及其 32 个依赖包。

#### 📝 修改的文件

**文件：** `/Users/huaan/Projects/AI/pdf-chatbot/pyproject.toml`

**修改前：**
```toml
requires-python = ">=3.14"
```

**修改后：**
```toml
requires-python = ">=3.10,<4.0"
```

#### ✅ 验证结果

```bash
poetry add langchain
```

**输出：**
```
Using version ^1.0.7 for langchain
Package operations: 32 installs, 0 updates, 0 removals

- Installing certifi (2025.11.12)
- Installing charset-normalizer (3.4.4)
... (省略中间 28 个包)
- Installing langchain (1.0.7)

Writing lock file
```

**安装的包：** 32 个（包括 langchain 及其所有依赖）

#### 💡 经验总结

1. **Python 版本范围要合理：**
   - 不要设置过于严格的版本要求（如 `>=3.14`）
   - 应该设置一个范围，同时兼容当前和未来的小版本
   - 推荐格式：`>=3.x,<4.0`（x 为最低支持版本）

2. **理解依赖的版本要求：**
   - 安装依赖前，查看它的 Python 版本要求
   - 确保项目的 Python 版本范围与依赖兼容
   - langchain 要求 `>=3.10,<4.0`，所以项目也应该这样设置

3. **Poetry 的版本解析逻辑：**
   - Poetry 会检查所有依赖的版本要求
   - 如果发现未来可能不兼容（如 Python 4.0），会提前报错
   - 这是好事，避免了以后的兼容性问题

4. **如何快速解决：**
   ```bash
   # 1. 查看错误提示中的版本要求
   # 2. 修改 pyproject.toml 中的 requires-python
   # 3. 设置为与依赖兼容的范围
   # 4. 重新运行 poetry add
   ```

#### 📚 相关知识

**Python 版本号规则：**
- `>=3.10` - 大于等于 3.10
- `<4.0` - 小于 4.0
- `>=3.10,<4.0` - 3.10 到 3.x 的所有版本
- `^1.0.7` - 1.0.7 到 2.0.0 之前的版本（Poetry 的 caret 语法）

**Poetry 依赖解析：**
1. 读取所有依赖的版本要求
2. 计算所有要求的交集
3. 如果交集为空（无法同时满足），报错
4. 如果有交集，选择最新的兼容版本

#### 🔗 参考资料

- [Poetry 依赖规范文档](https://python-poetry.org/docs/dependency-specification/)
- [Poetry Python 版本限制](https://python-poetry.org/docs/dependency-specification/#python-restricted-dependencies)

---

### 错误 #2: Python 3.14 无预编译包导致安装失败

**发生时间：** 2025-11-19
**项目：** pdf-chatbot
**操作：** `poetry add streamlit`

#### ❌ 错误信息

```
Package operations: 2 installs, 0 updates, 0 removals

  - Installing pyarrow (21.0.0): Failed

PEP517 build of a dependency failed

Backend subprocess exited when trying to invoke build_wheel

error: command 'cmake' failed: No such file or directory
```

**后续尝试安装其他包也失败：**
- `poetry add gradio` → pydantic-core 编译失败（需要 Rust）
- `poetry add chromadb` → onnxruntime 没有 Python 3.14 的预编译包，pypika 编译失败

#### 🔍 错误原因

1. **Python 3.14 太新（2025年1月刚发布）：**
   - 大部分 AI 库还没有为 Python 3.14 编译预编译包（wheel）
   - PyPI 上的包只有 Python 3.9-3.13 的预编译版本

2. **缺少预编译包时 Poetry 尝试从源码编译：**
   - pyarrow 需要 C++ 和 cmake
   - pydantic-core 需要 Rust 编译器
   - 编译环境复杂，容易失败

3. **根本问题：使用了不稳定的 Python 版本**
   - Python 3.14 是最新版，生态还不成熟
   - AI 开发推荐使用 Python 3.10-3.12（最稳定）

#### 🔧 解决方案

**最佳方案：切换到 Python 3.12（稳定版）**

**步骤1：安装 Python 3.12**
```bash
brew install python@3.12
```

**步骤2：让 Poetry 使用 Python 3.12**
```bash
poetry env use /opt/homebrew/bin/python3.12
```

**输出：**
```
Recreating virtualenv pdf-chatbot in /Users/huaan/Projects/AI/pdf-chatbot/.venv
```

**步骤3：重新安装所有依赖**
```bash
poetry install
```

**步骤4：安装 streamlit**
```bash
poetry add streamlit
```

**成功！** 安装了 streamlit 及其 28 个依赖包（包括 pyarrow）。

#### 📝 关键命令

```bash
# 1. 查看当前虚拟环境使用的 Python 版本
poetry env info

# 2. 切换 Python 版本
poetry env use /path/to/python3.12

# 3. 重新创建虚拟环境后，需要重新安装依赖
poetry install

# 4. 然后就可以正常安装新包了
poetry add streamlit
```

#### ✅ 验证结果

**成功安装的包：**
```
Package operations: 28 installs, 0 updates, 0 removals

- Installing pyarrow (21.0.0)      ← 之前失败的包
- Installing streamlit (1.51.0)
... (共 28 个包)
```

**检查虚拟环境 Python 版本：**
```bash
poetry run python --version
# 输出: Python 3.12.12
```

#### 💡 经验总结

1. **AI 开发推荐 Python 版本：**
   - ✅ **Python 3.12** - 最推荐（稳定 + 新特性）
   - ✅ Python 3.11 - 也很好
   - ✅ Python 3.10 - 兼容性最好
   - ⚠️ Python 3.13 - 较新，部分包可能没适配
   - ❌ Python 3.14 - 太新，大量包未适配

2. **识别"预编译包缺失"错误的特征：**
   - 错误信息包含 `build_wheel` 或 `PEP517 build failed`
   - 提示缺少编译工具（cmake、gcc、rust 等）
   - 错误发生在安装 C/C++/Rust 编写的包时（如 pyarrow、pydantic-core）

3. **遇到编译错误的处理策略：**
   ```
   第一步：检查是否使用了过新的 Python 版本
   第二步：切换到 Python 3.10-3.12
   第三步：重新创建虚拟环境
   第四步：重新安装依赖
   ```

4. **Poetry 虚拟环境管理：**
   - Poetry 为每个项目创建独立的虚拟环境
   - `poetry env use` 可以切换 Python 版本
   - 切换版本后虚拟环境会自动重建
   - 需要运行 `poetry install` 重新安装依赖

5. **为什么不安装编译工具而是换 Python 版本？**
   - 安装编译工具（cmake、gcc、rust）复杂且耗时
   - 即使能编译成功，可能因 Python 3.14 不兼容而运行出错
   - 使用稳定版 Python 可以直接用预编译包，简单可靠

#### 📚 相关知识

**什么是预编译包（wheel）？**
- Python 包的分发格式有两种：
  - **源码包（sdist）：** 需要在你电脑上编译
  - **预编译包（wheel）：** 已经编译好，直接安装
- wheel 文件命名包含 Python 版本和系统信息
- 例如：`pyarrow-21.0.0-cp312-cp312-macosx_11_0_arm64.whl`
  - `cp312` = Python 3.12
  - `macosx_11_0_arm64` = macOS ARM 架构

**为什么 AI 库需要编译？**
- 很多 AI 库（pyarrow、numpy、torch 等）用 C/C++ 编写核心部分
- 这样运行速度快（比纯 Python 快 100 倍）
- 但需要针对不同系统和 Python 版本编译

**Poetry 如何选择包版本？**
1. 先找预编译包（wheel）
2. 找不到就下载源码包（sdist）
3. 尝试在本地编译
4. 编译失败就报错

#### 🔗 参考资料

- [Python Packaging User Guide - Binary Distribution](https://packaging.python.org/en/latest/specifications/binary-distribution-format/)
- [Poetry 环境管理文档](https://python-poetry.org/docs/managing-environments/)
- [PyPI Python 版本支持统计](https://pypistats.org/)

#### 🎯 避坑指南

**创建新 AI 项目时：**
```bash
# ❌ 不要用系统默认的 Python（可能太新或太旧）
poetry new my-project

# ✅ 明确指定 Python 3.12
poetry new my-project
cd my-project
poetry env use python3.12
```

**修改现有项目的 Python 版本：**
```bash
# 1. 切换 Python 版本
poetry env use python3.12

# 2. 删除旧的 lock 文件（可选）
rm poetry.lock

# 3. 重新安装依赖
poetry install

# 4. 验证版本
poetry run python --version
```

---

## 错误模板（以后遇到新错误就按这个格式记录）

### 错误 #X: [错误简述]

**发生时间：** YYYY-MM-DD
**项目：** [项目名称]
**操作：** [触发错误的命令或操作]

#### ❌ 错误信息

```
[完整的错误信息]
```

#### 🔍 错误原因

[详细分析错误原因]

#### 🔧 解决方案

[尝试的解决方法和最终成功的方案]

#### 📝 修改的文件

[列出修改了哪些文件，修改前后的对比]

#### ✅ 验证结果

[验证解决方案是否有效]

#### 💡 经验总结

[从这个错误中学到了什么]

#### 🔗 参考资料

[相关文档或资料链接]

---

## 错误统计

| 错误类型 | 次数 | 最后发生时间 |
|---------|------|-------------|
| Python 版本不兼容 | 1 | 2025-11-19 |
| 预编译包缺失/编译失败 | 1 | 2025-11-19 |
| 依赖冲突 | 0 | - |
| API 密钥错误 | 0 | - |
| 网络问题 | 0 | - |

---

## 常见错误快速索引

### Poetry 相关
- [x] Python 版本不兼容 → 修改 `requires-python` 范围
- [x] 编译失败/预编译包缺失 → 切换到 Python 3.10-3.12

### LangChain 相关
- [ ] API 密钥未设置
- [ ] Token 超限
- [ ] 向量数据库错误

### Python 环境
- [x] Python 版本太新导致包不兼容 → 使用 Python 3.12
- [ ] 虚拟环境未激活
- [ ] 包未安装
- [ ] 版本冲突

---

**笔记创建时间：** 2025-11-19
**总错误数：** 2
**已解决：** 2
**未解决：** 0
