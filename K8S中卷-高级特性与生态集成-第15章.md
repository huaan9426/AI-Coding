# 第15章：调度器扩展与自定义

## 章节概述

在第14章中，我们学习了准入控制与策略管理。本章将深入学习Kubernetes的**调度器（Scheduler）**，这是集群资源管理的核心组件。我们将从调度器架构开始，逐步学习如何扩展和自定义调度器，最终实现一个GPU资源调度器。

**本章核心目标**：
- 深入理解Kubernetes调度器的架构和工作原理
- 掌握调度框架（Scheduling Framework）的扩展机制
- 学会开发自定义调度器和调度器插件
- 理解多调度器共存的机制
- 实战：开发GPU资源调度器

**为什么需要学习调度器？**

```
调度器的重要性：
┌────────────────────────────────────────────┐
│  资源效率                                   │
│  - 合理分配CPU、内存、GPU等资源            │
│  - 提高节点利用率                          │
│  - 减少资源浪费                            │
├────────────────────────────────────────────┤
│  应用性能                                   │
│  - 将Pod调度到合适的节点                   │
│  - 考虑亲和性和反亲和性                    │
│  - 优化网络延迟和存储访问                  │
├────────────────────────────────────────────┤
│  高可用性                                   │
│  - 实现工作负载的分散                      │
│  - 避免单点故障                            │
│  - 支持故障域隔离                          │
├────────────────────────────────────────────┤
│  特殊需求                                   │
│  - GPU/FPGA等专用硬件调度                  │
│  - 拓扑感知调度                            │
│  - 成本优化调度                            │
└────────────────────────────────────────────┘
```

**本章学习路径**：

```
15.1 调度器架构深入
  ↓
15.2 调度框架（Scheduling Framework）
  ↓
15.3 自定义调度器开发
  ↓
15.4 调度器插件开发
  ↓
15.5 多调度器共存
  ↓
15.6 实战：GPU资源调度器
```

---

## 15.1 调度器架构深入

### 15.1.1 调度器概述

**什么是Kubernetes调度器？**

Kubernetes调度器（kube-scheduler）是控制平面的核心组件，负责为新创建的Pod选择合适的节点（Node）运行。调度决策考虑了资源需求、硬件/软件/策略约束、亲和性和反亲和性规则、数据本地性、工作负载干扰等因素。

**调度器的核心职责**：

```
调度器的三大职责：
┌────────────────────────────────────────────┐
│  1. 监听（Watch）                          │
│     - 监听API Server的Pod创建事件          │
│     - 过滤出未调度的Pod（.spec.nodeName为空）│
│     - 加入调度队列                         │
└────────────────────────────────────────────┘
                ↓
┌────────────────────────────────────────────┐
│  2. 调度（Schedule）                       │
│     - 预选（Predicates/Filter）            │
│       找出所有满足条件的节点               │
│     - 优选（Priorities/Score）             │
│       给节点打分，选择最优节点             │
│     - 绑定前检查（Reserve）                │
│       预留资源，执行插件                   │
└────────────────────────────────────────────┘
                ↓
┌────────────────────────────────────────────┐
│  3. 绑定（Bind）                           │
│     - 将Pod绑定到选定的节点                │
│     - 更新API Server中Pod的.spec.nodeName  │
│     - 通知相关组件（kubelet）              │
└────────────────────────────────────────────┘
```

**调度器在K8s架构中的位置**：

```
Kubernetes控制平面架构：
┌─────────────────────────────────────────────────────┐
│                    Control Plane                     │
│  ┌────────────────┐  ┌────────────────┐            │
│  │ API Server     │  │ etcd           │            │
│  │ - REST API     │  │ - 数据存储     │            │
│  └────────────────┘  └────────────────┘            │
│           ↑                  ↑                       │
│           │                  │                       │
│  ┌────────┴──────────────────┴────────┐            │
│  │                                     │            │
│  │  ┌──────────────────────────────┐  │            │
│  │  │  Scheduler ⭐                │  │            │
│  │  │  - 监听未调度Pod             │  │            │
│  │  │  - 预选节点（Filter）        │  │            │
│  │  │  - 优选节点（Score）         │  │            │
│  │  │  - 绑定Pod到节点             │  │            │
│  │  └──────────────────────────────┘  │            │
│  │                                     │            │
│  │  ┌──────────────────────────────┐  │            │
│  │  │  Controller Manager          │  │            │
│  │  │  - Deployment控制器          │  │            │
│  │  │  - ReplicaSet控制器          │  │            │
│  │  └──────────────────────────────┘  │            │
│  └─────────────────────────────────────┘            │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│                    Worker Nodes                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────┐  │
│  │ Node 1       │  │ Node 2       │  │ Node 3   │  │
│  │ - kubelet    │  │ - kubelet    │  │ - kubelet│  │
│  │ - Pod 1      │  │ - Pod 2      │  │          │  │
│  │ - Pod 3      │  │              │  │          │  │
│  └──────────────┘  └──────────────┘  └──────────┘  │
└─────────────────────────────────────────────────────┘
```

### 15.1.2 调度器架构设计

**调度器的整体架构**：

```
kube-scheduler内部架构：
┌───────────────────────────────────────────────────────┐
│                   kube-scheduler                      │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │  Informer（信息同步器）                         │ │
│  │  ┌─────────────┐  ┌─────────────┐             │ │
│  │  │ Pod Informer│  │ Node Informer│            │ │
│  │  │ - Watch Pods│  │ - Watch Nodes│            │ │
│  │  │ - 缓存Pod   │  │ - 缓存Node   │            │ │
│  │  └─────────────┘  └─────────────┘             │ │
│  │  ┌─────────────┐  ┌─────────────┐             │ │
│  │  │PVC Informer │  │ PV Informer  │            │ │
│  │  └─────────────┘  └─────────────┘             │ │
│  └─────────────────────────────────────────────────┘ │
│                        ↓                              │
│  ┌─────────────────────────────────────────────────┐ │
│  │  调度队列（Scheduling Queue）                   │ │
│  │  ┌─────────────────────────────────────────┐   │ │
│  │  │ ActiveQ（活跃队列）                     │   │ │
│  │  │ - 待调度的Pod                           │   │ │
│  │  │ - 按优先级排序                          │   │ │
│  │  └─────────────────────────────────────────┘   │ │
│  │  ┌─────────────────────────────────────────┐   │ │
│  │  │ BackoffQ（退避队列）                    │   │ │
│  │  │ - 调度失败的Pod                         │   │ │
│  │  │ - 指数退避重试                          │   │ │
│  │  └─────────────────────────────────────────┘   │ │
│  │  ┌─────────────────────────────────────────┐   │ │
│  │  │ UnschedulableQ（不可调度队列）          │   │ │
│  │  │ - 暂时无法调度的Pod                     │   │ │
│  │  │ - 等待集群状态变化                      │   │ │
│  │  └─────────────────────────────────────────┘   │ │
│  └─────────────────────────────────────────────────┘ │
│                        ↓                              │
│  ┌─────────────────────────────────────────────────┐ │
│  │  调度算法（Scheduling Algorithm）               │ │
│  │  ┌─────────────────────────────────────────┐   │ │
│  │  │ Scheduling Framework（调度框架）        │   │ │
│  │  │ ┌───────────────────────────────────┐   │   │ │
│  │  │ │ Filter Plugins（预选插件）        │   │   │ │
│  │  │ │ - NodeResourcesFit               │   │   │ │
│  │  │ │ - NodeAffinity                   │   │   │ │
│  │  │ │ - PodTopologySpread              │   │   │ │
│  │  │ │ - TaintToleration                │   │   │ │
│  │  │ └───────────────────────────────────┘   │   │ │
│  │  │          ↓                               │   │ │
│  │  │ ┌───────────────────────────────────┐   │   │ │
│  │  │ │ Score Plugins（优选插件）         │   │   │ │
│  │  │ │ - NodeResourcesBalancedAllocation│   │   │ │
│  │  │ │ - ImageLocality                  │   │   │ │
│  │  │ │ - InterPodAffinity               │   │   │ │
│  │  │ │ - NodeAffinity                   │   │   │ │
│  │  │ └───────────────────────────────────┘   │   │ │
│  │  └─────────────────────────────────────────┘   │ │
│  └─────────────────────────────────────────────────┘ │
│                        ↓                              │
│  ┌─────────────────────────────────────────────────┐ │
│  │  Binder（绑定器）                               │ │
│  │  - 将Pod绑定到选定的Node                        │ │
│  │  - 更新API Server                               │ │
│  └─────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────┘
```

**核心组件详解**：

**1. Informer机制**

```go
// Informer机制
type Scheduler struct {
    // Pod Informer - 监听Pod变化
    podInformer coreinformers.PodInformer

    // Node Informer - 监听Node变化
    nodeInformer coreinformers.NodeInformer

    // PV/PVC Informer - 监听存储资源
    pvInformer coreinformers.PersistentVolumeInformer
    pvcInformer coreinformers.PersistentVolumeClaimInformer

    // Service/StorageClass等其他Informer
    serviceInformer coreinformers.ServiceInformer
    storageClassInformer storageinformers.StorageClassInformer
}

// Informer工作原理
┌─────────────────────────────────────────────┐
│  API Server                                 │
│  - 维护资源的真实状态                       │
└─────────────────────────────────────────────┘
          ↓ (Watch机制)
┌─────────────────────────────────────────────┐
│  Reflector                                  │
│  - List & Watch资源                        │
│  - 检测资源变化                             │
└─────────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────────┐
│  Delta FIFO Queue                           │
│  - 存储资源变化事件                         │
│  - Added/Updated/Deleted/Sync               │
└─────────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────────┐
│  Indexer（本地缓存）                        │
│  - 内存中的资源快照                         │
│  - 支持快速查询                             │
│  - 减少API Server压力                       │
└─────────────────────────────────────────────┘
          ↓
┌─────────────────────────────────────────────┐
│  Event Handler                              │
│  - 处理资源变化事件                         │
│  - 触发调度流程                             │
└─────────────────────────────────────────────┘
```

**2. 调度队列**

```go
// 调度队列的三个子队列
type SchedulingQueue interface {
    // ActiveQ: 活跃队列
    // - 存储待调度的Pod
    // - 按优先级排序（高优先级先调度）
    // - 使用堆（Heap）数据结构
    Pop() (*framework.QueuedPodInfo, error)

    // BackoffQ: 退避队列
    // - 存储调度失败的Pod
    // - 实现指数退避（Exponential Backoff）
    // - 重试间隔：1s, 2s, 4s, 8s, ... 最大64s
    AddUnschedulableIfNotPresent(*framework.QueuedPodInfo, int64) error

    // UnschedulableQ: 不可调度队列
    // - 存储暂时无法调度的Pod
    // - 等待集群状态变化（Node增加、Pod删除等）
    // - 定期将Pod移回ActiveQ重试
    MoveAllToActiveOrBackoffQueue(event string)
}

// 队列流程
┌────────────┐
│  新建Pod   │
└────────────┘
      ↓
┌────────────┐
│  ActiveQ   │ ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
└────────────┘                         │
      ↓                                │
  调度尝试                             │
      ↓                                │
  成功？                               │
   ↙  ↘                                │
 Yes   No                              │
  ↓     ↓                              │
 绑定  ┌────────────┐                  │
      │  BackoffQ  │ ─ ─ ─ ─(指数退避)┘
      └────────────┘
           ↓
      (多次失败)
           ↓
      ┌────────────────┐
      │ UnschedulableQ │ ─ ─(集群变化)─ ┘
      └────────────────┘
```

**3. 调度框架（Scheduling Framework）**

调度框架是Kubernetes 1.15引入的新架构，将调度过程分为多个扩展点（Extension Points）。

```
调度框架的扩展点：
┌─────────────────────────────────────────────────────┐
│                 调度周期                             │
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │  1. PreFilter（预过滤）                      │  │
│  │     - 预处理Pod信息                          │  │
│  │     - 计算前置条件                           │  │
│  │     示例：计算Pod的亲和性要求                │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  2. Filter（过滤/预选）                      │  │
│  │     - 找出所有可以运行Pod的节点              │  │
│  │     - 并发执行，检查每个节点                 │  │
│  │     示例：检查节点资源是否足够               │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  3. PostFilter（后过滤）                     │  │
│  │     - 当Filter阶段没有合适节点时触发         │  │
│  │     - 尝试抢占低优先级Pod                    │  │
│  │     示例：抢占逻辑                           │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  4. PreScore（预打分）                       │  │
│  │     - 预处理打分信息                         │  │
│  │     - 生成可共享的状态                       │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  5. Score（打分/优选）                       │  │
│  │     - 给每个节点打分                         │  │
│  │     - 并发执行                               │  │
│  │     示例：资源利用率打分                     │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  6. NormalizeScore（归一化打分）             │  │
│  │     - 将不同插件的分数归一化到0-100          │  │
│  │     - 应用插件权重                           │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  7. Reserve（预留）                          │  │
│  │     - 维护运行时状态                         │  │
│  │     - 预留资源                               │  │
│  │     示例：预留GPU、RDMA等资源                │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  8. Permit（许可）                           │  │
│  │     - 批准或拒绝调度结果                     │  │
│  │     - 可以等待外部批准                       │  │
│  │     示例：等待资源配额检查                   │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│                 绑定周期（异步）                     │
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │  9. PreBind（预绑定）                        │  │
│  │     - 绑定前的准备工作                       │  │
│  │     - 如挂载网络卷                           │  │
│  │     示例：预挂载PV                           │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  10. Bind（绑定）                            │  │
│  │      - 将Pod绑定到节点                       │  │
│  │      - 更新API Server                        │  │
│  └──────────────────────────────────────────────┘  │
│                    ↓                                 │
│  ┌──────────────────────────────────────────────┐  │
│  │  11. PostBind（后绑定）                      │  │
│  │      - 绑定后的清理工作                      │  │
│  │      - 发送事件通知                          │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 15.1.3 调度流程详解

**完整的调度流程**：

```
详细调度流程：
┌───────────────────────────────────────────────────┐
│  Step 1: 监听Pod创建事件                          │
│  ────────────────────────────────────────────     │
│  Informer监听API Server                           │
│  ↓                                                 │
│  检测到新创建的Pod（spec.nodeName为空）           │
│  ↓                                                 │
│  将Pod加入ActiveQ（按优先级排序）                 │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 2: 从队列中取出Pod                          │
│  ────────────────────────────────────────────     │
│  从ActiveQ Pop一个最高优先级的Pod                 │
│  ↓                                                 │
│  检查Pod是否可以调度                              │
│  - 是否被删除                                     │
│  - 是否已经调度                                   │
│  - 是否在调度中                                   │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 3: PreFilter阶段                            │
│  ────────────────────────────────────────────     │
│  执行PreFilter插件：                              │
│  ├─ NodePorts: 检查Pod需要的端口                  │
│  ├─ NodeAffinity: 解析节点亲和性要求              │
│  ├─ PodTopologySpread: 计算拓扑分布约束           │
│  └─ InterPodAffinity: 计算Pod间亲和性             │
│  ↓                                                 │
│  生成PreFilter状态（供后续插件使用）              │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 4: Filter阶段（预选）                       │
│  ────────────────────────────────────────────     │
│  并发检查每个节点：                               │
│  ┌────────────────────────────────────────────┐  │
│  │ Node 1                                     │  │
│  │ ├─ NodeUnschedulable: 节点是否可调度      │  │
│  │ ├─ NodeResourcesFit: 资源是否足够         │  │
│  │ ├─ NodeName: 是否指定了节点名             │  │
│  │ ├─ NodePorts: 端口是否冲突                │  │
│  │ ├─ NodeAffinity: 节点亲和性是否满足       │  │
│  │ ├─ VolumeBinding: 存储卷是否可用          │  │
│  │ ├─ TaintToleration: 污点容忍是否匹配      │  │
│  │ ├─ PodTopologySpread: 拓扑分布是否满足    │  │
│  │ └─ InterPodAffinity: Pod亲和性是否满足    │  │
│  │ 结果: ✅ 通过                              │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │ Node 2                                     │  │
│  │ ├─ ... 检查所有Filter插件                 │  │
│  │ 结果: ❌ 资源不足                          │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │ Node 3                                     │  │
│  │ ├─ ... 检查所有Filter插件                 │  │
│  │ 结果: ✅ 通过                              │  │
│  └────────────────────────────────────────────┘  │
│  ↓                                                 │
│  过滤结果: [Node 1, Node 3]                       │
└───────────────────────────────────────────────────┘
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
    有节点?                   无节点
        ↓                       ↓
      继续            ┌─────────────────────┐
                      │ Step 4.5: PostFilter│
                      │ ─────────────────── │
                      │ 尝试抢占:           │
                      │ - 找低优先级Pod     │
                      │ - 模拟删除          │
                      │ - 重新Filter        │
                      │ ↓                   │
                      │ 抢占成功?           │
                      │  ↙    ↘            │
                      │ Yes    No           │
                      │  ↓     ↓           │
                      │ 继续  失败          │
                      └─────────────────────┘
        ↓
┌───────────────────────────────────────────────────┐
│  Step 5: Score阶段（优选）                        │
│  ────────────────────────────────────────────     │
│  并发给每个候选节点打分：                         │
│  ┌────────────────────────────────────────────┐  │
│  │ Node 1                                     │  │
│  │ ├─ NodeResourcesBalancedAllocation: 30分  │  │
│  │ │   (CPU和内存使用均衡度)                  │  │
│  │ ├─ NodeResourcesLeastAllocated: 40分      │  │
│  │ │   (节点空闲资源越多分数越高)             │  │
│  │ ├─ ImageLocality: 20分                    │  │
│  │ │   (镜像是否在本地)                       │  │
│  │ ├─ InterPodAffinity: 50分                 │  │
│  │ │   (Pod间亲和性得分)                      │  │
│  │ ├─ NodeAffinity: 60分                     │  │
│  │ │   (节点亲和性得分)                       │  │
│  │ └─ PodTopologySpread: 45分                │  │
│  │     (拓扑分布均衡度)                       │  │
│  │ ─────────────────────────────────────     │  │
│  │ 加权求和: 30*1 + 40*1 + 20*5 + ...        │  │
│  │ 总分: 245 / 6 = 40.8                      │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │ Node 3                                     │  │
│  │ ... (同样计算)                             │  │
│  │ 总分: 75.5                                 │  │
│  └────────────────────────────────────────────┘  │
│  ↓                                                 │
│  选择得分最高的节点: Node 3 ✅                    │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 6: Reserve阶段                              │
│  ────────────────────────────────────────────     │
│  执行Reserve插件：                                │
│  ├─ VolumeBinding: 预留PV                         │
│  └─ 其他资源预留                                  │
│  ↓                                                 │
│  假设调度成功，更新内部状态                       │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 7: Permit阶段                               │
│  ────────────────────────────────────────────     │
│  等待外部批准（可选）：                           │
│  - 可以阻塞等待                                   │
│  - 可以设置超时                                   │
│  ↓                                                 │
│  批准通过                                         │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 8: PreBind阶段（异步）                      │
│  ────────────────────────────────────────────     │
│  执行PreBind插件：                                │
│  └─ VolumeBinding: 实际绑定PV                     │
│  ↓                                                 │
│  准备完成                                         │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 9: Bind阶段                                 │
│  ────────────────────────────────────────────     │
│  将Pod绑定到Node 3：                              │
│  ├─ 创建Binding对象                               │
│  ├─ POST到API Server                              │
│  ├─ 更新Pod.spec.nodeName = "node-3"              │
│  └─ API Server通知kubelet                         │
│  ↓                                                 │
│  绑定成功 ✅                                       │
└───────────────────────────────────────────────────┘
                    ↓
┌───────────────────────────────────────────────────┐
│  Step 10: PostBind阶段                            │
│  ────────────────────────────────────────────     │
│  执行PostBind插件：                               │
│  ├─ 记录事件                                      │
│  └─ 清理资源                                      │
│  ↓                                                 │
│  调度完成 🎉                                      │
└───────────────────────────────────────────────────┘
```

### 15.1.4 调度算法原理

**1. Filter算法（预选）**

Filter阶段使用多个插件并发检查每个节点，任何一个插件返回失败，该节点就被过滤掉。

**核心Filter插件**：

```yaml
# 1. NodeResourcesFit（资源适配）
算法: 检查节点的可分配资源是否满足Pod的资源请求

计算公式:
  NodeAllocatable = NodeCapacity - SystemReserved - KubeReserved
  PodRequest = sum(containers[].resources.requests)

  if PodRequest > (NodeAllocatable - NodeAllocated):
      return Fail
  else:
      return Success

示例:
  Node资源:
    - Capacity: CPU=8, Memory=16Gi
    - SystemReserved: CPU=0.5, Memory=1Gi
    - KubeReserved: CPU=0.5, Memory=1Gi
    - Allocatable: CPU=7, Memory=14Gi
    - Allocated: CPU=5, Memory=10Gi
    - Available: CPU=2, Memory=4Gi

  Pod请求:
    - CPU=1, Memory=2Gi

  结果: ✅ Success (1 < 2 and 2Gi < 4Gi)

---

# 2. NodeAffinity（节点亲和性）
算法: 检查Pod的节点亲和性是否匹配节点标签

匹配规则:
  requiredDuringSchedulingIgnoredDuringExecution:
    - 必须满足（硬约束）
    - 不满足则过滤该节点

  preferredDuringSchedulingIgnoredDuringExecution:
    - 尽量满足（软约束）
    - 用于打分阶段

示例:
  Pod亲和性:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
      - matchExpressions:
        - key: disktype
          operator: In
          values: ["ssd"]

  Node标签:
    disktype: ssd

  结果: ✅ Success

---

# 3. PodTopologySpread（拓扑分布）
算法: 检查Pod在拓扑域的分布是否满足约束

计算公式:
  Skew = PodCountInDomain - MinPodCount

  if Skew > MaxSkew:
      return Fail
  else:
      return Success

示例:
  拓扑约束:
    maxSkew: 1
    topologyKey: zone
    whenUnsatisfiable: DoNotSchedule

  当前分布:
    zone-a: 2 pods
    zone-b: 1 pod
    zone-c: 1 pod

  调度到zone-a:
    Skew = 3 - 1 = 2 > 1
    结果: ❌ Fail

  调度到zone-b:
    Skew = 2 - 1 = 1 <= 1
    结果: ✅ Success

---

# 4. TaintToleration（污点容忍）
算法: 检查Pod的容忍度是否匹配节点的污点

匹配规则:
  对于节点的每个污点:
    - 如果Pod有匹配的容忍度，则忽略该污点
    - 如果没有匹配的容忍度:
      - Effect=NoSchedule: 不可调度
      - Effect=PreferNoSchedule: 尽量不调度（软约束）
      - Effect=NoExecute: 不可调度且驱逐已有Pod

示例:
  Node污点:
    - key: gpu
      value: "true"
      effect: NoSchedule

  Pod容忍度:
    - key: gpu
      operator: Equal
      value: "true"
      effect: NoSchedule

  结果: ✅ Success (容忍度匹配)

---

# 5. VolumeBinding（存储卷绑定）
算法: 检查Pod需要的PV是否可以绑定到该节点

检查项:
  1. PVC是否已绑定到PV
  2. PV的节点亲和性是否匹配当前节点
  3. 动态PV: StorageClass是否支持该节点
  4. 本地PV: 是否在该节点上

示例:
  PVC需求:
    storageClassName: local-storage
    accessMode: ReadWriteOnce

  PV (Node 1):
    nodeAffinity:
      required:
        nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: In
            values: ["node-1"]

  调度到Node 1: ✅ Success
  调度到Node 2: ❌ Fail (PV不在Node 2上)

---

# 6. InterPodAffinity（Pod间亲和性）
算法: 检查Pod间的亲和性和反亲和性

匹配规则:
  requiredDuringSchedulingIgnoredDuringExecution:
    - 亲和性: 必须与匹配的Pod在同一拓扑域
    - 反亲和性: 不能与匹配的Pod在同一拓扑域

  preferredDuringSchedulingIgnoredDuringExecution:
    - 软约束，用于打分

示例:
  Pod反亲和性:
    requiredDuringSchedulingIgnoredDuringExecution:
    - labelSelector:
        matchLabels:
          app: web
      topologyKey: kubernetes.io/hostname

  Node 1上有Pod (app=web)
  Node 2上没有Pod (app=web)

  调度到Node 1: ❌ Fail (违反反亲和性)
  调度到Node 2: ✅ Success
```

**2. Score算法（优选）**

Score阶段给每个候选节点打分，选择得分最高的节点。

**核心Score插件**：

```yaml
# 1. NodeResourcesBalancedAllocation（资源均衡分配）
算法: 节点的CPU和内存使用率越接近，分数越高

计算公式:
  CPUFraction = (PodRequestCPU + NodeAllocatedCPU) / NodeAllocatableCPU
  MemoryFraction = (PodRequestMemory + NodeAllocatedMemory) / NodeAllocatableMemory

  Score = 100 - abs(CPUFraction - MemoryFraction) * 100

示例:
  Node 1:
    CPU: 50% 使用
    Memory: 40% 使用
    差值: |50% - 40%| = 10%
    分数: 100 - 10 = 90

  Node 2:
    CPU: 80% 使用
    Memory: 30% 使用
    差值: |80% - 30%| = 50%
    分数: 100 - 50 = 50

  Node 1得分更高 ✅

---

# 2. NodeResourcesLeastAllocated（最少分配）
算法: 节点空闲资源越多，分数越高

计算公式:
  CPUScore = (NodeAllocatableCPU - NodeAllocatedCPU) / NodeAllocatableCPU * 100
  MemoryScore = (NodeAllocatableMemory - NodeAllocatedMemory) / NodeAllocatableMemory * 100

  Score = (CPUScore + MemoryScore) / 2

示例:
  Node 1:
    CPU空闲: 50%
    Memory空闲: 60%
    分数: (50 + 60) / 2 = 55

  Node 2:
    CPU空闲: 70%
    Memory空闲: 80%
    分数: (70 + 80) / 2 = 75

  Node 2得分更高 ✅

---

# 3. ImageLocality（镜像本地性）
算法: 镜像已在节点上，分数越高（减少拉取时间）

计算公式:
  ImageSize = sum(已在节点的镜像大小)
  MaxImageSize = 1000Mi (配置值)

  Score = min(ImageSize / MaxImageSize * 100, 100)

示例:
  Pod镜像: nginx:1.21 (50Mi), app:1.0 (200Mi)

  Node 1:
    已有镜像: nginx:1.21 (50Mi)
    分数: 50Mi / 1000Mi * 100 = 5

  Node 2:
    已有镜像: nginx:1.21 (50Mi), app:1.0 (200Mi)
    分数: 250Mi / 1000Mi * 100 = 25

  Node 2得分更高 ✅

---

# 4. NodeAffinity（节点亲和性打分）
算法: 节点满足preferredDuringScheduling越多，分数越高

计算公式:
  Score = sum(满足的preferred条件的权重) / sum(所有preferred条件的权重) * 100

示例:
  Pod节点亲和性:
    preferredDuringSchedulingIgnoredDuringExecution:
    - weight: 50
      preference:
        matchExpressions:
        - key: disktype
          operator: In
          values: ["ssd"]
    - weight: 30
      preference:
        matchExpressions:
        - key: zone
          operator: In
          values: ["zone-a"]

  Node 1标签:
    disktype: ssd
    zone: zone-b
    满足: disktype (权重50)
    分数: 50 / 80 * 100 = 62.5

  Node 2标签:
    disktype: ssd
    zone: zone-a
    满足: disktype + zone (权重80)
    分数: 80 / 80 * 100 = 100

  Node 2得分更高 ✅

---

# 5. InterPodAffinity（Pod间亲和性打分）
算法: 根据Pod的preferred亲和性/反亲和性打分

计算公式:
  对于每个preferred规则:
    - 统计满足条件的Pod数量
    - 分数 += 权重 * 满足数量

  最终归一化到0-100

示例:
  Pod亲和性:
    preferredDuringSchedulingIgnoredDuringExecution:
    - weight: 100
      podAffinityTerm:
        labelSelector:
          matchLabels:
            app: cache
        topologyKey: kubernetes.io/hostname

  Node 1: 有2个 app=cache 的Pod
    分数: 100 * 2 = 200 (归一化后100)

  Node 2: 没有 app=cache 的Pod
    分数: 100 * 0 = 0

  Node 1得分更高 ✅

---

# 6. PodTopologySpread（拓扑分布打分）
算法: 使拓扑域的Pod分布更均衡

计算公式:
  Skew = PodCountInDomain - MinPodCount
  Score = (MaxSkew - Skew) / MaxSkew * 100

示例:
  拓扑约束:
    maxSkew: 2
    topologyKey: zone
    whenUnsatisfiable: ScheduleAnyway

  当前分布:
    zone-a: 3 pods (MinPodCount)
    zone-b: 4 pods
    zone-c: 5 pods

  调度到zone-a:
    Skew = 4 - 3 = 1
    分数: (2 - 1) / 2 * 100 = 50

  调度到zone-c:
    Skew = 6 - 3 = 3
    分数: (2 - 3) / 2 * 100 = -50 (取0)

  zone-a得分更高 ✅
```

**插件权重配置**：

```yaml
# 配置插件权重
apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
profiles:
- schedulerName: default-scheduler
  plugins:
    score:
      enabled:
      - name: NodeResourcesBalancedAllocation
        weight: 1
      - name: NodeResourcesLeastAllocated
        weight: 1
      - name: ImageLocality
        weight: 5  # 镜像本地性权重更高
      - name: InterPodAffinity
        weight: 2
      - name: NodeAffinity
        weight: 2
      - name: PodTopologySpread
        weight: 2
```

**最终得分计算**：

```
最终得分 =
  NodeResourcesBalancedAllocation * 1 +
  NodeResourcesLeastAllocated * 1 +
  ImageLocality * 5 +
  InterPodAffinity * 2 +
  NodeAffinity * 2 +
  PodTopologySpread * 2

归一化到0-100范围
```

---

**当前第15章进度**：
- ✅ 15.1 调度器架构深入（完成）
- ⏳ 15.2 调度框架（Scheduling Framework）（进行中）
- 📅 15.3 自定义调度器开发（待编写）
- 📅 15.4 调度器插件开发（待编写）
- 📅 15.5 多调度器共存（待编写）
- 📅 15.6 实战：GPU资源调度器（待编写）

---

## 15.2 调度框架（Scheduling Framework）

### 15.2.1 调度框架概述

**什么是调度框架**？

Kubernetes Scheduling Framework是在Kubernetes 1.15版本引入的可插拔调度器架构，它将调度器的核心逻辑抽象为一系列**扩展点（Extension Points）**，开发者可以通过实现插件接口来定制调度行为。

**为什么需要调度框架**？

在调度框架之前，定制调度器有两种方式：
1. **Fork调度器代码**：维护成本高，难以跟随K8s版本升级
2. **编写独立调度器**：需要重新实现大量基础逻辑

调度框架通过插件化设计解决了这些问题：
- ✅ **低侵入性**：无需修改核心代码
- ✅ **高复用性**：可复用内置插件
- ✅ **易维护性**：跟随K8s版本自动升级
- ✅ **高灵活性**：可在多个扩展点注入逻辑

**架构图**：

```
调度框架工作流程：
┌─────────────────────────────────────────────────────────────┐
│                       Scheduling Cycle                       │
├──────────────┬──────────────┬──────────────┬────────────────┤
│   Queue      │   Filter     │    Score     │    Reserve     │
│   Sort       │   (Predicate)│   (Priority) │                │
├──────────────┼──────────────┼──────────────┼────────────────┤
│              │              │              │                │
│ ┌──────────┐ │ ┌──────────┐ │ ┌──────────┐ │ ┌────────────┐│
│ │PreFilter │ │ │  Filter  │ │ │PostFilter│ │ │  Reserve   ││
│ └──────────┘ │ └──────────┘ │ └──────────┘ │ └────────────┘│
│      ↓       │      ↓       │      ↓       │       ↓        │
│ ┌──────────┐ │ ┌──────────┐ │ ┌──────────┐ │ ┌────────────┐│
│ │QueueSort │ │ │PreScore  │ │ │  Score   │ │ │  Permit    ││
│ └──────────┘ │ └──────────┘ │ └──────────┘ │ └────────────┘│
│              │              │      ↓       │                │
│              │              │ ┌──────────┐ │                │
│              │              │ │Normalize │ │                │
│              │              │ └──────────┘ │                │
└──────────────┴──────────────┴──────────────┴────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────┐
│                       Binding Cycle                          │
├──────────────┬──────────────┬──────────────┬────────────────┤
│  PreBind     │     Bind     │   PostBind   │   Unreserve    │
│              │              │              │   (on failure) │
└──────────────┴──────────────┴──────────────┴────────────────┘
```

### 15.2.2 扩展点详解

调度框架定义了**11个扩展点**，按调度阶段分为两个周期：

#### 1. QueueSort（队列排序）

**作用**：定义调度队列中Pod的排序规则

**接口定义**：
```go
type QueueSortPlugin interface {
    Plugin
    Less(*QueuedPodInfo, *QueuedPodInfo) bool
}
```

**实现示例**：
```go
// 优先级排序插件
type PrioritySort struct{}

func (pl *PrioritySort) Less(pInfo1, pInfo2 *framework.QueuedPodInfo) bool {
    p1 := corev1helpers.PodPriority(pInfo1.Pod)
    p2 := corev1helpers.PodPriority(pInfo2.Pod)
    return p1 > p2  // 高优先级在前
}
```

**使用场景**：
- 实现自定义优先级规则
- 基于业务属性排序（如租户级别、SLA等级）
- 实现公平调度（防止低优先级Pod饥饿）

---

#### 2. PreFilter（预过滤）

**作用**：
- 预处理Pod信息
- 提前计算可复用的数据（如Pod亲和性相关Pod列表）
- 快速拒绝明显无法调度的Pod

**接口定义**：
```go
type PreFilterPlugin interface {
    Plugin
    PreFilter(ctx context.Context, state *CycleState, p *v1.Pod) *Status
    PreFilterExtensions() PreFilterExtensions
}

type PreFilterExtensions interface {
    AddPod(ctx context.Context, state *CycleState,
           podToSchedule *v1.Pod, podToAdd *v1.Pod,
           nodeInfo *NodeInfo) *Status
    RemovePod(ctx context.Context, state *CycleState,
              podToSchedule *v1.Pod, podToRemove *v1.Pod,
              nodeInfo *NodeInfo) *Status
}
```

**实现示例**：
```go
// Pod间亲和性预过滤插件
type InterPodAffinity struct{}

func (pl *InterPodAffinity) PreFilter(ctx context.Context,
    state *CycleState, pod *v1.Pod) *Status {

    // 提前计算所有相关的Pod列表
    affinity := pod.Spec.Affinity
    if affinity == nil || affinity.PodAffinity == nil {
        return nil  // 无亲和性要求，直接返回
    }

    // 查找所有匹配亲和性规则的Pod
    allPods, err := pl.sharedLister.Pods().List(labels.Everything())
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    // 构建状态数据供Filter阶段使用
    affinityState := &affinityState{
        matchingPods: filterMatchingPods(allPods, affinity),
    }
    state.Write(affinityStateKey, affinityState)

    return nil
}
```

**使用场景**：
- 提前计算复杂信息（避免在每个节点重复计算）
- 快速拒绝（如Pod请求的资源超过集群总资源）
- 数据预处理（如构建拓扑域映射）

---

#### 3. Filter（过滤）

**作用**：过滤不满足Pod调度要求的节点

**接口定义**：
```go
type FilterPlugin interface {
    Plugin
    Filter(ctx context.Context, state *CycleState,
           pod *v1.Pod, nodeInfo *NodeInfo) *Status
}
```

**实现示例**：
```go
// 自定义GPU过滤插件
type GPUFilter struct{}

func (pl *GPUFilter) Filter(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeInfo *framework.NodeInfo) *Status {

    // 检查Pod是否需要GPU
    gpuRequest := getGPURequest(pod)
    if gpuRequest == 0 {
        return nil  // 不需要GPU，通过
    }

    // 检查节点GPU资源
    node := nodeInfo.Node()
    gpuCapacity := getGPUCapacity(node)
    gpuAllocated := getGPUAllocated(nodeInfo)

    gpuAvailable := gpuCapacity - gpuAllocated
    if gpuAvailable < gpuRequest {
        return framework.NewStatus(
            framework.Unschedulable,
            fmt.Sprintf("Insufficient GPU: need %d, available %d",
                        gpuRequest, gpuAvailable),
        )
    }

    // 检查GPU型号匹配
    requiredGPUModel := pod.Annotations["gpu-model"]
    if requiredGPUModel != "" {
        nodeGPUModel := node.Labels["gpu-model"]
        if nodeGPUModel != requiredGPUModel {
            return framework.NewStatus(
                framework.Unschedulable,
                fmt.Sprintf("GPU model mismatch: need %s, got %s",
                            requiredGPUModel, nodeGPUModel),
            )
        }
    }

    return nil  // 通过过滤
}
```

**内置Filter插件**：
```go
// K8s内置的核心Filter插件
var defaultFilterPlugins = []string{
    "NodeUnschedulable",      // 检查节点是否可调度
    "NodeResourcesFit",       // 检查节点资源是否充足
    "NodeName",               // 检查nodeName字段
    "NodePorts",              // 检查端口冲突
    "NodeAffinity",           // 检查节点亲和性
    "VolumeRestrictions",     // 检查卷限制
    "TaintToleration",        // 检查污点容忍
    "EBSLimits",              // 检查EBS卷数量限制
    "GCEPDLimits",            // 检查GCE PD卷限制
    "AzureDiskLimits",        // 检查Azure磁盘限制
    "VolumeBinding",          // 检查卷绑定
    "VolumeZone",             // 检查卷区域
    "PodTopologySpread",      // 检查拓扑分布约束
    "InterPodAffinity",       // 检查Pod间亲和性
}
```

---

#### 4. PostFilter（后过滤）

**作用**：
- 当所有节点都被Filter插件拒绝后调用
- 尝试抢占（Preemption）或其他补救措施

**接口定义**：
```go
type PostFilterPlugin interface {
    Plugin
    PostFilter(ctx context.Context, state *CycleState,
               pod *v1.Pod, filteredNodeStatusMap NodeToStatusMap)
               (*PostFilterResult, *Status)
}

type PostFilterResult struct {
    NominatedNodeName string
}
```

**实现示例**：
```go
// 默认抢占插件
type DefaultPreemption struct{}

func (pl *DefaultPreemption) PostFilter(ctx context.Context,
    state *CycleState, pod *v1.Pod,
    filteredNodeStatusMap framework.NodeToStatusMap) (*framework.PostFilterResult, *Status) {

    // 仅对高优先级Pod执行抢占
    if !isPodEligibleForPreemption(pod) {
        return nil, framework.NewStatus(framework.Unschedulable,
            "preemption is not helpful for this pod")
    }

    // 查找可抢占的节点
    candidateNodes := pl.findCandidates(ctx, pod, filteredNodeStatusMap)
    if len(candidateNodes) == 0 {
        return nil, framework.NewStatus(framework.Unschedulable,
            "no nodes available for preemption")
    }

    // 选择最佳抢占节点（驱逐最少的Pod）
    bestNode := selectBestNode(candidateNodes)

    // 执行抢占：删除低优先级Pod
    victimsToEvict := pl.selectVictims(bestNode, pod)
    for _, victim := range victimsToEvict {
        if err := pl.client.CoreV1().Pods(victim.Namespace).
            Delete(ctx, victim.Name, metav1.DeleteOptions{}); err != nil {
            return nil, framework.NewStatus(framework.Error, err.Error())
        }
    }

    // 返回提名节点
    return &framework.PostFilterResult{
        NominatedNodeName: bestNode.Name,
    }, framework.NewStatus(framework.Success)
}
```

**使用场景**：
- 实现自定义抢占策略
- 触发集群自动扩容（与Cluster Autoscaler集成）
- 记录调度失败原因

---

#### 5. PreScore（预打分）

**作用**：
- 为Score阶段预处理数据
- 计算可复用的全局信息

**接口定义**：
```go
type PreScorePlugin interface {
    Plugin
    PreScore(ctx context.Context, state *CycleState,
             pod *v1.Pod, nodes []*v1.Node) *Status
}
```

**实现示例**：
```go
// 服务拓扑感知预打分
type ServiceTopology struct{}

func (pl *ServiceTopology) PreScore(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodes []*v1.Node) *Status {

    // 获取Pod关联的Service
    services, err := pl.getServicesForPod(pod)
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    // 构建服务端点拓扑分布
    topologyMap := make(map[string][]string)  // zone -> endpoints
    for _, svc := range services {
        endpoints := pl.getServiceEndpoints(svc)
        for _, ep := range endpoints {
            zone := ep.Zone
            topologyMap[zone] = append(topologyMap[zone], ep.IP)
        }
    }

    // 存储到状态中供Score使用
    state.Write(serviceTopologyKey, topologyMap)

    return nil
}
```

---

#### 6. Score（打分）

**作用**：为通过Filter的节点打分

**接口定义**：
```go
type ScorePlugin interface {
    Plugin
    Score(ctx context.Context, state *CycleState,
          p *v1.Pod, nodeName string) (int64, *Status)
    ScoreExtensions() ScoreExtensions
}

type ScoreExtensions interface {
    NormalizeScore(ctx context.Context, state *CycleState,
                   p *v1.Pod, scores NodeScoreList) *Status
}
```

**实现示例**：
```go
// 数据本地化打分插件
type DataLocality struct{}

func (pl *DataLocality) Score(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) (int64, *Status) {

    // 获取Pod需要访问的数据集
    datasetName := pod.Annotations["dataset"]
    if datasetName == "" {
        return 0, nil  // 无数据本地化要求
    }

    // 检查节点是否有数据缓存
    node := pl.nodeInfoMap[nodeName]
    cachedDatasets := node.Labels["cached-datasets"]

    var score int64 = 0
    if strings.Contains(cachedDatasets, datasetName) {
        // 数据在本地，高分
        score = 100
    } else {
        // 计算网络距离分数
        distance := pl.getNetworkDistance(node, datasetName)
        score = max(0, 100 - distance*10)
    }

    return score, nil
}

func (pl *DataLocality) NormalizeScore(ctx context.Context,
    state *CycleState, pod *v1.Pod, scores framework.NodeScoreList) *Status {

    // 归一化到0-100范围
    var highest int64 = 0
    for _, score := range scores {
        if score.Score > highest {
            highest = score.Score
        }
    }

    if highest == 0 {
        return nil
    }

    for i := range scores {
        scores[i].Score = scores[i].Score * framework.MaxNodeScore / highest
    }

    return nil
}
```

**内置Score插件权重**：
```yaml
apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
profiles:
- pluginConfig:
  - name: NodeResourcesBalancedAllocation
    args:
      resources:
      - name: cpu
        weight: 1
      - name: memory
        weight: 1
  - name: ImageLocality
    args:
      weight: 5  # 镜像本地性权重较高
```

---

#### 7. Reserve（预留）

**作用**：
- 在绑定前预留节点资源
- 实现乐观并发控制

**接口定义**：
```go
type ReservePlugin interface {
    Plugin
    Reserve(ctx context.Context, state *CycleState,
            p *v1.Pod, nodeName string) *Status
    Unreserve(ctx context.Context, state *CycleState,
              p *v1.Pod, nodeName string)
}
```

**实现示例**：
```go
// GPU资源预留插件
type GPUReserve struct {
    gpuAllocator *GPUAllocator
}

func (pl *GPUReserve) Reserve(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) *Status {

    gpuRequest := getGPURequest(pod)
    if gpuRequest == 0 {
        return nil  // 不需要GPU
    }

    // 预留GPU资源
    gpuIDs, err := pl.gpuAllocator.Allocate(nodeName, gpuRequest)
    if err != nil {
        return framework.NewStatus(framework.Error,
            fmt.Sprintf("failed to reserve GPU: %v", err))
    }

    // 保存分配信息
    state.Write(gpuAllocationKey, gpuIDs)

    klog.Infof("Reserved %d GPUs on node %s for pod %s: %v",
               gpuRequest, nodeName, pod.Name, gpuIDs)

    return nil
}

func (pl *GPUReserve) Unreserve(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) {

    // 读取之前分配的GPU
    data, err := state.Read(gpuAllocationKey)
    if err != nil {
        return
    }

    gpuIDs := data.([]int)

    // 释放GPU资源
    pl.gpuAllocator.Release(nodeName, gpuIDs)

    klog.Infof("Unreserved GPUs on node %s for pod %s: %v",
               nodeName, pod.Name, gpuIDs)
}
```

---

#### 8. Permit（许可）

**作用**：
- 最后的守门人，可以批准、拒绝或等待
- 实现批量调度（Gang Scheduling）

**接口定义**：
```go
type PermitPlugin interface {
    Plugin
    Permit(ctx context.Context, state *CycleState,
           p *v1.Pod, nodeName string) (*Status, time.Duration)
}

// 返回状态:
// - Success: 立即允许绑定
// - Unschedulable: 拒绝调度
// - Wait: 等待指定时间（用于Gang调度）
```

**实现示例 - Gang调度**：
```go
// Gang调度插件：确保一组Pod同时调度
type Coscheduling struct {
    frameworkHandle framework.Handle
    waitingPods     map[string]*waitingPod  // podGroupID -> waiting pods
}

type waitingPod struct {
    pods          []*v1.Pod
    minAvailable  int
    waitTimeout   time.Duration
}

func (pl *Coscheduling) Permit(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) (*framework.Status, time.Duration) {

    // 检查是否属于PodGroup
    podGroupName := pod.Labels["pod-group.scheduling.sigs.k8s.io/name"]
    if podGroupName == "" {
        return framework.NewStatus(framework.Success), 0  // 单独调度
    }

    // 获取PodGroup定义
    podGroup, err := pl.getPodGroup(pod.Namespace, podGroupName)
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error()), 0
    }

    minAvailable := podGroup.Spec.MinMember

    // 记录已调度的Pod
    pl.mu.Lock()
    defer pl.mu.Unlock()

    key := fmt.Sprintf("%s/%s", pod.Namespace, podGroupName)
    waiting, exists := pl.waitingPods[key]
    if !exists {
        waiting = &waitingPod{
            pods:         []*v1.Pod{},
            minAvailable: minAvailable,
            waitTimeout:  60 * time.Second,
        }
        pl.waitingPods[key] = waiting
    }

    waiting.pods = append(waiting.pods, pod)

    // 检查是否满足最小数量
    if len(waiting.pods) >= minAvailable {
        // 批准所有等待的Pod
        for _, p := range waiting.pods {
            pl.frameworkHandle.IterateOverWaitingPods(func(wp framework.WaitingPod) {
                if wp.GetPod().UID == p.UID {
                    wp.Allow(pl.Name())  // 解除等待
                }
            })
        }
        delete(pl.waitingPods, key)
        return framework.NewStatus(framework.Success), 0
    }

    // 继续等待
    klog.Infof("Pod %s waiting for gang scheduling: %d/%d",
               pod.Name, len(waiting.pods), minAvailable)

    return framework.NewStatus(framework.Wait), waiting.waitTimeout
}
```

**Gang调度示例配置**：
```yaml
# PodGroup CRD定义
apiVersion: scheduling.sigs.k8s.io/v1alpha1
kind: PodGroup
metadata:
  name: tf-job-group
  namespace: default
spec:
  minMember: 3  # 至少3个Pod同时调度
  scheduleTimeoutSeconds: 60

---
# 应用Pod配置
apiVersion: v1
kind: Pod
metadata:
  name: tf-worker-1
  labels:
    pod-group.scheduling.sigs.k8s.io/name: tf-job-group
spec:
  schedulerName: gang-scheduler
  containers:
  - name: tensorflow
    image: tensorflow/tensorflow:latest
```

---

#### 9. PreBind（预绑定）

**作用**：
- 在真正绑定前执行准备工作
- 典型用例：绑定网络资源、挂载存储卷

**接口定义**：
```go
type PreBindPlugin interface {
    Plugin
    PreBind(ctx context.Context, state *CycleState,
            p *v1.Pod, nodeName string) *Status
}
```

**实现示例 - VolumeBinding**：
```go
// 卷绑定插件（K8s内置）
type VolumeBinding struct {
    volumeBinder *volumebinder.VolumeBinder
}

func (pl *VolumeBinding) PreBind(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) *Status {

    // 获取Pod的PVC
    podVolumes, err := pl.volumeBinder.GetPodVolumes(pod)
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    // 为每个PVC绑定PV
    for _, pvc := range podVolumes.StaticBindings {
        klog.Infof("Binding PVC %s to PV %s", pvc.Name, pvc.VolumeName)

        if err := pl.volumeBinder.BindPodVolumes(pod, pvc); err != nil {
            return framework.NewStatus(framework.Error,
                fmt.Sprintf("failed to bind volume: %v", err))
        }
    }

    // 动态供应PV
    for _, pvc := range podVolumes.DynamicProvisions {
        klog.Infof("Provisioning volume for PVC %s", pvc.Name)

        if err := pl.volumeBinder.ProvisionVolume(pod, pvc); err != nil {
            return framework.NewStatus(framework.Error,
                fmt.Sprintf("failed to provision volume: %v", err))
        }
    }

    return nil
}
```

**自定义网络预绑定示例**：
```go
// 网络IP分配插件
type NetworkPreBind struct {
    ipam *IPAMClient
}

func (pl *NetworkPreBind) PreBind(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) *Status {

    // 检查是否需要静态IP
    needStaticIP := pod.Annotations["network.static-ip"] == "true"
    if !needStaticIP {
        return nil
    }

    // 从IPAM分配IP
    subnet := pod.Annotations["network.subnet"]
    ip, err := pl.ipam.AllocateIP(subnet, pod)
    if err != nil {
        return framework.NewStatus(framework.Error,
            fmt.Sprintf("failed to allocate IP: %v", err))
    }

    // 更新Pod注解
    if pod.Annotations == nil {
        pod.Annotations = make(map[string]string)
    }
    pod.Annotations["network.allocated-ip"] = ip

    klog.Infof("Allocated IP %s for pod %s", ip, pod.Name)

    return nil
}
```

---

#### 10. Bind（绑定）

**作用**：将Pod绑定到节点

**接口定义**：
```go
type BindPlugin interface {
    Plugin
    Bind(ctx context.Context, state *CycleState,
         p *v1.Pod, nodeName string) *Status
}
```

**默认实现**：
```go
// K8s默认绑定插件
type DefaultBinder struct {
    client clientset.Interface
}

func (pl *DefaultBinder) Bind(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) *Status {

    klog.V(3).Infof("Attempting to bind %s to %s", pod.Name, nodeName)

    binding := &v1.Binding{
        ObjectMeta: metav1.ObjectMeta{
            Name:      pod.Name,
            Namespace: pod.Namespace,
            UID:       pod.UID,
        },
        Target: v1.ObjectReference{
            Kind: "Node",
            Name: nodeName,
        },
    }

    err := pl.client.CoreV1().Pods(pod.Namespace).Bind(ctx, binding, metav1.CreateOptions{})
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    return nil
}
```

**自定义绑定示例**：
```go
// 虚拟节点绑定插件（适用于Virtual Kubelet）
type VirtualNodeBinder struct {
    client clientset.Interface
}

func (pl *VirtualNodeBinder) Bind(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) *Status {

    // 检查是否是虚拟节点
    node, err := pl.client.CoreV1().Nodes().Get(ctx, nodeName, metav1.GetOptions{})
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    if node.Labels["type"] != "virtual-kubelet" {
        return framework.NewStatus(framework.Skip, "not a virtual node")
    }

    // 调用Virtual Kubelet API（而非标准Bind）
    virtualKubeletURL := node.Annotations["virtual-kubelet-url"]
    if err := pl.bindToVirtualKubelet(virtualKubeletURL, pod); err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    // 更新Pod状态
    pod.Spec.NodeName = nodeName
    pod.Status.Phase = v1.PodPending

    _, err = pl.client.CoreV1().Pods(pod.Namespace).UpdateStatus(ctx, pod, metav1.UpdateOptions{})
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    return nil
}
```

---

#### 11. PostBind（后绑定）

**作用**：
- 绑定成功后执行收尾工作
- 通常用于清理、日志、指标记录

**接口定义**：
```go
type PostBindPlugin interface {
    Plugin
    PostBind(ctx context.Context, state *CycleState,
             p *v1.Pod, nodeName string)
}
```

**实现示例**：
```go
// 调度指标记录插件
type SchedulingMetrics struct {
    metricsRecorder *MetricsRecorder
}

func (pl *SchedulingMetrics) PostBind(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) {

    // 计算调度延迟
    scheduleLatency := time.Since(pod.CreationTimestamp.Time)

    // 记录指标
    pl.metricsRecorder.RecordScheduleLatency(
        pod.Namespace,
        pod.Labels,
        scheduleLatency,
    )

    // 记录节点选择
    pl.metricsRecorder.RecordNodeSelection(nodeName, pod.Spec.PriorityClassName)

    // 记录调度事件
    pl.eventRecorder.Eventf(pod, v1.EventTypeNormal, "Scheduled",
        "Successfully assigned %s/%s to %s (latency: %v)",
        pod.Namespace, pod.Name, nodeName, scheduleLatency)

    klog.V(2).Infof("Pod %s/%s bound to %s in %v",
                    pod.Namespace, pod.Name, nodeName, scheduleLatency)
}
```

**资源清理示例**：
```go
// GPU资源清理插件
type GPUCleanup struct{}

func (pl *GPUCleanup) PostBind(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeName string) {

    // 从状态中读取GPU分配信息
    data, err := state.Read(gpuAllocationKey)
    if err != nil {
        return  // 没有GPU分配
    }

    gpuIDs := data.([]int)

    // 持久化GPU分配（写入etcd）
    annotation := fmt.Sprintf("gpu-ids: %v", gpuIDs)
    pl.updatePodAnnotation(ctx, pod, "allocated-gpus", annotation)

    klog.Infof("Persisted GPU allocation for pod %s: %v", pod.Name, gpuIDs)
}
```

---

### 15.2.3 调度上下文（CycleState）

**CycleState作用**：
- 在调度周期的各个扩展点之间传递数据
- 避免重复计算
- 存储中间结果

**数据结构**：
```go
// CycleState provides a mechanism for plugins to store and retrieve arbitrary data.
type CycleState struct {
    mx      sync.RWMutex
    storage map[StateKey]StateData
    recordPluginMetrics bool
}

type StateKey string
type StateData interface {
    Clone() StateData
}
```

**使用示例**：
```go
// 1. PreFilter阶段写入数据
func (pl *InterPodAffinity) PreFilter(ctx context.Context,
    state *CycleState, pod *v1.Pod) *Status {

    affinityState := &affinityState{
        matchingPods: pl.findMatchingPods(pod),
    }

    state.Write(affinityStateKey, affinityState)  // 写入
    return nil
}

// 2. Filter阶段读取数据
func (pl *InterPodAffinity) Filter(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeInfo *NodeInfo) *Status {

    // 读取PreFilter阶段计算的数据
    data, err := state.Read(affinityStateKey)
    if err != nil {
        return framework.NewStatus(framework.Error, err.Error())
    }

    affinityState := data.(*affinityState)

    // 使用预计算的数据
    if !pl.satisfiesAffinity(nodeInfo, affinityState.matchingPods) {
        return framework.NewStatus(framework.Unschedulable,
            "node doesn't satisfy pod affinity")
    }

    return nil
}
```

**Clone方法实现**：
```go
type affinityState struct {
    matchingPods []*v1.Pod
}

func (s *affinityState) Clone() framework.StateData {
    clone := &affinityState{
        matchingPods: make([]*v1.Pod, len(s.matchingPods)),
    }
    copy(clone.matchingPods, s.matchingPods)
    return clone
}
```

---

### 15.2.4 插件配置

**配置文件结构**：
```yaml
apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration

# 调度器配置文件
clientConnection:
  kubeconfig: /etc/kubernetes/scheduler.conf

profiles:
- schedulerName: default-scheduler

  # 插件启用/禁用配置
  plugins:
    # QueueSort阶段
    queueSort:
      enabled:
      - name: PrioritySort

    # PreFilter阶段
    preFilter:
      enabled:
      - name: NodeResourcesFit
      - name: NodePorts
      - name: PodTopologySpread
      - name: InterPodAffinity
      - name: VolumeBinding

    # Filter阶段
    filter:
      enabled:
      - name: NodeUnschedulable
      - name: NodeResourcesFit
      - name: NodeName
      - name: NodePorts
      - name: NodeAffinity
      - name: VolumeRestrictions
      - name: TaintToleration
      - name: EBSLimits
      - name: GCEPDLimits
      - name: AzureDiskLimits
      - name: VolumeBinding
      - name: VolumeZone
      - name: PodTopologySpread
      - name: InterPodAffinity
      disabled:
      - name: NodeResourcesBalancedAllocation  # 禁用某插件

    # PostFilter阶段
    postFilter:
      enabled:
      - name: DefaultPreemption

    # PreScore阶段
    preScore:
      enabled:
      - name: InterPodAffinity
      - name: PodTopologySpread
      - name: TaintToleration

    # Score阶段
    score:
      enabled:
      - name: NodeResourcesBalancedAllocation
        weight: 1
      - name: ImageLocality
        weight: 5
      - name: InterPodAffinity
        weight: 2
      - name: NodeResourcesLeastAllocated
        weight: 1
      - name: NodeAffinity
        weight: 2
      - name: PodTopologySpread
        weight: 2
      - name: TaintToleration
        weight: 1

    # Reserve阶段
    reserve:
      enabled:
      - name: VolumeBinding

    # Permit阶段（默认无插件）
    permit:
      enabled: []

    # PreBind阶段
    preBind:
      enabled:
      - name: VolumeBinding

    # Bind阶段
    bind:
      enabled:
      - name: DefaultBinder

  # 插件参数配置
  pluginConfig:
  - name: NodeResourcesFit
    args:
      scoringStrategy:
        type: LeastAllocated
        resources:
        - name: cpu
          weight: 1
        - name: memory
          weight: 1

  - name: PodTopologySpread
    args:
      defaultConstraints:
      - maxSkew: 1
        topologyKey: zone
        whenUnsatisfiable: ScheduleAnyway
      - maxSkew: 3
        topologyKey: hostname
        whenUnsatisfiable: ScheduleAnyway

  - name: InterPodAffinity
    args:
      hardPodAffinityWeight: 1

  - name: NodeAffinity
    args:
      addedAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
          - matchExpressions:
            - key: node-role.kubernetes.io/control-plane
              operator: DoesNotExist

# 多Profile配置（支持多调度器）
- schedulerName: gpu-scheduler
  plugins:
    filter:
      enabled:
      - name: GPUFilter  # 自定义GPU过滤插件
    score:
      enabled:
      - name: GPUScore   # 自定义GPU打分插件
        weight: 10
```

**应用配置**：
```bash
# 1. 创建ConfigMap
kubectl create configmap scheduler-config \
  --from-file=scheduler-config.yaml \
  -n kube-system

# 2. 修改Scheduler静态Pod配置
vim /etc/kubernetes/manifests/kube-scheduler.yaml

# 添加配置文件挂载
spec:
  containers:
  - command:
    - kube-scheduler
    - --config=/etc/kubernetes/scheduler-config.yaml
    volumeMounts:
    - name: scheduler-config
      mountPath: /etc/kubernetes/scheduler-config.yaml
      subPath: scheduler-config.yaml
      readOnly: true
  volumes:
  - name: scheduler-config
    configMap:
      name: scheduler-config
```

---

### 15.2.5 插件开发完整示例

下面展示一个完整的自定义插件开发流程：

**场景**：开发一个"成本优化调度插件"，优先将Pod调度到价格更低的节点（如Spot实例）

**步骤1：定义插件结构**

```go
package costoptimizer

import (
    "context"
    "fmt"

    v1 "k8s.io/api/core/v1"
    "k8s.io/apimachinery/pkg/runtime"
    "k8s.io/kubernetes/pkg/scheduler/framework"
)

const (
    Name = "CostOptimizer"

    // 状态键
    costStateKey = "CostOptimizerState"
)

// 插件配置参数
type CostOptimizerArgs struct {
    // 价格权重（0-100）
    PriceWeight int `json:"priceWeight"`

    // 性能权重（0-100）
    PerformanceWeight int `json:"performanceWeight"`
}

// 插件主体
type CostOptimizer struct {
    handle framework.Handle
    args   *CostOptimizerArgs
}

// 插件工厂函数
func New(args runtime.Object, handle framework.Handle) (framework.Plugin, error) {
    costArgs, ok := args.(*CostOptimizerArgs)
    if !ok {
        return nil, fmt.Errorf("invalid args type %T", args)
    }

    return &CostOptimizer{
        handle: handle,
        args:   costArgs,
    }, nil
}

// 插件名称
func (co *CostOptimizer) Name() string {
    return Name
}
```

**步骤2：实现PreFilter接口**

```go
// 预过滤：预计算价格信息
func (co *CostOptimizer) PreFilter(ctx context.Context,
    state *framework.CycleState, pod *v1.Pod) *framework.Status {

    // 检查Pod是否要求成本优化
    optimizeCost := pod.Annotations["scheduler.cost-optimizer/enable"]
    if optimizeCost != "true" {
        return nil  // 不需要成本优化
    }

    // 查询所有节点的价格信息（从外部API或ConfigMap）
    priceMap, err := co.getNodePrices()
    if err != nil {
        return framework.NewStatus(framework.Error,
            fmt.Sprintf("failed to get node prices: %v", err))
    }

    // 保存到状态
    costState := &CostState{
        NodePrices: priceMap,
    }
    state.Write(costStateKey, costState)

    return nil
}

func (co *CostOptimizer) PreFilterExtensions() framework.PreFilterExtensions {
    return nil  // 不需要动态更新
}

// 状态数据结构
type CostState struct {
    NodePrices map[string]float64  // nodeName -> price per hour
}

func (s *CostState) Clone() framework.StateData {
    clone := &CostState{
        NodePrices: make(map[string]float64, len(s.NodePrices)),
    }
    for k, v := range s.NodePrices {
        clone.NodePrices[k] = v
    }
    return clone
}
```

**步骤3：实现Filter接口**

```go
// 过滤：排除超出预算的节点
func (co *CostOptimizer) Filter(ctx context.Context,
    state *framework.CycleState, pod *v1.Pod,
    nodeInfo *framework.NodeInfo) *framework.Status {

    // 读取价格信息
    data, err := state.Read(costStateKey)
    if err != nil {
        return nil  // 无价格信息，不过滤
    }

    costState := data.(*CostState)

    // 检查预算限制
    maxBudget := pod.Annotations["scheduler.cost-optimizer/max-budget"]
    if maxBudget == "" {
        return nil  // 无预算限制
    }

    maxPrice, _ := strconv.ParseFloat(maxBudget, 64)
    nodePrice := costState.NodePrices[nodeInfo.Node().Name]

    if nodePrice > maxPrice {
        return framework.NewStatus(framework.Unschedulable,
            fmt.Sprintf("node price $%.2f/hr exceeds budget $%.2f/hr",
                        nodePrice, maxPrice))
    }

    return nil
}
```

**步骤4：实现Score接口**

```go
// 打分：价格越低分数越高
func (co *CostOptimizer) Score(ctx context.Context,
    state *framework.CycleState, pod *v1.Pod,
    nodeName string) (int64, *framework.Status) {

    // 读取价格信息
    data, err := state.Read(costStateKey)
    if err != nil {
        return 0, nil  // 无价格信息，返回中性分数
    }

    costState := data.(*CostState)
    nodePrice := costState.NodePrices[nodeName]

    // 计算价格分数（价格越低分数越高）
    maxPrice := co.getMaxPrice(costState.NodePrices)
    var priceScore int64
    if maxPrice > 0 {
        priceScore = int64((maxPrice - nodePrice) / maxPrice * 100)
    }

    // 获取性能分数（CPU频率、网络带宽等）
    performanceScore := co.getPerformanceScore(nodeName)

    // 加权计算最终分数
    finalScore := (priceScore * int64(co.args.PriceWeight) +
                   performanceScore * int64(co.args.PerformanceWeight)) / 100

    return finalScore, nil
}

func (co *CostOptimizer) ScoreExtensions() framework.ScoreExtensions {
    return co  // 实现NormalizeScore
}

// 归一化
func (co *CostOptimizer) NormalizeScore(ctx context.Context,
    state *framework.CycleState, pod *v1.Pod,
    scores framework.NodeScoreList) *framework.Status {

    var highest int64 = 0
    for _, score := range scores {
        if score.Score > highest {
            highest = score.Score
        }
    }

    if highest == 0 {
        return nil
    }

    for i := range scores {
        scores[i].Score = scores[i].Score * framework.MaxNodeScore / highest
    }

    return nil
}

// 辅助方法
func (co *CostOptimizer) getMaxPrice(prices map[string]float64) float64 {
    var max float64
    for _, price := range prices {
        if price > max {
            max = price
        }
    }
    return max
}

func (co *CostOptimizer) getPerformanceScore(nodeName string) int64 {
    // 从节点标签读取性能指标
    node, _ := co.handle.ClientSet().CoreV1().Nodes().
        Get(context.TODO(), nodeName, metav1.GetOptions{})

    cpuFreq := node.Labels["cpu-frequency"]
    networkBW := node.Labels["network-bandwidth"]

    // 简化的性能计算
    var score int64 = 50  // 默认中等性能

    if cpuFreq == "high" {
        score += 25
    }
    if networkBW == "10Gbps" {
        score += 25
    }

    return score
}

func (co *CostOptimizer) getNodePrices() (map[string]float64, error) {
    // 实际场景中从云服务商API或ConfigMap读取
    // 这里使用节点标签模拟
    nodes, err := co.handle.ClientSet().CoreV1().Nodes().
        List(context.TODO(), metav1.ListOptions{})
    if err != nil {
        return nil, err
    }

    prices := make(map[string]float64)
    for _, node := range nodes.Items {
        priceStr := node.Labels["node-price-per-hour"]
        if priceStr == "" {
            priceStr = "0.1"  // 默认价格
        }
        price, _ := strconv.ParseFloat(priceStr, 64)
        prices[node.Name] = price
    }

    return prices, nil
}
```

**步骤5：注册插件**

```go
package main

import (
    "k8s.io/kubernetes/cmd/kube-scheduler/app"
    "k8s.io/kubernetes/pkg/scheduler/framework/runtime"

    "mycompany.com/scheduler-plugins/pkg/costoptimizer"
)

func main() {
    // 注册插件
    command := app.NewSchedulerCommand(
        app.WithPlugin(costoptimizer.Name, costoptimizer.New),
    )

    if err := command.Execute(); err != nil {
        os.Exit(1)
    }
}
```

**步骤6：编译和部署**

```bash
# 编译
go build -o kube-scheduler-with-plugins main.go

# 制作镜像
cat > Dockerfile <<EOF
FROM k8s.gcr.io/kube-scheduler:v1.28.0
COPY kube-scheduler-with-plugins /usr/local/bin/kube-scheduler
EOF

docker build -t myregistry/kube-scheduler:v1.28.0-costoptimizer .
docker push myregistry/kube-scheduler:v1.28.0-costoptimizer

# 修改Scheduler静态Pod
vim /etc/kubernetes/manifests/kube-scheduler.yaml

# 更新镜像
spec:
  containers:
  - image: myregistry/kube-scheduler:v1.28.0-costoptimizer
```

**步骤7：配置插件**

```yaml
# scheduler-config.yaml
apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration

profiles:
- schedulerName: cost-optimizer-scheduler
  plugins:
    preFilter:
      enabled:
      - name: CostOptimizer
    filter:
      enabled:
      - name: CostOptimizer
    score:
      enabled:
      - name: CostOptimizer
        weight: 10  # 高权重

  pluginConfig:
  - name: CostOptimizer
    args:
      priceWeight: 70        # 价格权重70%
      performanceWeight: 30  # 性能权重30%
```

**步骤8：使用插件**

```yaml
# 节点标签（模拟价格）
kubectl label node node1 node-price-per-hour=0.05  # Spot实例
kubectl label node node2 node-price-per-hour=0.15  # 按需实例
kubectl label node node3 node-price-per-hour=0.10  # 可抢占实例

# 应用Pod
apiVersion: v1
kind: Pod
metadata:
  name: cost-optimized-app
  annotations:
    scheduler.cost-optimizer/enable: "true"
    scheduler.cost-optimizer/max-budget: "0.12"  # 最高$0.12/hr
spec:
  schedulerName: cost-optimizer-scheduler
  containers:
  - name: app
    image: myapp:v1
    resources:
      requests:
        cpu: 500m
        memory: 512Mi
```

**验证**：
```bash
# 查看Pod调度结果
kubectl get pod cost-optimized-app -o wide
# 应该调度到node1（价格最低）

# 查看调度事件
kubectl describe pod cost-optimized-app | grep -A 5 Events
```

---

### 15.2.6 调度框架最佳实践

#### 1. 插件开发原则

```go
// ✅ 好的实践
type MyPlugin struct {
    handle framework.Handle  // 保存handle引用
    cache  *CustomCache      // 使用缓存减少API调用
}

func (p *MyPlugin) Filter(...) *Status {
    // 使用handle访问集群信息
    node, err := p.handle.SnapshotSharedLister().NodeInfos().Get(nodeName)

    // 从缓存读取
    data := p.cache.Get(key)

    // 快速返回
    if simpleCheck() {
        return nil
    }

    // 复杂逻辑
    ...
}

// ❌ 不好的实践
func (p *MyPlugin) Filter(...) *Status {
    // 直接创建clientset（开销大）
    client, _ := kubernetes.NewForConfig(config)

    // 每次都查询API Server（性能差）
    node, _ := client.CoreV1().Nodes().Get(...)

    // 阻塞操作
    time.Sleep(5 * time.Second)

    return nil
}
```

#### 2. 错误处理

```go
func (p *MyPlugin) PreFilter(...) *Status {
    data, err := p.fetchData()

    if err != nil {
        // 区分错误类型
        if errors.Is(err, ErrNotFound) {
            // 资源不存在 - 可调度
            return nil
        } else if errors.Is(err, ErrTemporary) {
            // 临时错误 - 不可调度（可重试）
            return framework.NewStatus(framework.Unschedulable, err.Error())
        } else {
            // 严重错误 - 报错
            return framework.NewStatus(framework.Error, err.Error())
        }
    }

    return nil
}
```

#### 3. 性能优化

```go
// 使用PreFilter预计算
func (p *MyPlugin) PreFilter(ctx context.Context,
    state *CycleState, pod *v1.Pod) *Status {

    // 只计算一次，供所有节点使用
    expensiveData := p.computeExpensiveData(pod)
    state.Write(stateKey, expensiveData)

    return nil
}

func (p *MyPlugin) Filter(ctx context.Context,
    state *CycleState, pod *v1.Pod, nodeInfo *NodeInfo) *Status {

    // 直接读取预计算的数据
    data, _ := state.Read(stateKey)

    // 快速过滤
    return p.quickCheck(data, nodeInfo)
}
```

#### 4. 插件组合

```yaml
# 组合多个插件实现复杂策略
profiles:
- schedulerName: ml-training-scheduler
  plugins:
    filter:
      enabled:
      - name: GPUFilter          # GPU过滤
      - name: InfiniBandFilter   # 高速网络过滤
      - name: DataLocalityFilter # 数据本地化过滤

    score:
      enabled:
      - name: GPUScore
        weight: 10
      - name: NetworkBandwidthScore
        weight: 5
      - name: DataLocalityScore
        weight: 8

    permit:
      enabled:
      - name: GangScheduling  # Gang调度（等待所有Pod就绪）
```

#### 5. 可观测性

```go
func (p *MyPlugin) Filter(...) *Status {
    start := time.Now()
    defer func() {
        // 记录指标
        metrics.PluginExecutionDuration.WithLabelValues(
            p.Name(), "Filter",
        ).Observe(time.Since(start).Seconds())
    }()

    // 记录日志
    klog.V(4).Infof("Filtering node %s for pod %s/%s",
                    nodeName, pod.Namespace, pod.Name)

    result := p.doFilter(...)

    // 记录结果
    if !result.IsSuccess() {
        metrics.PluginFilterRejectionsTotal.WithLabelValues(
            p.Name(), result.Code().String(),
        ).Inc()
    }

    return result
}
```

---

**当前第15章进度**：
- ✅ 15.1 调度器架构深入（完成）
- ✅ 15.2 调度框架（Scheduling Framework）（完成）
- ⏳ 15.3 自定义调度器开发（待编写）
- 📅 15.4 调度器插件开发（待编写）
- 📅 15.5 多调度器共存（待编写）
- 📅 15.6 实战：GPU资源调度器（待编写）

接下来将继续编写15.3节的内容。
